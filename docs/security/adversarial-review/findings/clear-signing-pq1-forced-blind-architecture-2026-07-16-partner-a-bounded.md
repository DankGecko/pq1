BEGIN PARTNER A BOUNDED RESPONSE V1

**Raw ID and origin**
`XB-001` — origin Partner B, post-freeze cross candidate (B cross report SHA-256 `8f2310602fbd36f09994ed1de794c332f0bf1ea85dbd98f6dc1a0f00c6a2e193`, as supplied; I did not read B's other cross conclusions). Identity rechecked before answering: HEAD `9647b79374d5e2e10445254492308101b8be708b`; status = the two expected docs only; untracked 0; ignored files 0; diff `b8e270747a5c16eafde0b74fe94f34be9ab4504d91559234fb2a1ca056d59c25`; worktree read-only. Frozen reports unchanged: A V5 `4b34ae5f…23a884c`, B V4 `bcb8a52e…5f1110f`, A cross V1 `d11bf34e8b6ece6eac442ef26674ea7604c816bbc8dd50804539327f32186e70`. **No drift.**

**Disposition:** **CONFIRMED (NARROWED)**

**Exact reproduced / refuting evidence** — all personally re-opened in the frozen target:

*Reproduced (B's core claim holds):*
- `secure/src/nsc/cmd_sign_userop.rs:13-39` enumerates the handler's modes: **"Deploy (`FLAG_INCLUDE_INIT_CODE` only, slot_index = 0)"**, **"Rotation (`FLAG_REGISTER_SLOT` only, slot_index ≥ 1)"**. Both flags are parsed at `:205` and branched at `:226-228`. `proto/src/lib.rs:1260` / `:1281` define them (`0x8000_0000` / `0x4000_0000`).
- **Rotation emits two signature frames from one request.** `proto/src/lib.rs:1281`, verbatim: *"set by the companion to ask the firmware to emit a **Type 1 slot-registration frame before the Type 2 user-tx frame**."* So the plan's §5 phrase *"single UserOp only"* is **not** equivalent to "one signature emitted" — B's distinction is real.
- **Deploy folds an unrepresented signed input.** `proto:1260`: firmware *"synthesises `initCode` … **folds its hash into the slot-0 Type 2 `userOpHash`**"*; confirmed at `aa/src/userop.rs:694` — `.chain_update(params.init_code_digest)`. **`grep -rn "init_code" secure/src/tx/display/` returns zero hits — `initCode` is never rendered by any renderer.** This is the same defect class as my PA5-5 (`entry_point`) and B's own PB-AR-004: the plan's §4 byte-binding invariant is incomplete, and the forced transcript — whose entire justification is *"exact raw signed context"* — would omit a field the signature commits.
- **The frozen text does not exclude these modes.** §5's fatal enumeration (batch, EIP-712/off-chain, Safe `approveHash`, CoW `setPreSignature`, malformed Safe/MultiSend, delegatecall, malformed envelope/pointers, mandatory-page overflow) **contains no lifecycle entry**. An implementer reading §5 has no textual basis to exclude Deploy or Rotation. The flags are orthogonal to eligibility, which is computed from the inner calldata/descriptor — so `FLAG_REGISTER_SLOT` + a filter-positive tuple with absent metadata is reachable on the frozen text.

*Narrowing (two of B's implicit premises do not hold):*
- **The Type-1 rotation is not unconsented today.** `cmd_sign_userop.rs:1191` `build_slot_rotation_pages(slot_index)` → signer identity proof (`:1194-1205`) → nonce-lane proof (`:1215-1221`) → **its own ceremony at `:1229` `confirm_checked(rotate_pages.as_slice())`**, gated on its own sentinel. So the defect is **scope and transcript representation**, not a missing consent. B's wording ("artifacts not covered by the forced transcript") is precise; any reading that these frames go unconfirmed is not supported.
- **The surface is two enumerated, already-exclusive modes, not an open combinatorial space.** Mutual exclusion is enforced in code, not merely documented: `:279` `if include_init_code && register_slot` → reject; `:283` init_code requires `slot_index == 0`; `:287` register_slot requires `slot_index != 0`; and the flags are **re-parsed at `:306`** (`include_init_code_r, register_slot_r, …`) as an FI re-read. `proto:1260` states the exclusion normatively.
- **Evidence B did not cite that further bounds urgency:** `proto:1281` already carries a production quarantine on the rotation flag — *"Seedless production companions must therefore refuse this flag until a reviewed protocol bump supplies that binding material; they must not submit a no-op Type-1 UserOp or repeatedly harvest fresh bootstrap signatures."* `FLAG_REGISTER_SLOT` is thus already fenced for production companions. This narrows practical exposure; it does **not** repair the frozen architecture text, since the device must not rely on companion compliance.

**Required correction**
Adopt B's correction, with the exclusion placed where it is enforceable rather than in prose. Add to the **fatal enumeration** (my cross §6 taxonomy / PB-AR-005's pre-dispatch classifier), not to a scope sentence:
```
FatalReason ⊇ { IncludeInitCodeSet, RegisterSlotSet, NotExactlyOneSteadyStateType2 }
```
Forced blind is admissible only when `include_init_code == false` **and** `register_slot == false` **and** exactly one steady-state Type-2 signature will be emitted — evaluated from the **FI-re-read** flag values (`:306`), consistent with PA5-1's rule that eligibility is minted affirmatively and never by negation. Lifecycle modes remain fatal for PQ1. Independently: if a lifecycle mode were ever admitted later, `init_code_digest` must first join §4's byte-binding invariant and the frozen transcript schema (with `entry_point`, `maxFeePerGas`, `maxPriorityFeePerGas` per PA5-5 / PB-AR-004) — otherwise a deploy would commit factory, master and slot-0 public keys unseen.

**Stage impact and owner decision**
**Architecture: contributes to the existing block; does not create an independent new one.** XB-001 is another missing member of the same unclosed fatal enumeration already adjudicated blocking under PB-AR-005 (CONFIRMED) and PA5-2 (CONFIRMED) — it sharpens the required correction rather than adding a new blocker. **Implementation/merge: gate** — the acceptance matrix needs `include_init_code=true → fatal` and `register_slot=true → fatal` rows alongside the one-element-batch row. **Production: no independent impact.** **Owner decision remains: NO on PQ1 admissibility** (both partners' positions coincide — lifecycle modes fatal/deferred; I concur and this needs no owner adjudication). **Owner decision remains: YES** on one point only — whether Deploy/Rotation are *permanently* excluded or *deferred behind a stated expansion gate*, which is the same gate governing batch/EIP-712 (plan §7 slice 5).

**Evidence class and honest residual**
**Source-only, no PoC, executed nothing** — no build, no ELF, no QEMU, no hardware; the forced-blind feature does not exist in the tree, so no counterexample is constructible. All quoted lines were personally re-opened in the frozen, read-only target this turn. **Residual:** I verified the Type-1/Type-2 dual-frame behaviour from `proto:1281`'s **normative contract** and from the rotation ceremony at `:1191-1229`; I did **not** trace the response-emission code to confirm byte-for-byte that two signatures are serialized into one response. I did not re-inspect the batch handler's flag treatment. My narrowing rests on reading the enforcement at `:279-287`/`:306` as complete; I did not exhaustively prove no other path sets these flags. This response does not reopen any first-pass row, raises no new candidate, and I have not read Partner B's other cross conclusions.

END PARTNER A BOUNDED RESPONSE V1
