kimi version 0.36.0
• This is a deep adversarial review of an EasyCrypt formalization. The user wants me to attack their placement decision. Read-only. I need to read actual source files: experiments/wots-badenc/, cdrafts-split/WOTS_C_Real.ec, WOTS_C_Interactive.ec, WOTS_C_Scheme.ec, WOTS_TW_ES.ec, GprocQWired.ec, etc.

  This is a large investigation. I should delegate parallel exploration to subagents, but the final synthesis (the 4 questions) requires deep understanding. Let me first look at the key files myself to understand the structure, then delegate detailed verification.

  Given the complexity, I'll use a swarm of explore agents for specific verification tasks, and read the key files myself. Actually, the questions are deep and load-bearing; I should read the core files myself (the countermodel, the charged lemma, the R_int_WOTSTW wiring, the +C scheme) and delegate peripheral verification.

  Let me plan reads:
  1. experiments/wots-badenc/base/BadEncCountermodel.ec
  2. experiments/wots-badenc/base/ — the nhchwcoll_hchwpre_msg file (probably WOTS_TW_ES.ec or similar)
  3. cdrafts-split/WOTS_C_Real.ec:214 region
  4. cdrafts-split/WOTS_C_Interactive.ec around :1753 R_int_WOTSTW
  5. cdrafts-split/WOTS_C_Scheme.ec:141-143
  6. GprocQWired.ec:457 and GprocQWiredWotsCharged.ec
  7. WOTS_TW_ES.ec:1513 region (the original false admit)

  Let me start with a listing of experiments/wots-badenc and read the countermodel.

• I'll start by mapping the new work and reading the key source files.experiments/wots-badenc/:
total 488
drwxrwxrwx 6 nicola nicola   4096 Aug 13 14:07 .
drwxrwxrwx 6 nicola nicola   4096 Aug 12 18:09 ..
drwxrwxrwx 2 nicola nicola   4096 Aug 13 14:28 base
drwxrwxrwx 2 nicola nicola   4096 Aug 13 11:43 cd
-rw-r--r-- 1   1001 root      659 Aug 13 11:32 closure.out
-rw-r--r-- 1   1001 root     6622 Aug 13 14:28 cm.out
drwxrwxrwx 2 nicola nicola   4096 Aug 13 14:07 controls
-rw-r--r-- 1   1001 root     1424 Aug 13 11:42 ctlw.out
-rw-r--r-- 1   1001 root       66 Aug 12 18:47 down.out
-rw-r--r-- 1   1001 root    80402 Aug 13 14:02 dump.out
-rwxrwxrwx 1 nicola nicola    163 Aug 13 13:38 dump.sh
-rw-rw-r-- 1 nicola nicola    227 Aug 13 13:31 .gitignore
-rwxrwxr-x 1 nicola nicola   1781 Aug 13 14:07 mkctl.sh
-rw-r--r-- 1   1001 root     1213 Aug 13 11:41 new.out
-rwxrwxrwx 1 nicola nicola   3887 Aug 12 18:09 PREDICTION.md
drwxrwxrwx 2 nicola nicola   4096 Aug 12 18:19 probe
-rw-r--r-- 1   1001 root    86032 Aug 12 18:19 probe.out
-rwxrwxrwx 1 nicola nicola  16681 Aug 12 21:54 RESULT.md
-rwxrwxrwx 1 nicola nicola    868 Aug 12 21:54 runclosure.sh
-rwxrwxrwx 1 nicola nicola    222 Aug 13 13:34 runcm.sh
-rw-rw-r-- 1 nicola nicola    242 Aug 13 14:07 runctl.sh
-rwxrwxrwx 1 nicola nicola   1295 Aug 13 11:43 runctlw.sh
-rwxrwxrwx 1 nicola nicola    497 Aug 12 18:40 rundown.sh
-rwxrwxrwx 1 nicola nicola    241 Aug 13 11:33 runnew.sh
-rw-r--r-- 1   1001 root    86828 Aug 12 18:39 run.out
-rwxrwxrwx 1 nicola nicola    206 Aug 12 18:16 runprobe.sh
-rwxrwxrwx 1 nicola nicola    198 Aug 12 18:11 run.sh
-rwxrwxrwx 1 nicola nicola    241 Aug 12 21:40 runxm.sh
-rwxrwxr-x 1 nicola nicola   1100 Aug 12 21:47 setup.sh
-rw-r--r-- 1   1001 root   105060 Aug 12 21:53 xm.out

experiments/wots-badenc/base/:
total 1000
drwxrwxrwx 2 nicola nicola   4096 Aug 13 14:28 .
drwxrwxrwx 6 nicola nicola   4096 Aug 13 14:07 ..
-rwxrwxrwx 1 nicola nicola  20389 Aug 13 14:08 BadEncCountermodel.ec
-rw-r--r-- 1   1001 root     6066 Aug 13 14:28 BadEncCountermodel.eco
-rwxrwxrwx 1 nicola nicola  19123 Aug 12 18:10 BinaryTrees.ec
-rwxrwxrwx 1 nicola nicola  67816 Aug 12 18:10 FL_SL_XMSS_MT_ES.ec
-rw-r--r-- 1   1001 root     6414 Aug 12 18:41 FL_SL_XMSS_MT_ES.eco
-rwxrwxrwx 1 nicola nicola 337166 Aug 12 18:10 FORS_ES.ec
-rw-r--r-- 1   1001 root     6434 Aug 12 18:47 FORS_ES.eco
-rwxrwxrwx 1 nicola nicola   3237 Aug 12 18:13 HashAddresses.eca
-rwxrwxrwx 1 nicola nicola  47582 Aug 12 18:13 KeyedHashFunctions.eca
-rwxrwxrwx 1 nicola nicola  11934 Aug 12 18:10 MerkleTrees.ec
-rwxrwxrwx 1 nicola nicola  68457 Aug 12 18:13 OpenPRE_From_TCR_DSPR_THF.eca
-rwxrwxrwx 1 nicola nicola  27074 Aug 12 18:10 PRE_From_SPR_DSPR.ec
-rwxrwxrwx 1 nicola nicola  38611 Aug 12 18:10 SPHINCS_PLUS.ec
-rw-r--r-- 1   1001 root     6921 Aug 12 18:47 SPHINCS_PLUS.eco
-rwxrwxrwx 1 nicola nicola  20391 Aug 12 18:13 TweakableHashFunctions.eca
-rwxrwxrwx 1 nicola nicola 296345 Aug 12 18:36 WOTS_TW_ES.ec
-rw-r--r-- 1   1001 root     5934 Aug 12 18:39 WOTS_TW_ES.eco


• Now let me look at the charged WOTS_TW_ES.ec in experiments/wots-badenc/base — the BadEncFlag module, Game4_WOTSTWES_BadEnc, the charged theorem around :6771, the nhchwcoll_hchwpre_msg lemma, and the :1476 nhchwcoll_hchwpre. Also the original admit at :1513. Plus the +C layer files: cdrafts-split/WOTS_C_Real.ec, WOTS_C_Interactive.ec, WOTS_C_Scheme.ec. And GprocQWired.ec:457 + GprocQWiredWotsCharged.ec.

  This is a lot. I'll read key regions myself in parallel and delegate some verification. Let me grep for the relevant identifiers first._assembly_unfold_wip.ec
_assembly_wip.ec
_branch2_trh_wip.ec
C10DeployedCapstone.ec
C10DeployedCapstone.eco
C10DeployedGeometry.ec
C10DeployedGeometry.eco
C10DeployedInstance.ec
C10DeployedInstance.eco
C10SpecControls.ec
C10SpecControls.eco
_compose_wip.ec
DarkSideC10.ec
DarkSideC10.eco
DarkSide.ec
DarkSide.eco
FORS_C10.ec
FORS_C10.eco
FORS_C10_Multi.ec
FORS_C10_Multi.eco
FORSC10_Wire.ec
FORS_C.ec
FORS_C_Multi.ec
FORS_C_Tree.ec
FORS_C_TreePort.ec
FORS_C_TreePort.eco
FxChain.ec
FxChain.eco
_gamehops_scratch.ec
_gamehops_wip.ec
GFailCharged.ec
GFailCharged.eco
good_clone_probe.ec
GprocChargedQWired.ec
GprocChargedQWired.eco
GprocFORSC10.ec
GprocFORSC10.eco
GprocQBound.ec
GprocQBound.eco
GprocQWired.ec
GprocQWired.eco
GprocQWiredWotsCharged.ec
GprocQWiredWotsCharged.eco
GprocT1Opre.ec
GprocT1Opre.eco
GprocT2Trh.ec
GprocT2Trh.eco
GprocT3Trco.ec
GprocT3Trco.eco
GprocVI.ec
GprocVI.eco
Grind.ec
Grind.eco
_gut.ec
IncEnc.ec
LeafWiring.ec
_member_audit_wip.ec
_okc_ghost_dev.ec
prf_hop_wip.ec
R6probe.ec
  6810 experiments/wots-badenc/base/WOTS_TW_ES.ec
   416 cdrafts-split/WOTS_C_Real.ec
  3262 cdrafts-split/WOTS_C_Interactive.ec
   236 cdrafts-split/WOTS_C_Scheme.ec
 10724 total
Wait — `cd` folder is inside `experiments/wots-badenc/cd/`? The `ls experiments/wots-badenc/cd/` output shows many files including GprocQWiredWotsCharged.ec, WOTS_C_*... but the user said "cd/GprocQWiredWotsCharged.ec". And the user referenced `cdrafts-split/WOTS_C_Real.ec:214`. Interesting — the base tree is `experiments/wots-badenc/base` and `experiments/wots-badenc/cd` presumably is a copy of cdrafts-split.

  Let me read the key regions:
  1. WOTS_TW_ES.ec :640-680 (P definition, encode), :1476-1530 (nhchwcoll lemmas), :3190-3320 (BadEncFlag + Game4), :6470-6810 (charged theorem).
  2. cdrafts-split/WOTS_C_Scheme.ec full (236 lines).
  3. cdrafts-split/WOTS_C_Real.ec (416 lines).
  4. WOTS_C_Interactive.ec around :1700-1800 (R_int_WOTSTW).
  5. experiments/wots-badenc/cd/GprocQWiredWotsCharged.ec.

  This is a large synthesis task. I'll read core files myself and spawn explore agents for peripheral verification (e.g., where the +C layer WOTS messages come from, whether R_int_WOTSTW destroys structure, the GprocQWired wiring).

  Let me read the main regions in parallel.Now the charged theorem at :6470-6810, and the +C layer files. Let me read the final theorem region, then WOTS_C_Scheme.ec, WOTS_C_Real.ec, and the R_int_WOTSTW region of WOTS_C_Interactive.ec, plus GprocQWiredWotsCharged.ec.Now the +C layer. Read WOTS_C_Scheme.ec (full, 236 lines), WOTS_C_Real.ec (416 lines), and WOTS_C_Interactive.ec around R_int_WOTSTW :1753. Also GprocQWiredWotsCharged.ec and the deployed adversary chain. Let me read these.Now the interactive layer: `WOTS_C_Interactive.ec:1753` — R_int_WOTSTW. Also the deployed adversary chain `R_int_WOTSTW(R_MEUFGCMAWOTSC_EUFNAGCMA_C(R_top_C(F)))`. Let me read the relevant regions of WOTS_C_Interactive.ec. And GprocQWiredWotsCharged.ec, GprocQWired.ec:457.

  Also RESULT.md in experiments/wots-badenc may summarize their framing — worth reading.

  Let me grep for R_int_WOTSTW in WOTS_C_Interactive.ec and read around it.Now I need to understand the deployed instantiation chain and the +C layer more. Key files:
  - cdrafts-split/WOTS_C_Interactive.ec around :1730-1830 (R_int_WOTSTW module), and the interactive_D1 statement around :2300-2340.
  - experiments/wots-badenc/cd/GprocQWiredWotsCharged.ec and GprocQWired.ec:457.
  - Where does R_top_C / R_MEUFGCMAWOTSC_EUFNAGCMA_C live? Probably in cdrafts-split. Grep for them.

  Also important for Question 2: the seed-withholding — Adv_MEUFGCMA_WOTSC's forge gets ps (line 143: `proc forge(ps : pseed)`). Wait, user said "M_EUF_GCMA_WOTSC_NPRF's adversary WITHHOLDS ps from choose (WOTS_C_Scheme.ec:141-143)". Yes: choose() gets no ps; forge(ps) gets it. That's the naCMA (non-adaptive) structure: adversary commits queries before seeing the seed.

  Key analytical question: is the BadEnc charge at the WOTS-TW layer bounded for the deployed adversary R_int_WOTSTW(...)? The deployed WOTS-TW adversary queries messages of the form ThC ps ad x c — but note: R_int_WOTSTW's O_wrap.query receives m from the +C adversary A, grinds the counter via OC, and queries the WOTS-TW oracle with the digest d = ThC ps ad m c. And in forge, A outputs (i, m', sigc') where sigc' = (sig', c'); R_int_WOTSTW queries the WOTS-TW game with m' encoded... Actually need to see the forge part of R_int_WOTSTW.

  Key insight for Q1: the BadEnc event at the WOTS-TW layer is encode_msgWOTS(m) = encode_msgWOTS(m') where m = ThC ps ad x c (queried digest) and m' = ... what does R_int_WOTSTW pass as the forgery message? Under the encode bridge, the +C verify recomputes encode_msgWOTS_C ps ad m' c' = encode_msgWOTS(ThC ps ad m' c'). So the BadEnc event pushed through R_int_WOTSTW becomes encode(ThC ps ad x c) = encode(ThC ps ad m' c') — a collision of encode ∘ ThC between a queried (x,c=grind) pair and the adversary's forged (m',c') pair, with the constraint predC on both sides... And the +C adversary chooses x (messages) before seeing ps (naCMA withholding), and chooses m', c' after seeing ps.

  So the natural named assumption: "encode ∘ ThC collision resistance under naCMA seed-withholding" — i.e., an adversary that commits to a list of messages before ps is sampled, then given ps and oracle access to ThC (collection oracle OC), must produce (m', c') with ThC ps ad m' c' colliding under encode with some queried ThC ps ad x (grindC ps ad x). This is essentially a flavor of target-collision-resistance of encode ∘ ThC, or "S-TCR(+C)-like". Actually it's very close to the S-TCR(+C) assumption already in the stack: STCRC_WC.S_TCR_C. The question is whether BadEnc at the deployed adversary reduces to S-TCR(+C) or needs a new assumption.

  Also note the crucial subtlety: in Game4_WOTSTWES_BadEnc, the BadEnc event is em = em' where the game ALSO requires P m /\ P m' (constant-sum gate) and !hchwcoll and m <> m'. Under the collision em = em', P m holds iff P m' automatically. So BadEnc event = encoding collision on the constant-sum surface.

  At the +C layer: the queried message is d = ThC ps ad x (grindC ps ad x) which satisfies predC by the grind; the forged digest is d' = ThC ps ad m' c' and +C verify checks predC d'. So both on the constant-sum surface. The BadEnc event pulled back to +C: encode(ThC ps ad x c) = encode(ThC ps ad m' c') with (m',c') ≠ (x,c)... but wait, is_fresh at WOTS-TW layer is d' <> d (digests), at +C layer is m' <> x.

  Hmm, actually there's a subtlety: under R_int_WOTSTW, the WOTS-TW forgery message is the digest d' = ThC ps ad m' c'. is_fresh at WOTS-TW layer requires d' <> d. If ThC collides (d' = d with m' <> x), then the +C forgery is valid (verify recomputes same encoding; sig replay works?) Actually if ThC collides then replaying gives a valid +C forgery with is_fresh m' <> x — that's exactly what S-TCR(+C) prevents. So S-TCR(+C) handles ThC collisions; BadEnc handles the residual encode-collisions where d' <> d but encode d' = encode d.

  So the charge can be bounded at the +C layer by a game: adversary (naCMA, committed queries before seed) finds d' <> d on the constant-sum surface with encode d' = encode d. Name it something like "T-COLL-RES" — wait, the repo already has that name! The comment at :1473 says "That gap IS the T-COLL-RES event (Def 11)". And there's experiments/tcollres-leg/. Let me check what T-COLL-RES is in this repo. There's experiments/tcollres-leg/ThCWidth.ec mentioned. Let me grep for "T_COLL_RES" or "TCOLLRES" in cdrafts-split.

  Now for the placement question (Q1): Should the charge be defined at the +C layer instead? Considerations:

  Arguments for moving to +C layer:
  - Bounded uniformly for all Adv_MEUFGCMA_WOTSC, not just the one deployed instantiation. The WOTS-TW theorem with charged summand is true but the summand is unbounded (probability 1 achievable) for some adversaries — meaning the "theorem" as stated provides no quantitative guarantee at its own layer. The charged WOTS-TW bound's RHS ≥ 1 for A_coll, so as a statement about arbitrary Adv_MEUFGCMA_WOTSTWESNPRF it's quantitatively vacuous: the bound is true but says nothing (any probability ≤ something ≥ 1).
  - At +C layer, the event becomes encode(ThC ...)= encode(ThC ...) which can be bounded by a named assumption on encode ∘ ThC, uniformly in A.

  But wait — can the charge even be *defined* at the +C layer cleanly? The BadEnc event at WOTS-TW layer, pulled back through R_int_WOTSTW, becomes the +C-level event "encode(ThC ps ad m c) = encode(ThC ps ad m' c')" with d' <> d (else S-TCR handles it). The +C game M_EUF_GCMA_WOTSC_NPRF would need its own flag: em <- encode_msgWOTS_C ps ad m counter etc. That's definable. The interactive reduction (interactive_D1) bounds the +C game by S-TCR + WOTS-TW game at R_int_WOTSTW(A). If instead one proves a bound directly at the +C level: Pr[M_EUF_GCMA_WOTSC] <= S-TCR + [WOTS-TW charged bound at R_int_WOTSTW(A)] and then bound the BadEnc summand at that specific adversary by a reduction to the new encode∘ThC assumption — that's the "keep at WOTS-TW, bound at deployment" route. Alternatively, restructure: split the +C game on the BadEncC event first, reduce the !BadEncC half to WOTS-TW... hmm, but under !BadEncC at the +C level, does the reduction go through without the charge? The issue: R_int_WOTSTW queries the WOTS-TW game with d = ThC ps ad m c, and the forgery at WOTS-TW level is d' = ThC ps ad m' c'. BadEnc at WOTS-TW level is encode d = encode d', which is exactly BadEncC at the +C level. So the event commutes with the reduction: BadEnc(R_int_WOTSTW(A)) in the WOTS-TW game corresponds exactly to BadEncC(A) in the +C game (modulo the coupling). So the two placements are actually *equivalent in content*: bounding the charge at the deployed adversary IS bounding a +C-level event. The difference is purely presentational/compositional:

  - Keep at WOTS-TW: the exported theorem MEUFGCMA_WOTSTWESNPRF_Charged is true-but-vacuous for general A; the +C composition then needs an extra lemma bounding Pr[BadEnc(R_int_WOTSTW(A))] ≤ Pr[new game(B(A))]. 
  - Move to +C: the WOTS-TW theorem would need to keep the disjunction... but wait — the WOTS-TW layer can't avoid the charge entirely: the disjunction arises in the WOTS-TW proof itself (Step_Game4). You cannot prove the WOTS-TW theorem without either the admit or the charge. So "moving the charge to +C" doesn't eliminate the WOTS-TW-level term; it means not exporting the charged WOTS-TW theorem as the interface, but instead proving the composed +C-level statement where the charge appears as a +C-level event bounded by the new assumption.

  Actually, a cleaner architecture: define the charge at +C as a game GameC_BadEnc(A) (with flag em = em' via encode_msgWOTS_C), and prove the composed theorem: Pr[M_EUF_GCMA_WOTSC(A)] ≤ S-TCR + (w-2)UD + TCR + PRE + Pr[GameC_BadEnc(A)]. Then the WOTS-TW charged theorem stays as an internal lemma. Then name the assumption: "encode ∘ ThC target-collision resistance on the constant-sum surface" — call it e.g. SM-DT-TCC... The repo calls the gap T-COLL-RES (Def 11). Let me check what Def 11 / T-COLL-RES is in the repo — grep.

  Also need to check: does the S-TCR(+C) assumption already imply the BadEnc bound? S-TCR(+C) is about finding a second preimage/collision under ThC with predC gate... If encode had collisions on the constant-sum surface that are easy to find without breaking ThC collision resistance, then no. E.g., encode is many-to-one (2^127-to-1 at C10 geometry per the header), so encode ∘ ThC has MANY more collisions than ThC. Collision-resistance of ThC does not bound collisions of encode ∘ ThC. So a genuinely new assumption is needed: it's a form of "collision resistance of encode on the constant-sum surface, for digests produced by ThC under committed targets". Hmm — but is it actually a *plausible* assumption? Finding two digests d ≠ d' with digitsum encode d = digitsum encode d' = target and encode d = encode d' means d ≠ d' with encode d = encode d' — i.e., a collision of encode on the constant-sum surface. For the deployed encoder (base_w with the extra structure?), collisions of encode on the constant-sum surface... Actually wait: what IS encode_msgWOTS at C10? If it's base_w encoding of the n-byte digest plus checksum... in the fork, encode_msgWOTS : msgWOTS -> emsgWOTS abstract. The header says the encoder is 2^127-to-one at deployed widths. So collisions are abundant as *values*, but finding d, d' in ThC's image requires finding two ThC outputs that land in the same encode-fiber. Given ps is hidden until after query commitment, and the adversary gets OC (ThC collection oracle) access... The adversary can compute ThC itself via OC queries once it has ps (forge gets ps). So the adversary can search for collisions of encode ∘ ThC ps ad · by hashing many (x, c) pairs. Cost ~ sqrt(#fibers) birthday... The encode fibers on the constant-sum surface: number of constant-sum codewords vs digest space 2^256. The number of distinct codewords of length len with digit sum target_sum: for WOTS w=16, len=67, target 205... that's a large but much smaller than 2^256 set. A collision of encode on the surface is a pair of digests mapping to same codeword — finding one via ThC queries is a birthday search on codewords: ~sqrt(#codewords on surface). For len=67, w=16, sum=205: this is C(205+66,66)-ish restricted... it's astronomically large (~2^250?), so birthday is ~2^125. So as a concrete assumption it's a standard "encode-collision resistance of ThC outputs" — plausible but NEW, not implied by SM-DT-TCR-C.

  Hmm wait — but actually there's an important check: is the charge even *needed* at the deployed geometry if the digest is 256 bits and emsgWOTS is len*log2(w) = 67*4 = 268 bits? encode_msgWOTS : msgWOTS (256-bit) -> emsgWOTS (67 digits of 4 bits = 268 bits) — injective is impossible (2^256 -> 2^268 is fine, injective possible!). Wait 268 > 256, so injectivity is *possible* size-wise. But the header said "at deployed widths the encoder is 2^127-to-one". Hmm, that suggests the deployed encode maps 256 bits to fewer effective bits? Or n=... Let me not guess; check C10DeployedGeometry.ec. Actually in the countermodel header: "the theorem quantifies over ALL msgWOTS, and at deployed widths the encoder is 2^127-to-one". So msgWOTS is wider than the codeword space. E.g., C10: msgWOTS might be 2n bytes (64 bytes?) mapped to len digits... If msgWOTS is 512 bits and codeword 268 bits, then ~2^244-to-one. Whatever — verified later if needed.

  Actually wait, this matters for the truth of the claim "MM45's admit is false at deployed C10 geometry". MM45's admit: m <> m' => encode m <> encode m' (injectivity). If msgWOTS is wider than emsgWOTS capacity, injectivity is impossible by pigeonhole — hence false. Let me verify: check the sizes. In the fork WOTS_TW_ES.ec, check msgWOTS and emsgWOTS. There's experiments/tcollres-leg/ThCWidth.ec "predC_sum_inhabited (via Proj129), at C10 geometry". "Proj129" suggests a projection from 256-bit digest to 129? Hmm "2^127-to-one" suggests msgWOTS 256 bits and codeword 129 bits? If len*log2 w = 129? Not integer... Anyway.

  Let me now read R_int_WOTSTW module (:1730-1830), interactive_D1 statement (:2300-2340), GprocQWiredWotsCharged.ec, and grep for T-COLL-RES definition. Also the deployed capstone instantiation. Let me also check where R_top_C and R_MEUFGCMAWOTSC_EUFNAGCMA_C are defined.

  I'll parallelize: read R_int_WOTSTW region, read GprocQWiredWotsCharged.ec (in experiments/wots-badenc/cd/), grep for R_MEUFGCMAWOTSC and T-COLL-RES.

• Now let me read the `R_int_WOTSTW` reduction module, the charged wire-up file, and find the existing T-COLL-RES definition.  251 experiments/wots-badenc/cd/GprocQWiredWotsCharged.ec
  531 experiments/wots-badenc/cd/GprocQWired.ec
  782 total
CtlA.ec
CtlA.out
CtlB.ec
CtlB.out
CtlC.ec
CtlC.out
CtlD.ec
CtlD.out


• Now read GprocQWiredWotsCharged.ec (251 lines) and GprocQWired.ec:457 region. Also check where the deployed instantiation R_top_C / R_MEUFGCMAWOTSC_EUFNAGCMA_C lives. Grep in cdrafts-split.Now I have most of the picture. Let me check a couple more load-bearing things:

  1. The seed-withholding claim: `Adv_MEUFGCMA_WOTSC.choose` gets no ps; `forge(ps)` does. Verified at WOTS_C_Scheme.ec:141-144. ✓. But also check `R_MEUFGCMAWOTSC_EUFNAGCMA_C`'s choose — does the underlying hypertree adversary A_ht see ps during choose? XMSSMT_C_Reduction.ec:461. The EUF-NAGCMA game is "non-adaptive" — queries committed before key. Let me check that reduction's choose quickly.

  2. The key question for Q2: at the deployed adversary, is the BadEnc event reducible to a clean assumption? Let me think through the structure:

  Game4_WOTSTWES_BadEnc(R_int_WOTSTW(B)).main:
  - ps sampled, oracles init.
  - R_int_WOTSTW(B).choose(): B.choose() runs with O_wrap; each query (wad, x) → grind c via OC, d = ThC ps ad x c, O.query(wad, d) stores (ad, d, pk, sig) in qs. Note B = R_MEUFGCMAWOTSC_EUFNAGCMA_C(R_top_C(F)) — its choose phase: does B see ps? The +C game gives ps only at forge. But R_int_WOTSTW wraps: in the WOTS-TW game, R_int_WOTSTW.choose is called without ps, and forge gets ps. Inside, AA = B(O_wrap, OC) — B.choose gets no ps, B.forge gets ps. So the naCMA withholding is preserved by the reduction. ✓

  - forge: B.forge(ps) outputs (i, m', (sig', c')); reduction returns (i, ThC ps ad_i m' c', sig').

  BadEnc event: encode(d_i) = encode(ThC ps ad_i m' c') where d_i = ThC ps ad_i x_i c_i, plus res: is_valid (verify of replayed sig' under pk_i), is_fresh: ThC ps ad_i m' c' ≠ d_i, P d_i (holds: grind, unless grind_fails — hmm, interesting: P d_i holds iff grind succeeded; if grind failed, P d_i likely false; the game requires P m so grind-fail instances are excluded from res ∧ badenc), P m' i.e. predC(ThC ps ad_i m' c'), !hchwcoll (auto under encode equality), etc.

  So BadEnc at the deployed adversary is exactly: after committing queries x_1..x_q before seeing ps, and given ps plus OC access, B outputs (i, m', c') with d' = ThC ps ad_i m' c' ≠ d_i = ThC ps ad_i x_i c_i, predC d', and encode d' = encode d_i.

  That's a named assumption: something like "encode-target-collision resistance of ThC on the +C surface" — call it TCC ("target codeword collision") or, following repo naming (SM-DT-*), maybe "SM-DT-ECC"? The repo already names this gap "T-COLL-RES" (Def 11 of the IncEnc paper — but that's a different encoder, TSW/IncEnc, FORS-level). Actually careful: IncEnc.ec's T-COLL-RES is for the IncEnc encoder in a different context (FORS+C?). The WOTS-level one here is distinct.

  The reduction: from a BadEnc-winner B to an adversary B' against the new game. The new game: 
  - Adversary commits (ad, x) pairs (non-adaptively, before seed).
  - ps sampled; adversary gets ps and ThC collection oracle.
  - Wins if it outputs (i, m', c') with predC(ThC ps ad_i m' c'), encode(ThC ps ad_i m' c') = encode(ThC ps ad_i x_i (grind ps ad_i x_i)), and ThC inequality.

  Hmm, but there's a wrinkle: the queried-side digest uses c_i = grindC ps ad_i x_i, which depends on ps. So the target codeword isn't fixed at commitment time; the game is: targets are the grinded digests. That's fine — the game samples ps, computes the targets, then lets the adversary find a codeword collision with any of them.

  Alternatively, a simpler-to-state assumption: codeword-collision resistance of encode ∘ ThC: given ps and OC, find (ad, m, c) ≠ (ad, m', c') [same ad?] with predC both and encode(ThC ps ad m c) = encode(ThC ps ad m' c'). But the BadEnc event has same ad (ad_i) — the forge binds to instance i. Same ad. And one side is the grinded one.

  Now Q1 — placement. Key structural observation: the BadEnc event commutes with R_int_WOTSTW: BadEnc at WOTS-TW layer for R_int_WOTSTW(B) ⟺ (essentially) the +C-level event encode_msgWOTS_C ps ad x c = encode_msgWOTS_C ps ad m' c' with digest inequality. Because of encb, encode_msgWOTS_C p a x cc = encode_msgWOTS(ThC p a x cc), the events coincide. So "defining the charge at the +C layer over ThC outputs" is not in tension with the WOTS-TW-level term — it's the same event, seen through the reduction. 

  The real question is composition architecture:

  Option A (current): charged WOTS-TW theorem exported; at the deployed instance the summand Pr[Game4_WOTSTWES_BadEnc(R_int(...))] sits raw in the headline bound, to be bounded by a subsequent lemma Pr[BadEnc(R_int_WOTSTW(B))] ≤ Pr[NewGame(B')] + (S-TCR?).

  Option B: move charge to +C layer: prove a "charged interactive_D1": Pr[M_EUF_GCMA_WOTSC(B)] ≤ S-TCR + Pr[GameC_BadEnc(B)] + [WOTS-TW terms at R_int_WOTSTW(B) but WITHOUT the BadEnc summand?]... 

  Wait — can the WOTS-TW summand drop its BadEnc term when composed? The chain: M_EUF_GCMA_WOTSC(B) ≤ S-TCR + M_EUF_GCMA_WOTSTWESNPRF(R_int(B)) ≤ S-TCR + UD + TCR + PRE + BadEnc(R_int(B)). The BadEnc term at R_int(B) is what needs bounding. If instead we hop the +C game first on the +C BadEncC event: Pr[M_WOTSC(B)] = Pr[win ∧ BadEncC] + Pr[win ∧ ¬BadEncC]. Under ¬BadEncC, does the reduction to WOTS-TW go through without the charge? The charge arises in Step_Game4 inside the WOTS-TW theorem proof — i.e., inside the black box being applied to R_int(B). The black box charges BadEnc(R_int(B)) regardless. But BadEnc(R_int(B)) and the +C-level BadEncC(B) event are the same event (via the coupling equiv Alt/Game4 at the R_int(B) instance). So bounding BadEnc(R_int(B)) IS a +C-level statement automatically. There is no way to "move" the event; there's only a choice of where to name and bound it.

  Hmm — but one more subtlety: is BadEnc(R_int(B)) exactly the +C event, or could R_int(B) trigger BadEnc in ways that don't correspond to a +C-level collision? The WOTS-TW BadEnc event: encode(m_qs) = encode(m') where m_qs is the *stored query message* at index i and m' the forgery message. Under R_int, stored query message = d_i = ThC ps ad_i x_i c_i (via OC), forgery message = ThC ps ad_i m' c'. So yes — any BadEnc win of R_int(B) gives encode(ThC ps ad_i x_i c_i) = encode(ThC ps ad_i m' c'), d' ≠ d (is_fresh), predC d' (P m'), and predC d_i... wait P m requires P d_i — i.e., the grinded digest is on the surface, which holds iff grind succeeded. Also the res conjuncts include is_valid etc. For bounding purposes, BadEnc(R_int(B)) ≤ Pr[B produces the +C collision] (drop conjuncts). So the bound reduces to the new assumption cleanly, via a *wrapper reduction* B ↦ adversary against the new game. The wrapper is essentially the identity — the new game can be defined so that Game4_WOTSTWES_BadEnc(R_int_WOTSTW(B))'s badenc∧res event implies the new game's win. Need to double check the is_valid/is_fresh interplay: is_fresh is d' ≠ d_i, good; the new game shouldn't require validity of sig' (drop it — fewer conjuncts, larger event, still a valid upper bound). Also disj_wgpidxs etc. can be dropped.

  One concern: the new game's adversary B' must be constructible from B: B' runs B, simulates O_wrap via its own ThC oracle and the WOTS-TW signing... wait — the new game's oracle needs to provide signatures? No: B' needs to answer B's +C signing queries, which require WOTS-TW signatures of d_i. In Game4_BadEnc those come from O_Game34_AltX. If the new assumption game doesn't provide signing, B' can't simulate... but the new assumption game can just give B' a signing oracle (it's an assumption — we define it as needed). Cleaner: define the assumption game to mirror the +C game: committed queries, keyed ThC, sign-oracle, win = codeword collision with digest freshness + predC. But then the assumption includes the signing oracle machinery, which is ugly.

  Alternative cleaner formulation: note the WOTS secret keys are irrelevant to the BadEnc event except through validity — dropping is_valid removes the need for sig'. But B' still must *answer signing queries* to run B. Two options: (i) the assumption game provides a sign oracle (makes the assumption stronger but standard-ish: "even with a signing oracle, you can't find codeword collisions"); (ii) observe that B's queries can be answered with random junk — no, B might check.

  Honestly (i) is fine: the event only depends on the transcript B sees; the game that gives B its exact Game4 view and checks the collision. So the assumption game = Game4_WOTSTWES_BadEnc minus the crypto-checking conjuncts — i.e., precisely "Pr[Game4_BadEnc(R_int(B)) : badenc ∧ fresh digests ∧ predC both]" — hmm, but that's still at the WOTS-TW layer with R_int inside. To state the assumption at the +C layer, define game over Adv_MEUFGCMA_WOTSC directly:

  Game TCC_C (A : Adv_MEUFGCMA_WOTSC): 
  - ps <$ dpseed; O.init(ps); OC.init(ps); A.choose(); (i, m', (sig', c')) <@ A.forge(ps); (ad, x, pk, (sig, c)) <@ O.get(i);
  - return 0≤i<nrqs ∧ predC(ThC ps ad m' c') ∧ encode d = encode d' ∧ d ≠ d' [∧ predC d — implied by grind success... careful with grind_fails].

  Then lemma: Pr[Game4_BadEnc(R_int_WOTSTW(A)) : res ∧ badenc] ≤ Pr[TCC_C(A) : res]. Proof by byequiv/success-implication — the coupling already exists (choose_tw/forge_tw in WOTS_C_Interactive). This is "a game + a reduction" in the house shape. And the game is uniform in A — no need to mention R_int.

  So the answer to Q1: the placement is not *wrong* — the WOTS-TW-layer charge cannot be eliminated (the disjunction is born inside MM45's Step_Game4), and since the event commutes with R_int_WOTSTW, keeping the exported charged theorem and adding one +C-level bridging lemma is equivalent in content to "defining it at +C". But there's a real presentational hazard: the exported theorem MEUFGCMA_WOTSTWESNPRF_Charged, quantified over all Adv_MEUFGCMA_WOTSTWESNPRF, has RHS ≥ 1 for A_coll — so as a standalone theorem it's quantitatively vacuous. It's fine as an internal lemma, but exporting it as "the WOTS-TW bound" invites misuse. Verdict: keep the charge where it is born (it has no choice), but the *named assumption* and the bounding must be at +C; do NOT try to restate the event at +C and re-prove — instead prove the one bridging lemma. Actually the user's Q1 asks: "Should the charge instead be defined at the +C layer, over ThC outputs, so that it is bounded uniformly rather than only at one instantiation? What breaks if I restate it there — specifically, does WOTS_C_Interactive.ec:1753's R_int_WOTSTW let a +C-level event be pushed down, or does the reduction destroy the structure needed?"

  Answer: R_int_WOTSTW preserves exactly the structure needed — it maps +C messages x to WOTS-TW messages ThC ps ad x c injectively enough that the BadEnc event at the pushed-down side is precisely encode∘ThC collision. The reduction does not destroy the structure: forge returns (i, ThC ps ad_i m' c', sig') (:1813), query stores d = ThC pp ad m c (:1788-1793). So a +C-level event pushes down cleanly. Nothing breaks. But there's a nuance: defining at +C doesn't get you uniformity for free — the +C game M_EUF_GCMA_WOTSC_NPRF's adversary class is exactly what the deployed instance instantiates; the +C-level charge CAN be bounded uniformly for all Adv_MEUFGCMA_WOTSC by the TCC_C assumption. So actually the +C-level statement IS uniform over its adversary class. Good.

  What breaks / costs if restated at +C:
  - You must prove a new coupling lemma BadEnc(R_int_WOTSTW(A)) ≤ TCC_C(A) — the existing coupling lemmas (choose_tw, forge_tw, interactive_hop2) give the transcript correspondence; the bridge is a moderate pRHL proof but the pieces exist.
  - The grind_fails edge: P d_i on the query side holds only if grind succeeded at instance i. TCC_C as I stated it should either require predC d (then the bridge must show Game4's res implies it — res includes P m so yes, P d_i is a conjunct of res! Good: Game4_BadEnc's res includes P m ∧ P m', so res ∧ badenc implies predC d_i ∧ predC d'_i. So TCC_C can require predC on both digests and the implication holds.) Nice — so TCC_C win := 0≤i<nrqs ∧ d ≠ d' ∧ encode d = encode d' ∧ predC d ∧ predC d'. All five are implied by res ∧ badenc (given the coupling equates d with stored m and d' with forge message; encode equality is badenc; freshness: is_fresh at WOTS-TW level is m' ≠ m i.e. d' ≠ d — exactly). The other res conjuncts (validity, disj etc.) get dropped. So the bridge is a clean "event inclusion" proof. 

  - One more check: does Game4_BadEnc use O_Game34_WOTSTWES_AltX whose stored message is the queried d? Yes: query stores (ad, m, pk, sig) with m = the argument = d. And R_int's O_wrap.query calls O.query(wad, d). ✓. And forge returns ThC ps (wad_i) m' c'. But careful: TCC_C's get(i) returns ad from O_MEUFGCMA_WOTSC_Default.qs; under the coupling, O_wrap.qs vs wads... R_int_O_wrap stores wads separately and forge uses nth wads i. The address used in forge is WAddress.val(nth wads i) — the same ad as query i. ✓.

  - What about OC query disjointness conjuncts (disj_wgpidxs)? Dropped in TCC_C — fine for an upper bound.

  So Q1 verdict: placement at WOTS-TW is forced (event is born there) and NOT wrong, provided the plan is to bound it via a +C-level assumption through the R_int_WOTSTW bridge — which is uniform in the +C adversary A, NOT just the one deployed instantiation. The user's claim "it can only be bounded for the specific deployed adversary" is too pessimistic: the bridge lemma is uniform in A <: Adv_MEUFGCMA_WOTSC. That's an important correction to their framing (Q4 material): they said "It can only be bounded for the specific deployed adversary R_int_WOTSTW(R_MEUFGCMAWOTSC_EUFNAGCMA_C(R_top_C(F)))". Actually the bridge is at R_int_WOTSTW(A) for generic A — the uniformity comes free because R_int_WOTSTW is where the ThC structure is introduced. So "defined at the +C layer" and "bounded at R_int_WOTSTW(A) for generic A" are the same thing. The charge placement only looks instantiation-specific if you forget R_int_WOTSTW is a functor.

  However, one thing WOULD break if they tried to literally *remove* the summand from the exported WOTS-TW theorem and only carry it at +C: the WOTS-TW theorem is false without it (countermodel), so any +C-level composition that quotes a charge-free WOTS-TW bound is unsound. The charge must stay in the WOTS-TW theorem statement; the +C layer adds the bridge + assumption.

  Q2 — the named assumption. Name: following house style (SM-DT-TCR-C etc.), I'd name it something like "TCC_C" / "SM-DT-ECC-C"... The repo's own literature pointer: IncEnc.ec Def 11 "T-COLL-RES". The gap comment at :1473 calls it "the T-COLL-RES event (Def 11)". But IncEnc's T_COLL_RES module is for a different encoder (TSW/IncEnc). For WOTS+C the honest name: "codeword target-collision resistance of encode∘ThC on the predC surface" — game as above. Is seed-withholding the lever? Partially: withholding ps from choose means the adversary commits x_1..x_q before ps — this prevents trivially choosing x_i to be a pre-computed codeword collision partner of... hmm, actually what does withholding buy? Without withholding, the adversary could pick queries depending on ps, e.g. grind both sides. With withholding + the freshness/predC structure, the assumption becomes: given key ps and hash oracle, find a second digest in the same encode-fiber as a target digest that the game computes from your committed message. This is a target-collision flavor. Is withholding *enough*? Not sufficient by itself — the assumption is still a new computational assumption about encode∘ThC; withholding just makes it a *clean* (non-interactive-in-a-bad-way) assumption, analogous to naCMA. The deeper question: is the assumption *true*/plausible at C10? encode's fibers on the constant-sum surface: finding d ≠ d' with encode d = encode d' means finding a collision of the encoder — but the adversary doesn't choose d directly; it must come from ThC outputs. Since ThC is a random-oracle-like collection member, finding encode-collisions among its outputs is a birthday search over codeword space of size ~#surface codewords. For C10 geometry... The user says the encoder is 2^127-to-one, so digest space 2^256, fibers 2^127 → codeword space ~2^129; surface codewords (sum=205)... the number of len-digit words with digit sum 205... anyway birthday ~2^64-ish. That's the quantitative content the assumption would carry. Not my job to compute exactly, but worth noting the assumption is a birthday-bound assumption, not an exotic one.

  Wait, actually, I should double check "2^127-to-one" — let me verify msgWOTS width vs emsgWOTS width at C10 geometry in C10DeployedGeometry.ec. If msgWOTS = 2^256 and emsgWOTS = 2^129, average fiber is 2^127. Let me check quickly: c10_len, w. If len=35, w=16: 35*4=140 bits. Hmm. Let me grep c10_len.

  Also verify the claim "encode_msgWOTS is 2^127-to-one" is from their docs — actually the countermodel doesn't need it (hypothesis-based). But for my answer the fiber size matters for assessing the assumption's plausibility. Let me check C10DeployedGeometry.

  Q3 — salvageability. Blunt verdict: the charged architecture is sound progress IF the next step (TCC_C bridge + named assumption) is taken, because the alternative (admit) was a false statement. A bound that is 1 for some adversaries but meaningful at the deployed composition is NOT a bookkeeping trick *provided* the theorem is never quoted as a standalone bound for arbitrary A — it's a *conditional* bound, and conditionality must travel with the statement. This is exactly how reductions work: SM-DT-TCR-C etc. are also just "debt instruments" — named probabilities. The difference: those are plausibly-small for all adversaries by assumption; BadEnc is plausibly-small only for ThC-structured adversaries. So the exported charged theorem is fine as an intermediate, dangerous as a headline. The current GprocQWiredWotsCharged exposes it raw in the headline — that's honest (it's displayed, named, and the countermodel documents its unboundedness) but the deliverable is incomplete until the TCC_C term replaces it.

  Strongest objection (Q4 / deliverable 4): Let me think about what could be fatal.

  Candidate objection 1: The TCC_C assumption may not be *needed* — maybe BadEnc at R_int(A) is already bounded by S-TCR(+C)? No — encode collisions don't require ThC collisions; S-TCR is about d = d'. The fibers are huge (2^127), so encode collisions are vastly easier than ThC collisions. New assumption genuinely needed. ✓ their framing acknowledges this.

  Candidate objection 2: The countermodel shows worse than advertised? A_coll shows Pr = 1 for the game with res ∧ badenc — so at generic A the charged theorem is p ≤ ≥1, vacuous. The exported theorem MEUFGCMA_WOTSTWESNPRF_Charged is true but content-free as a universal statement. The hazard: anything downstream that quotes it for a non-ThC adversary gets nothing. Currently only quoted at R_int(...). OK.

  Candidate objection 3 (strongest, I think): the P m ∧ P m' gating interplay. Note Game4_BadEnc's res requires P m (the queried digest on the surface). For generic A there's no reason queries are on the surface — but A_coll queries cm with P cm by hypothesis, fine. For the deployed R_int(A): P d_i requires the grind to have succeeded — if grind_fails, d_i is off-surface and res is false regardless; so BadEnc wins are confined to grind-success instances. Fine for the bridge.

  Candidate objection 4: the freshness mismatch. At the +C level, the scheme's is_fresh is m' ≠ x (dgstblock messages). At the WOTS-TW level, is_fresh is d' ≠ d. In TCC_C I keyed freshness to d ≠ d'. But consider: B could win TCC_C with m' = x_i but c' ≠ c_i giving d' ≠ d with encode d' = encode d. Is that a "real" event? At the +C game that's not a forgery (m' = x not fresh), but TCC_C is an assumption game, not a forgery game — it just needs to upper-bound BadEnc(R_int(A)); including extra wins only strengthens the assumption... wait, it makes the event larger, so the bound Pr[BadEnc] ≤ Pr[TCC_C] holds trivially, but the *assumption* "TCC_C is hard" becomes stronger/less plausible? Including m'=x, c'≠c_i cases: d' = ThC ps ad x c' — finding c' with predC and same codeword as d. That's still a hard birthday search presumably. Fine.

  Candidate objection 5 — the actually sharp one: **the countermodel also kills the +C-level plan if predC were the only gate**... no. Hmm.

  Let me think about the sharpest objection to the whole architecture. The user's Q3: "is a bound that holds only at one instantiation, and is 1 elsewhere, a meaningful security statement or a bookkeeping trick?"

  Sharpest objection: the exported charged WOTS-TW theorem is not a theorem *about WOTS-TW security* at all — it's a *decomposition* (Pr ≤ stuff + residual). That's fine and normal. But there's a subtle soundness trap in the OTHER direction that I should check: is the charged bound *complete* — i.e., did they verify that Game4_WOTSTWES_Alt's `res` in the !badenc branch is genuinely equivalent to MM45's original proof path? The Step proof at :6690-6696: under !badenc (hbadenc : !BadEncFlag.badenc{1}), nhchwcoll_hchwpre_msg gives disjunction; left disjunct (encode equal) contradicts !badenc — wait, badenc is em = em' computed in the game from the same m, m'. The left disjunct is encode m = encode m' which equals BadEncFlag.badenc{1}; with hbadenc they smt() it away. ✓ (verified at :6693-6696).

  Another sharp objection: the mu_split + strengthened equiv — verified structurally; compiles admit-free per their claim (I can't re-run easycrypt here — can I? There's a docker container "container-only" for the gate; running easycrypt compile locally might be possible but they said read-only, don't modify files. Compiling creates .eco files — that modifies. So no compiling. Fine.)

  Another candidate for strongest objection: **the TCC_C assumption, once named, is doing ALL the work, and it's essentially "the encoder is collision-resistant on the surface for ThC outputs" — which is precisely the kind of assumption that the +C construction was supposed to avoid**. Recall the design rationale of SPHINCS+C: grinding to constant-sum makes codewords an antichain, so no strict digit domination — the security rests on the antichain. But the antichain argument needs encode-INJECTIVITY on the surface to conclude m = m'... no wait — the antichain gives: same sum + domination ⇒ equal codewords. The forgery logic: sig' reveals chain values; if codewords differ, somewhere digit strictly less ⇒ preimage (SM-DT-PRE). If codewords equal but digests differ — BadEnc — then sig' = sig is a valid forgery WITHOUT breaking anything: it's a *signature replay across digests*. So the entire security of the +C scheme against replay rests on "distinct ThC digests have distinct codewords on the surface" — i.e., encode is injective ON THE IMAGE OF ThC restricted to the surface. That's an assumption about the composition encode∘ThC. It's plausible (birthday-bound) but it is a NEW, non-standard, non-MM45 assumption that the deployed C10 parameter set must be checked against CONCRETELY: if the surface codeword count is ~2^k, security is ~k/2 bits birthday. If C10's parameters make the surface small (e.g., if target_sum is attained by few codewords), the assumption could be quantitatively WEAK. THAT is the strongest objection: the architecture is salvageable only if the named assumption comes with a concrete parameter audit (surface codeword count at c10 geometry), and the repo has the tools (ThCWidth.ec, Proj129) but hasn't connected them. If the surface count is small, the charge is not just unbounded at the WOTS-TW layer — it's LARGE at the deployed layer too, and no assumption-naming will save it.

  Hmm wait, is that right? Let me sanity check with C10 numbers. Let me look at C10DeployedGeometry.ec for c10_len, w, target. The comment at WOTS_C_Real:234 said firmware target 205... `sphincs-c10/src/wots.rs:160` `if sum != TARGET_SUM`. With w=16 (digits 0..15), len=? If len=67 (n=32... WOTS+ len = len1 + len2; for n=16... hmm C10 uses n=16?). Actually let me just check the geometry file. If len=67, max sum = 67*15=1005, target 205. Number of codewords with sum 205: coefficient of x^205 in (1+x+...+x^15)^67 ≈ approx total 16^67 / spread... mean sum = 67*7.5 = 502. Sum 205 is in the tail — FEWER codewords there. Tail coefficient could be smallish. If #surface-codewords ≈ 2^b, birthday bound b/2 bits. Also the encoder maps 256-bit digest to 67 digits — 268 bits capacity, so encode can be injective... wait then why "2^127-to-one"?? If msgWOTS is 256 bits and codeword 268 bits, injectivity is possible and MM45's admit wouldn't be refuted by pigeonhole... unless the deployed encoder isn't base_w of the raw digest but of a truncated projection ("Proj129"!). The ThCWidth.ec "via Proj129" — suggests the deployed encode projects the digest to 129 bits?? If the deployed codeword space is 2^129 while digest is 2^256, average fiber 2^127 — matches "2^127-to-one". So len*log2(w) = 129? Not integer... maybe len digits with w=16: 32 digits = 128 bits + checksum 1 digit? e.g. len1=32 (128 bits) + len2=... Actually WOTS with n=16: len1 = 32, w=16 → len2 = floor(log(32*15)/log16)+1 = 3, len=35, total 140 bits. Hmm "129"... Proj129 might be their name. Let me check C10DeployedGeometry.ec to get it right.

  Actually — with the checksum REMOVED in +C (constant-sum replaces checksum), the codeword = base_w of the digest (len1 digits) and encode is... if encode_msgWOTS at C10 is base_w of a 128-bit... and digest 256 bits... The details matter for the objection. Let me read C10DeployedGeometry.ec quickly.

  Also I should verify the user's claim that the gate re-ran etc. — not needed; that's environmental.

  Also check the deployed-geometry claim about the encoder and "residual Q2b". Let me grep C10DeployedGeometry.ec.

  Let me also read interactive_D1's statement fully (:2300-2340) to confirm the N2 premise shape and whether the WOTS-TW side theorem applied is MEUFGCMA_WOTSTWESNPRF (charged version would add the summand at R_int_WOTSTW(A) — uniform in A). And XMSSMT_C_Reduction.ec:881-905 for the applied version.

  Given time, spawn subagents? I've gathered most myself. Remaining verifications:
  1. C10DeployedGeometry numbers (encoder width, 2^127-to-one claim).
  2. interactive_D1 statement + how XmssmtCC_All applies the charged theorem (XmssmtCC_All.ec:8811, :8902).
  3. R_MEUFGCMAWOTSC_EUFNAGCMA_C choose: does the inner A_ht see ps during choose? (Checks the withholding lever survives the composition.)
  4. Whether T_COLL_RES in IncEnc.ec is same/different (naming collision).

  I can do 1-4 quickly with targeted greps/reads. Let me do them in parallel.

• I have the core picture. Let me verify a few remaining load-bearing facts: the deployed geometry numbers, the `R_MEUFGCMAWOTSC_EUFNAGCMA_C` choose-phase (does the withholding survive composition?), and how `XmssmtCC_All` applies the charged theorem.

• Key numbers verified: C10 geometry n=16 (dgstblock = 128 bits → msgWOTS = 2^128... wait, c10_message_space: 2^(8*c10_n) = 2^128 — so msgWOTS is 128 bits, not 256), codeword space w^len = 8^43 = 2^129. So codeword space (2^129) is LARGER than message space (2^128). So the encoder could be injective in principle! But "the encoder is 2^127-to-one"?? Hmm — the user's countermodel header says "at deployed widths the encoder is 2^127-to-one". With domain 2^128 and codomain 2^129, a generic map is not 2^127-to-one... Wait — but the deployed encoder is base_w of the digest: 128-bit digest → base-8 digits = ceil(128/3) = 43 digits (since 8^43 = 2^129 > 2^128). Base_w of a 128-bit integer gives 43 digits — and that IS injective! Hmm, but then MM45's admit would be true?

  Wait, but the "WIDTH OBSTRUCTION" says the codeword space width 129 is not a multiple of 8 — so emsgWOTS can't be expressed as 8k bits... that's about pinning the encoder definition in the model.

  Hmm, so why does the user claim the admit is FALSE at deployed geometry? If encode is base_w of the raw 128-bit digest, it's injective. Unless the deployed encoder is NOT that — the +C scheme replaces the checksum with grinding, so encode_msgWOTS might be just base_w... Let me check what "residual Q2b" says and the tcollres-leg findings. The header in the countermodel says: "at deployed widths the encoder is 2^127-to-one. ... residual Q2b supplies them" (colliding pairs). 2^127-to-one would mean fibers of size 2^127, i.e., effective image of size 2 — that seems wrong. Or maybe msgWOTS in the model is 8*n_m with n_m... In the fork at :193: `op P x <- size x = 8 * n_m` — that's for some other type. msgWOTS might be `dgst` (arbitrary length)? Let me check the type of msgWOTS in the fork. In WOTS_TW_ES.ec: `type msgWOTS` — clone parameter. In the deployed instantiation, msgWOTS <- dgstblock (128 bits)? But ThC returns msgWOTS = join_dgst of two dgstblocks = 256 bits! Look: WOTS_C_Real clone: `type out_t <- msgWOTS` and ThC : ... -> msgWOTS built from join_dgst (two dgstblocks). And in R_int_WOTSTW, `var d, y : msgWOTS; (* ThC outputs *)` and `d0, d1 : dgstblock; (* the two projections *)` — "the wide digest". So msgWOTS is the WIDE digest = 2 dgstblocks = 256 bits at C10! And the codeword space is 2^129. So the encoder domain is 2^256, codomain 2^129 → average fiber 2^127. THAT's "2^127-to-one". ✓ consistent.

  So at C10: dgstblock = 128 bits (n=16), msgWOTS = 256 bits (wide digest, two dgstblocks joined), emsgWOTS = 43 base-8 digits = 2^129 codewords. The encoder maps 2^256 → 2^129; collisions abound (fibers ~2^127 on average). MM45's injectivity admit is false. Verified consistent with source: c10_n=16, c10_w=8, c10_len=43, codeword 2^129, and msgWOTS is the joined wide digest (2·8n = 256 bits) — I verified msgWOTS is wide via R_int_WOTSTW comments (:1767-1768) and ThC : ... -> msgWOTS = join_dgst of two thfc outputs (WOTS_C_Real:214-216).

  Hmm wait — but is msgWOTS 256 bits in the MODEL or is that Route-D? The model's msgWOTS is abstract; under the deployed instantiation dgstblock=128 bits, and ThC outputs msgWOTS as join of two. But is msgWOTS instantiated to a 256-bit type? In FL_SL_XMSS_MT_ES clone... The width obstruction file says encoder domain has 2^(8n) = 2^128 elements ("The encoder's DOMAIN has 2^(8*n) elements", :114). Hmm — that says the domain is 2^128, not 2^256! Then average fiber = 2^128/2^129 < 1... and injectivity is *possible*. But that contradicts "2^127-to-one".

  Hmm. Let me look more carefully. C10DeployedGeometry.ec:110-120: "The encoder's DOMAIN has 2^(8*n) elements (WOTS_TW_ES.ec:163, :213). The encoder's CODOMAIN has w^len elements (WOTS_TW_ES.ec:219)." So in the model, msgWOTS = 8*n bits = 128 bits at C10. And codeword = 2^129. So encode could be injective! Then MM45's admit is NOT pigeonhole-false...

  But then Route (D): ThC outputs the WIDE digest via join_dgst of two dgstblocks, and out_t <- msgWOTS in the STCRC clone — so msgWOTS must hold 256 bits?? Contradiction unless msgWOTS is not 8n bits. Wait — maybe msgWOTS = dgst (list)? WOTS_TW_ES.ec:163/:213 — let me check the actual definitions. The geometry file says domain has 2^(8n) elements. But join_dgst y0 y1 : msgWOTS — so msgWOTS holds two dgstblocks = 256 bits. These conflict unless n differs: maybe msgWOTS relates to n_m or the message compression... In SPHINCS+, the WOTS message is an n-byte digest. Here C10's wots_digest returns 32 bytes = 256 bits (sha256), while dgstblock n=16 bytes. The model's dgstblock is 128 bits but the deployed message digest is 256 bits — the "wide digest" route D models the 256-bit digest as msgWOTS = pair of dgstblocks. So msgWOTS in the model must be 2·8n bits = 256 bits... but then C10DeployedGeometry's "domain 2^(8n)" is about... hmm, maybe msgWOTS is defined with n_m? At fork :193: `op P x <- size x = 8 * n_m`. There might be a separate n_m for messages. But geometry says 2^(8*n).

  I shouldn't burn too much more time; but the "2^127-to-one" claim is load-bearing for the user's framing ("admit is FALSE at deployed geometry"). The countermodel itself is conditional (hypothesis-based), so the *mechanized* content doesn't depend on it. But the user asserts falsity. Let me check quickly: msgWOTS definition in the fork WOTS_TW_ES.ec.

• One inconsistency to resolve: the geometry file says the encoder's domain is `2^(8·n)` = 2^128, but Route-D makes `ThC` output a *wide* 256-bit `msgWOTS`. The "2^127-to-one" claim hinges on this. Let me check the actual types.

• Verified: `msgWOTS = mdgstblock` = 8·n_m = 8·2n = 256 bits at C10 (n=16). Codeword space = w^len = 8^43 = 2^129. So domain 2^256 → codomain 2^129: average fiber 2^127. The user's "2^127-to-one" claim is consistent with source. ✓ (The C10DeployedGeometry "domain 2^(8n)" remark refers to dgstblock, not the wide msgWOTS — fine, not my problem to reconcile their prose.)

  Remaining checks: XmssmtCC_All application of charged theorem (:8811/:8902), and the interactive_D1 statement — but I have enough for the analysis. Let me quickly verify XmssmtCC_All's application and the charged EUNFAGCMA unfolded lemma to confirm the wiring claim, then write the answer. Actually let me also confirm the seed-withholding survives R_MEUFGCMAWOTSC_EUFNAGCMA_C: its choose calls A(OC).choose() — A_ht is Adv_EUFNAGCMA (non-adaptive: commits message list before seeing key). In the +C game, R's choose gets no ps (Adv_MEUFGCMA_WOTSC.choose has no ps param) and R's forge gets ps. So A_ht never sees ps during choose. ✓ verified from the module signature.

  One quick check of XmssmtCC_All application site. Then write the final verdict. Let me do a targeted grep.I have enough. Let me now also quickly double-check one thing for the "strongest objection": whether the S-TCR(+C) assumption (already in the bound) is *strictly weaker* than needed — i.e., BadEnc is not implied by S-TCR. S-TCR(+C): adversary targets... The S_TCR_C game: target collision resistance of ThC under predC gate — adversary gets targets and must find second preimage/collision d' = d. BadEnc needs encode d' = encode d with d' ≠ d — strictly weaker winning condition for the adversary, so hardness of S-TCR does NOT bound BadEnc. New assumption needed. ✓ (reasoning, verified by reading the game shapes).

  Also for Q2 — is the withholding the lever and is it enough? Consider: without withholding (adaptive CMA where choose sees ps), the deployed adversary could still not trivially win TCC_C... Actually with ps known during choose, adversary can offline-search x, c pairs itself via OC until it finds two colliding codewords... but each query x_i commits one message; the forgery must be on the same instance/address ad_i. The win needs d' = ThC ps ad_i m' c' colliding under encode with d_i where d_i = ThC ps ad_i x_i grind(x_i). With ps known at choose time, adversary can compute ThC outputs itself (OC needs ps? OC.init(ps) — the oracle has pp set at init; adversary queries OC without knowing ps but the oracle uses ps. Can the adversary compute ThC offline? Only via OC queries. So withholding ps from choose doesn't prevent computation — OC gives oracle access anyway. What withholding actually buys: it prevents the adversary from *selecting* the committed messages as a function of the seed — but since ThC is only accessible via OC anyway, the withholding is nearly cosmetic for THIS event! Hmm, wait: in choose, the adversary HAS OC access (choose can call OC.query — the module type allows it: `proc choose() : unit { O.query, OC.query }`). So during choose, the adversary can evaluate ThC at the real ps via OC. So it could grind for an x whose grinded digest has a codeword with a easy second preimage... but it still needs to FIND a codeword collision, which is the hard birthday search regardless. So seed-withholding is NOT the real lever for BadEnc hardness — the lever is the birthday hardness of finding codeword collisions among ThC outputs, full stop. Withholding matters for the *naCMA structure* of the +C game generally, but for TCC_C the assumption's plausibility rests on encode-fiber collision search being hard (~2^64.5 work given 2^129 codewords... birthday over codeword space 2^129 → ~2^64.5 ThC evaluations; that's the quantitative content).

  Hmm, actually there's a subtlety: the adversary doesn't need a collision with a *specific* target; it needs d' colliding with d_i for the instance i it picks, where it can compute d' candidates offline via OC and it knows d_i (it's in the transcript? The adversary sees the signature sig and pk but does it see d_i? In the +C game, query returns (pk, (sig, c)) — NOT d. The adversary knows x_i, c_i (returned counter!), and can compute d_i = ThC ps ad_i x_i c_i via OC (it has ps at forge time, and c_i is in the signature it received). So yes it can compute the target codeword encode(d_i). Then it searches for (m', c') with encode(ThC ps ad_i m' c') = that codeword — a *target codeword preimage within fiber* search: each random (m',c') gives a random codeword; probability of hitting the specific fiber of size 2^127 out of 2^256 digests is 2^-129 per try... wait: encode(d') = specific codeword ⟺ d' in the fiber of that codeword, fiber size ~2^127, so per-try success 2^127/2^256 = 2^-129. That's WORSE than birthday! The adversary can't do better than 2^-129 per ThC evaluation for hitting a *specific* codeword... unless it can find ANY collision pair — but no: freshness requires d' ≠ d_i and the win is tied to instance i's codeword. Hmm, could the adversary in choose plant MANY queries x_1..x_q and in forge find d' colliding with ANY of them? It must output index i; the win checks against query i. So it's: given q target codewords (from its own chosen messages), find d' landing in any of the q fibers. Per-try success ~q·2^-129. With q = 2^64 queries... still 2^-65 per try. So TCC_C is plausibly VERY hard — ~129-bit — as long as fibers are near-uniform. Actually that's great news for the architecture: the assumption is a *targeted fiber preimage* assumption, much stronger-plausible than generic collision. Wait, but why did I earlier think birthday? Birthday applies if the adversary needs any two colliding d, d'. Here one side is pinned by the transcript (it must collide with a committed query's codeword, and it learns those codewords only... well it can compute them via OC once ps is available at forge). So per-candidate probability q/2^129. With q ≈ 2^10 (the hypertree instance count c — actually c = number of WOTS instances = big), still fine.

  BUT — crucial asymmetry: the adversary also controls the queried side! It picks x_i (committed), then the game computes d_i = ThC ps ad_i x_i grind(x_i). Could the adversary pick x_i such that the fiber of d_i is HUGE or degenerate? Fibers are determined by encode; a codeword with an enormous fiber (up to... max fiber of encode) — if encode is the natural base-8 projection (take first 43 digits of the 256-bit number? no—) hmm, what is the deployed encoder on a 256-bit digest producing 43 base-8 digits = 129 bits? It must DROP 127 bits. If encode(d) = base8(d mod 8^43) i.e. ignores the high 127 bits — then fiber = all digests sharing low 129 bits, size exactly 2^127, uniform. Then finding d' in the fiber of d_i = finding d' with same low 129 bits = second-preimage of 129-bit truncation under ThC — ~2^129 work per target... wait no: d' = ThC(ps, ad_i, m', c') is a random 256-bit value; need its low 129 bits to match: 2^-129 per try. So ~2^129/q work. Plausibly hard. Good.

  Unless the adversary can influence WHICH fiber via x_i choice to pick... all fibers same size. OK.

  BUT WAIT — one more edge: c' is chosen by the adversary in forge, and m' too; and the win requires predC d' (digit sum = 205). Fraction of codewords on the surface: codewords are 43 digits of 0..7, mean sum 150.5, target 205 — in the upper tail. Fraction maybe 2^-10 or so. That just multiplies work. Fine.

  Also the queried side: P d_i required (res conjunct P m). The grind guarantees predC d_i when grind succeeds. ✓.

  So Q2's answer: the assumption to name is a *targeted encode-fiber second-preimage* game; and importantly, seed-withholding is NOT the main lever — the adversary has OC access during choose and gets ps at forge, so it can evaluate ThC at will after commitment; what makes the assumption plausible is that the committed side pins q target codewords and the adversary must land in one of ~q fibers out of 2^256 — a ~2^129/q per-try event. Hmm wait, but actually can the adversary ALSO re-roll the queried side? The queried digests d_i depend on x_i committed pre-ps. Since the adversary can't predict ps, it can't steer d_i into a chosen fiber. If withholding were ABSENT (choose sees ps), the adversary could compute d(x) for many x via OC and select queries whose fibers it has pre-computed second-preimages for?? To have a "pre-computed partner" it needs two digests in the same fiber — that's a fiber COLLISION search (birthday over codewords: 2^64.5 work) then commit one as query and forge the other. THAT works if choose sees ps (it can evaluate ThC via OC during choose and find colliding pairs offline)! Wait — but even WITH withholding, can it do the collision search during forge? During forge it has ps and OC: find ANY pair (d1', d2') with encode equal, predC both — but the win requires d2' = d_i for a committed i... it can't retroactively commit. Hmm, but wait: actually can it? The win condition: d' (forge side) must collide with d_i (query side). During forge, d_i are already fixed by committed x_i. The adversary could, during forge, search for d' colliding with one of the q committed digests — per-try q/2^129. Alternatively during choose (if it could evaluate ThC then — IT CAN, OC is available in choose even without ps! OC.query uses the oracle's stored ps). OH. So withholding ps does NOT prevent ThC evaluation during choose — OC is keyed with ps at init, before choose runs. So even in the naCMA game, the adversary can evaluate ThC under the real seed during choose via OC!

  Then what stops: during choose, find codeword collision pair (d1 = ThC ps ad x1 c1, d2 = ThC ps ad x2 c2) both predC (grind c1, c2), commit query x1 (oracle grinds c = grindC(x1), which may differ from c1! The oracle computes its own counter via grind — deterministic, so if adversary computed grind itself correctly, d_i = ThC ps ad x1 (grind x1) = d1). Then forge with m' = x2, c' = c2 — d' = d2, encode d' = encode d1, d' ≠ d2... is_fresh at WOTS-TW level: d' ≠ d ⟺ d2 ≠ d1 ✓ (they're distinct digests). And m' ≠ x at +C level ✓. So THIS IS A WINNING STRATEGY for TCC_C — and even for the actual +C game?? Wait — in the actual +C game, forge (m'=x2, c'=c2, sig'=sig replayed): verify recomputes encode d2 = encode d1, checks pk match — sig from query x1 verifies under message digest d2 since codewords equal! And predC d2 ✓, is_fresh m' ≠ x1 ✓. So a genuine FORGERY against WOTS+C, costing a birthday collision search over encode∘ThC codewords ≈ 2^64.5 work (codeword space 2^129, restricted to surface ~times 2^something).

  Hold on — this is exactly A_coll lifted to the +C layer, and it shows TCC_C is bounded by a ~2^64.5 birthday attack, NOT 2^129. Because the adversary controls BOTH sides as long as it can commit the query side after finding the pair — and naCMA withholding doesn't stop it since OC is live during choose.

  So: (a) the withholding is NOT the lever — OC.query is available in choose, keyed with the real ps; (b) the right named assumption is essentially collision resistance of encode∘ThC on the predC surface, which is a ~birthday assumption with concrete cost ≈ sqrt(#surface codewords); (c) at C10 geometry: #43-digit base-8 words with sum 205 — need the number. Sum 205 out of max 301, mean 150.5, sd ≈ sqrt(43·var) where var of uniform 0..7 = 5.25, sd ≈ 15. So 205 is (205-150.5)/15 ≈ 3.6 sigma up. Fraction ≈ 4·10^-4 ≈ 2^-11.3. So #surface codewords ≈ 2^129 · 2^-11.3 ≈ 2^117.7, birthday ≈ 2^59 work. Hmm — that would be a ~59-bit term! That's a REAL quantitative number that belongs in the bound. Actually wait — but the adversary needs both d1, d2 in the surface AND same codeword — birthday over surface codewords: ~2^(117.7/2) ≈ 2^59 ThC evaluations. That's below C10's presumed 128-bit security target — but it's an assumption-term, like all the others; the bound just has to carry it honestly. Hmm, actually is it a real attack on the scheme? It's a forgery at ~2^59 work?! That would be a genuine security-relevant finding... but note the deployment comment: "THE DEPLOYED WALLET IS NOT AFFECTED... C10's WOTS layer never encodes an adversary-chosen value — it encodes key-determined internal nodes". In the hypertree, the WOTS+C queries are roots of subtrees — in the REAL scheme, the adversary doesn't choose x_i freely either: R_MEUFGCMAWOTSC_EUFNAGCMA_C builds the cube from A_ht's committed leaf messages; upper-layer roots are computed by the reduction. A_ht commits leaf messages, and those get hashed... the WOTS+C queries at layer 0 sign... hmm, the messages signed by WOTS are roots/pk — the adversary influences them through chosen FORS messages? In SPHINCS+, the bottom-layer WOTS signs FORS public keys which depend on the message. So the adversary does influence WOTS inputs via chosen messages — malleable but hashed (ThC applied). The x_i are ThC/preimage-derived values the adversary can't directly set, only sample by choosing messages. It can still do the birthday search: choose message pairs until the induced (x, d) values collide in codeword. The induced d_i are still ThC outputs of the committed messages, so the attack is: find two committed messages whose grinded ThC digests share a codeword — 2^59-ish work — then query one, forge the other. Wait, but is_fresh at the EUF-NAGCMA level... the forgery must be a fresh hypertree message — the two committed messages m1 ≠ m2 both get signed in choose (NAGCMA signs all committed); forging means outputting a signature for a message NOT in the committed list. Here m' = x2 IS in the committed list! Hmm — in the +C WOTS game, is_fresh is on dgstblock x; in the hypertree game, freshness is on the hypertree message. If A_ht commits both m1, m2, both are signed — no forgery. To forge, A_ht needs the colliding pair to occur ACROSS the query boundary: one inside the cube (committed) and one in the forgery. The forgery message m' is free at forge time, but then its hypertree path goes through WOTS instances whose roots were NOT queried... The reduction R_MEUFGCMAWOTSC_EUFNAGCMA_C extracts a WOTS+C forgery at some layer where the forged root differs from the honest root — the forge-side WOTS message is root' computed from the forged signature's pkWOTS reconstruction. So the forge-side WOTS input x' is not freely chosen; it's derived. Can the adversary steer root' into a colliding fiber? It can grind its forgery (try many forged signatures/messages) — each attempt gives a new x' — and check encode(ThC ps ad x' c') against the target codeword of the instance it aims at: per-try success 2^-129·(fiber)... wait no — the check is encode d' = encode d_i for the SPECIFIC instance i where the forgery lands. Per-try probability 2^127/2^256 = 2^-129 (hitting fiber of that codeword). Unless the adversary can choose which instance — the forgery "finds the layer that is a WOTS+C forgery" — it can aim at any instance whose root it can malleate... but per attempt it must fix the instance to check against. It can check against ALL q instances simultaneously: q/2^129 per try. Hmm OK so in the deployed composition, the committed side can't be steered into a pre-found collision pair (the pair-mate must be committed BEFORE ps... no wait, the pair-mate must be committed during choose, and during choose A_ht CAN evaluate OC (it gets OC directly — :498 `ml <@ A(OC).choose()`). So A_ht CAN do the 2^59 birthday search during choose and commit BOTH mates... but both mates then get signed — no forgery. It needs one mate signed and the other fresh. Since ALL committed messages get signed in NAGCMA, committing both kills freshness. Commit only m1: then during forge, must find m' inducing d' in d1's fiber: 2^-129 per try (single target). BUT it could commit q = 2^64 messages all in ONE fiber?? No — to commit messages all landing in one fiber it must FIND them: each committed message's d is computable via OC during choose, so it can filter: evaluate ThC for many candidate messages, keep those whose codeword equals a fixed target codeword cw0 — 2^129 work to find each... no wait, 2^-129 per candidate... Finding one message hitting a fixed codeword = 2^129 work. Alternatively birthday: generate 2^59 candidates, get ~sqrt collisions internally, but a collision PAIR both committed = both signed = useless.

  So the asymmetry of NAGCMA (all committed are signed) forces the forge side to be found after commitment against fixed targets: q/2^129 per try, q = poly → ~2^129 security. GOOD for the scheme. And this is where seed-withholding DOES matter: wait, no — as established, OC is live during choose, so withholding ps doesn't limit ThC evaluation. What limits the attack is the NAGCMA structure (commit-then-all-signed) + the freshness condition. Hmm, but actually there's a subtlety: does A_ht's committed list have size bound l (constant)? In EUF-NAGCMA, the adversary commits a list of l messages. The WOTS+C queries per committed message = the hypertree cube instances. The targets available for the forge-side hit = all WOTS instances in the cube — many (c instances). The win needs encode d' = encode d_i where d_i is the QUERIED message at the extracted instance and d' = the reconstructed forge-side digest at that instance, with d' ≠ d_i.

  OK here's the thing though — the reduction R_MEUFGCMAWOTSC_EUFNAGCMA_C's forge: the extracted WOTS+C forgery (fidx, root', (sig', counter')) — the +C game's forge output is (i, m', sigc') where m' = root' (a dgstblock node value), and the game checks is_fresh m' ≠ x_i where x_i was the queried root at instance i. Now BadEnc: encode(ThC ps ad_i x_i c_i) = encode(ThC ps ad_i m' c'). The adversary controls m' = root' only through its forged hypertree signature's reconstructed pk — it can grind over forgery randomness. Each grind attempt: probability that encode(ThC(root')) hits the fiber of ANY of the q cube digests ≈ q·2^-129. So TCC_C at the deployed adversary ≈ birthday-infeasible 2^129/q — SECURE. 

  BUT the direct WOTS+C game adversary (not the deployed composition) CAN do the 2^59 attack? No wait — same NAGCMA constraint: in M_EUF_GCMA_WOTSC_NPRF, ALL choose-queries get signed; forge must be fresh m' ≠ x_i for the extracted i... the game's is_fresh checks m' ≠ m where m is the i-th query — only the i-th! m' could equal x_j for j ≠ i! Re-read: `is_fresh <- m' <> m` where (ad, m, pk, sig) = get(i) — the i-th query only. So the adversary can commit x_1..x_q, find a codeword collision pair (x_1 at address ad_1, x_2 at... hmm but addresses: query i uses wad_i chosen by adversary per query. The forgery binds to instance i via index i, and the check compares against query i only. So: commit (wad_1, x_1), (wad_2, x_2) with encode d_1 = encode d_2, d_1 ≠ d_2 (found by 2^59 birthday during choose via OC). Forge: (i=1, m'=x_2, sigc' = sig from query 2)! Check: is_fresh: m' = x_2 ≠ x_1 = m ✓. is_valid: verify(pk_1, x_2, (sig_2, c_2)): recompute encode(ThC ps ad_1 x_2 c_2)... WAIT — the address! verify uses ad = ad_1 (instance 1's address) but d' computed at ad_1 of (x_2, c_2) — whereas d_2 = ThC ps ad_2 x_2 c_2. If ad_1 ≠ ad_2, ThC differs, collision doesn't transfer. So the attack needs ad_1 = ad_2. But dist_addresses requires uniq wgpidxs — same wad twice is disallowed (uniq_wgpidxs over query addresses)! dist_wgpidxs conjunct kills it. Hmm — does it? uniq on get_wgpidxs of the addresses. Same wad → not uniq → res false. So the classic replay-across-instances is blocked by dist_addresses, which the countermodel respects (A_coll uses ONE query). OK so with one query per address, the adversary can still: commit x_1 at ad_1, and during forge grind (m', c') until encode(ThC ps ad_1 m' c') = encode d_1 — 2^-129 per try. So TCC_C is a ~2^129/q assumption. 

  Wait, but hold on — could the adversary use MULTIPLE queries with the SAME address but... dist_addresses blocks it in the win. OK.

  So final quantitative story: the named assumption (call it TCC_C) is: committed (ad_i, x_i) with distinct wgpidxs; keyed ThC; win = (i, m', c') with predC d', encode d' = encode d_i, d' ≠ d_i. Per-try success ≤ q/2^129 (fiber size 2^127 over 2^256 digests, times q targets). This is a plausible, honest, quantitative assumption at C10 geometry — ~equivalent to a 129-bit second-preimage assumption on the truncation. NOT a hand-wave; and the fiber-uniformity caveat (encoder must not have skewed fibers) is the concrete audit item.

  Now — one more critical check on my claim "OC is live during choose": Adv_MEUFGCMA_WOTSC: `proc choose() : unit { O.query, OC.query }` — yes, choose may call OC.query (WOTS_C_Scheme.ec:142). And OC.init(ps) happens before A.choose() in the game (:199-201). So the adversary evaluates ThC under the real seed during choose. Seed-withholding from choose is therefore NOT what protects the BadEnc event. This directly answers the user's "is that the lever, and is it enough?" — No and no: the lever is the commit-then-sign-all structure plus per-instance address uniqueness (dist_wgpidxs) plus freshness; withholding ps is nearly irrelevant here because OC is keyed and live during choose. That's a solid, blunt, source-verified correction to their framing (Q4).

  Hmm wait, actually let me double-check that the OC in choose is keyed with the real ps: M_EUF_GCMA_WOTSC_NPRF.main: ps <$ dpseed; O.init(ps); OC.init(ps); A.choose(). Yes (:197-201). And in Game4_WOTSTWES_BadEnc: same order (:3277-3282). ✓.

  And in the SPHINCS+ EUF-NAGCMA game at the top — is OC available to A_ht.choose? R_MEUFGCMAWOTSC_EUFNAGCMA_C.choose calls A(OC).choose() — yes (:498). ✓.

  Now Q1 final verdict. Options: (a) keep charge at WOTS-TW + bridge lemma at +C (my recommendation); (b) redefine at +C. Content-wise identical since the event commutes through R_int_WOTSTW. But (b) done naively = "remove summand from WOTS-TW theorem" is unsound (theorem is false without it — countermodel). What breaks if restated at +C: you still need the WOTS-TW-internal disjunction and the mu_split; the charge cannot be excised from the WOTS-TW layer, only bounded from above. And uniformity: the bridge lemma is uniform in A <: Adv_MEUFGCMA_WOTSC — the user's belief that bounding only works at the single deployed instantiation is wrong; R_int_WOTSTW is a functor and the bridge quantifies over all A. So "move it to +C" in the sense "state and bound the assumption at +C, uniformly" = correct and compatible with keeping the charge where born. Does R_int_WOTSTW preserve the needed structure? YES — verified: query stores d = ThC pp ad m c (:1788-1793) as the WOTS-TW message; forge emits ThC ps ad_i m' c' (:1813); under encb the +C event and WOTS-TW event coincide. One caveat: the coupling lemmas (choose_tw/forge_tw/interactive_hop2) were built for the standard game; the bridge to Game4_WOTSTWES_BadEnc needs a new event-inclusion proof, but the transcript correspondence is the same one already mechanized. Also the grind_fails nuance: on the query side, Game4's res includes P m — the P conjunct kills grind-failure instances, so the bridge can require predC d_i for free.

  Another "what breaks": at the +C layer, the natural +C-level BadEncC event would be encode_msgWOTS_C ps ad x c = encode_msgWOTS_C ps ad m' c' — but note verify ALSO has the okC gate and counter in the signature; a +C-level flag defined inside M_EUF_GCMA_WOTSC_NPRF would need the game re-run with the flag module — same pattern as Game4_WOTSTWES_BadEnc, fine.

  Q3 salvageable? Yes — with one correction: it's not "a bound that holds only at one instantiation". It's: a TRUE decomposition at the WOTS-TW layer (universally quantified, but whose RHS exceeds 1 for some A — like any bound with an explicit failure-event term, e.g. Pr ≤ ε + Pr[Bad]) plus a uniform bridge at +C. That's standard "bad-event" analysis (the Bellare-Rogaway game-playing idiom): Pr[G] ≤ Pr[G'] + Pr[bad]. MM45's theorem with an explicit BadEnc term is EXACTLY the fundamental-lemma shape. The defect was never the shape; it was that the original theorem hid Pr[bad] = 0 as an admit. Is the exported theorem vacuous? For arbitrary A, RHS can exceed 1 — but that's true of ANY bad-event bound before the bad event is bounded; the theorem's content is the decomposition, and its quotation sites must pair it with a bad-event bound. The one place it's quoted (XmssmtCC_All → GprocQWiredWotsCharged) does so at a ThC-structured adversary where the TCC_C bridge applies. So: progress, not a bookkeeping trick — PROVIDED the exported theorem is never quoted standalone for arbitrary A, and the TCC_C term eventually replaces the raw BadEnc term in the headline. If they stop here, the headline carries a raw term that is 1 for some adversaries — displayed, named, honest, but undischarged: that's an incomplete deliverable, not a trick.

  Strongest single objection: I need to pick THE strongest. Candidates:

  A. Seed-withholding is not the lever (OC is keyed and live during choose; commit-sign-all + dist_wgpidxs + freshness are). If they build the assumption on the wrong lever, they'll state the wrong game — e.g., an assumption game without OC access in choose would be FALSE-to-model (the real adversary has it) — i.e., they might state an assumption that is *weaker than reality demands*... wait, if the assumption game denies the adversary OC during choose but the real adversary has it, the bridge reduction fails to simulate — the assumption must give OC throughout. If they'd built it on "withholding is the lever," they'd have stated an assumption that doesn't cover the real adversary — a silent gap. This is a concrete, load-bearing, framing-level error with a mechanical consequence. Strong.

  B. The quantitative audit: the TCC_C event's probability depends on encode's fiber structure over ThC outputs; nothing in the repo currently pins encode_msgWOTS at all (width obstruction: the encoder CANNOT be pinned in the model — C10DeployedGeometry documents this). So the assumption TCC_C is about an ABSTRACT encoder; instantiating it at the deployed encoder is the same unpinned-encoder gap that already dogs target_sum reachability (ThCWidth.ec hand-transcription). The charge can be bounded only modulo an assumption whose deployed-instance truth is currently not mechanically checkable in this repo. That's also strong — it means "bounded at +C" will forever be "assumed at +C", and the assumption's plausibility argument (fiber uniformity, 2^129 work) is extra-mechanized prose.

  Which is the strongest objection? B is about the ceiling of the whole approach; A is about a specific error in their current plan. The user asks "the single strongest objection you have". I think B is more fundamental: the encoder is unpinnable in the model (their own geometry file proves the width obstruction), so ANY assumption about encode∘ThC — including TCC_C — is an assumption about an object the formalization cannot instantiate; the best the repo can do is carry it as a named axiom-term forever, alongside the existing hand-transcribed ThCWidth leg. But that's arguably "just how assumptions work" (thfc is abstract too — SM-DT-TCR-C is also an assumption about an abstract collection). Hmm — so B is not fatal; assumptions about abstract ops are house style. The difference: SM-DT-TCR-C is a standard published assumption; TCC_C is novel and needs external cryptanalysis. Still fine.

  Actually, let me reconsider A vs a sharper version: the real sharpest objection might be about the S-TCR overlap: is TCC_C actually IMPLICAD... no.

  Or: the countermodel's conditional nature — encode_msgWOTS is FREE in the model; the colliding pair is a hypothesis. The claim "the charge is 1 at deployed geometry" additionally requires that a colliding pair EXISTS ON THE CONSTANT-SUM SURFACE with P cm (both on surface) — surface collisions. Average surface fiber: #digests/#surface-codewords = 2^256/2^117.7 = 2^138 per surface codeword — abundant. Fine.

  Hmm, but here's another sharp one for Q3/Q4: **the mu_split charge is not tight** — wait, actually let me check something potentially devastating to the "sound" claim: in Step_Game4_WOTSTWES_SMDTPREC, the split is Pr[Alt : res] = Pr[Alt : res ∧ badenc] + Pr[Alt : res ∧ !badenc], and the !badenc half is bounded by the PRE reduction. The PRE reduction's win extraction uses hchwpre which comes from nhchwcoll_hchwpre_msg's RIGHT disjunct given !badenc. Fine — verified at :6690-6696.

  And is the charge possibly DOUBLE-COUNTED? The res ∧ badenc branch: could those wins also be wins of the PRE game? Doesn't matter — it's an upper bound via split; both terms bounded separately, sum is valid.

  One more possible strongest objection: **the BadEnc event as defined (em = em') is broader than the event the proof needs** — the proof needs exactly "encode collision" — no, it's exactly that.

  Let me also double check the claim in the header "the deployed wallet is not affected... compute_fors_pk takes no message argument" — that's about FORS. For WOTS in the hypertree, messages ARE influenced by adversary-chosen roots as discussed. The header's claim "C10's WOTS layer never encodes an adversary-chosen value" — in SPHINCS+, bottom-layer XMSS WOTS signs FORS roots which are message-dependent (adversary-influenced via chosen messages). So "never adversary-chosen" is too strong — but adversary-INFLUENCED through hashes is exactly what TCC_C models. The wallet claim is about a specific deployed usage; fine, not my main attack surface. Actually it matters for Q3's "meaningful security statement": the TCC_C assumption IS the right object precisely because WOTS inputs are adversary-influenced ThC outputs.

  Now, deliverable structure:

  (1) Placement verdict: KEEP the charge at the WOTS-TW layer (it is born there; the theorem is false without it — their own countermodel proves removal is unsound), AND name/bound it at the +C layer via a uniform bridge lemma at R_int_WOTSTW(A) for all A <: Adv_MEUFGCMA_WOTSC. The user's dichotomy ("move it to +C vs keep it") is partly false: the event commutes through R_int_WOTSTW (verified at :1788-1793, :1813), so "defined at +C over ThC outputs" and "bounded at R_int_WOTSTW(A)" are the same statement. What breaks if you literally restate it at +C and drop it from WOTS-TW: the WOTS-TW theorem becomes unprovable (countermodel); the disjunction is generated inside Step_Game4 and cannot be discharged after the fact from one layer up. Also the "only at one instantiation" claim is wrong — uniformity is free because R_int_WOTSTW is a functor.

  (2) The named assumption: TCC_C-style game. State it:

  ```
  module TCC_C (A : Adv_MEUFGCMA_WOTSC) = {  (* same oracle interface; the assumption game *)
    proc main() = {
      ps <$ dpseed; O.init(ps); OC.init(ps);
      A.choose();
      (i, m', sigc') <@ A.forge(ps);
      (ad, m, pk, sigc) <@ O.get(i);
      d  <- ThC ps ad m  (grindC ps ad m);   (* hmm — or use stored counter sigc.`2 *)
      d' <- ThC ps ad m' sigc'.`2;
      return 0 <= i < nrqs /\ d' <> d /\ predC d /\ predC d'
             /\ encode_msgWOTS d = encode_msgWOTS d' ... 
  ```

  Careful: queried-side digest — in Game4 the stored m is d_i = ThC ps ad_i x_i c_i with c_i the grind result. In the +C oracle O_MEUFGCMA_WOTSC_Default.qs stores (ad, x, pk, (sig, c)) with c the counter. So TCC_C can read c from the stored query: d = ThC ps ad x sigc.`2. And note P d_i in Game4 — predC d — holds on res. Bridge lemma: Pr[Game4_WOTSTWES_BadEnc(R_int_WOTSTW(A)) : res ∧ badenc] ≤ Pr[TCC_C(A) : res] — proof by byequiv with the existing coupling (choose_tw/forge_tw transcript invariants), dropping the unneeded conjuncts. Should TCC_C keep dist_wgpidxs and the nrqs bounds? Keep them — cheap, and they strengthen... no wait, keeping conjuncts makes the event SMALLER (bound still valid) and the assumption WEAKER (easier to believe)? Keeping conjuncts shrinks the win event → Pr smaller → assuming it's small is a WEAKER assumption, and the bridge requires showing res∧badenc implies TCC_C win — so keep exactly the conjuncts that res∧badenc guarantees: 0≤i<nrqs ✓, d'≠d (is_fresh) ✓, predC both ✓ (P m, P m'), encode equal (badenc) ✓, dist_addresses ✓ (res has it), disj_wgpidxs ✓ (res has it). Keep them all — the bridge is then pure event inclusion, and the assumption is as weak as possible. Good — that's the house-correct shape.

  But note: TCC_C keeps the WOTS+C SIGNING oracle in the game (A needs its queries answered) — the assumption is "even with the full signing oracle, you can't produce a codeword collision across the query/forge boundary." Fine — that's the naCMA-flavored assumption.

  Name: repo style — "SM-DT-..." no; it's not a per-member collection assumption. IncEnc.ec already uses T_COLL_RES for the Def-11 analog. This one is the WOTS+C analog: I'd name it `T_COLL_RES_C` or better descriptive: `CW_TCR_C` ... The repo's own pointer calls the gap "the T-COLL-RES event (Def 11)" (:1473). Naming: `T_COLL_RES_WOTSC` with advantage term `Adv^{T-COLL-RES}_{encode∘ThC}`. State the shape: game as above + reduction (the bridge lemma) + note it is NOT implied by S-TCR(+C) (encode collisions need no ThC collisions; fibers are 2^127 of 2^129 codewords... i.e., codeword equality is 2^127× coarser than digest equality).

  And the withholding answer: no — OC is keyed at init and callable in choose (WOTS_C_Scheme.ec:142, :197-201); the real levers are (i) all committed queries get signed & freshness is per-index, (ii) dist_wgpidxs forbids address reuse so cross-instance replay is out, (iii) the per-try fiber-hit probability q·2^-129. Withholding ps from choose is close to irrelevant for this event — and an assumption game built on "withholding is what protects us" risks omitting OC from choose and being unsound-to-model.

  (3) Salvageable? Yes, bluntly: what they built is the fundamental-lemma-of-game-playing shape; the previous state was a FALSE admit. Net progress, unconditionally. But the exported MEUFGCMA_WOTSTWESNPRF_Charged must be treated as an internal decomposition lemma; the only honest headline is the one carrying the explicit term (as GprocQWiredWotsCharged does) until TCC_C replaces it. A bound that is 1 at some adversaries is not a trick — Pr[G] ≤ stuff + Pr[bad] is never vacuous as a decomposition; it is vacuous as a SECURITY STATEMENT only if quoted without bounding Pr[bad]. They are not doing that (the term is displayed raw at the headline). So: salvageable, is progress, not a trick — conditional on the TCC_C bridge being built and on nobody quoting the charged WOTS-TW theorem as a standalone bound.

  (4) Strongest objection: pick one. I'll go with the lever misidentification + its mechanical consequence (A), because it's a concrete error in their stated plan ("Note M_EUF_GCMA_WOTSC_NPRF's adversary WITHHOLDS ps from choose — is that the lever?") with a wrong-game risk. Actually wait — they ASKED whether it's the lever; they didn't assert it. The deliverable (4) is "the single strongest objection you have". Hmm. Options:

  - Objection B (unpinnable encoder): the named assumption can never be instantiated in-repo; its deployed-plausibility argument (fiber count, 2^129) rests on the same hand-transcribed width leg they already flagged. This caps what the whole exercise can ever certify: the charge moves from "false admit" to "named assumption whose concrete truth is outside the mechanization". Some would call that the honest end-state; but the strongest form: the quantitative claim "2^127-to-one ⇒ TCC_C is a 129-bit assumption" depends on fiber UNIFORMITY of the deployed encoder (sha256-truncation structure), which is exactly the kind of thing that could fail (e.g., if encode ignores 127 bits, fibers are uniform — fine; if encode's fibers are skewed, per-try probability could be much higher for adversarially-chosen targets... but targets are ThC outputs, so adversary can't steer into a fat fiber except by grinding: if a fat fiber of relative size 2^-100 exists, per-try hit q·2^-100...). The assumption's quantitative content is genuinely new cryptanalysis, not a citation. 

  - Objection A (wrong lever → wrong game).

  I think the strongest single objection is actually the combination: the countermodel proves unboundedness at the WOTS-TW layer, but at the +C layer the SAME replay attack survives in diluted form... no wait, we showed it's bounded by fiber-hit. Hmm, let me stress once more whether TCC_C might actually be FALSE (attackable) at the deployed adversary — if TCC_C were false, the whole architecture collapses. Attack: adversary needs (i, m', c'): encode(ThC ps ad_i m' c') = encode(ThC ps ad_i x_i c_i). It knows cw_i = encode d_i after computing d_i via OC (ps available at forge, c_i in transcript). Grinding (m', c'): per try, d' uniform 256-bit → hit fiber (2^127 of 2^256) = 2^-129. Plus predC d' needed (2^-11ish). So ~2^140 work. No structural shortcut: the adversary cannot choose d' directly (ThC is random-oracle-like), cannot reuse another instance (dist_wgpidxs), cannot set c' retroactively... c' IS free — it tries counters c' for a fixed m'? Each (m', c') is one ThC eval. No shortcut. Also could the adversary exploit grind determinism: the query side counter is grindC ps ad x — the SMALLEST predC counter (per Grind.grind semantics, presumably first in enum order). Does that skew the codeword distribution of d_i? Slightly (selection bias toward predC — but that's just conditioning on the surface). The forge side needs the SAME codeword, and it's on the surface too. So the event is: two surface digests, same codeword. Fiber sizes on the surface ~2^138 average (2^256 digests / 2^118 surface codewords). Per-try hit ≈ 2^138/2^256 = 2^-118. ~2^118 work. Still fine. So TCC_C is plausible. Good — architecture stands.

  One more consideration for Q1's "what breaks": if they move the flag/event to the +C layer by modifying M_EUF_GCMA_WOTSC_NPRF itself (adding BadEncFlag there), they'd fork the +C game away from its paper shape and every existing lemma (interactive_D1 etc.) would need re-proof — whereas keeping the charge at WOTS-TW reuses everything and adds one bridge lemma. Concrete cost asymmetry. Also: the +C-layer event cannot be pushed down past R_int_WOTSTW into a *charge-free* WOTS-TW theorem because the WOTS-TW proof needs the disjunction internally — the !badenc half of the split is what feeds SM-DT-PRE; without carrying badenc through Game4, the PRE reduction's extraction step has no hchwpre. So "defined at +C and pushed down" is not available; only "born at WOTS-TW, pulled up and bounded at +C" is. That's a direct answer to "does R_int_WOTSTW let a +C-level event be pushed down, or does the reduction destroy the structure needed?" — R_int_WOTSTW preserves the structure (message = ThC output, verified), but push-DOWN is the wrong direction; the event is born below. Pull-UP works.

  Also Q4 "where is my framing wrong" — collected items:
  1. "It can only be bounded for the specific deployed adversary" — wrong: uniform in A via functor R_int_WOTSTW(A); the bridge quantifies over all Adv_MEUFGCMA_WOTSC.
  2. "Seed-withholding is the lever" — wrong: OC is keyed before choose and callable in choose; levers are commit-sign-all + per-index freshness + dist_wgpidxs + fiber-hit rarity.
  3. "The charged bound's RHS ≥ 1 at generic A ⇒ quantitatively vacuous ⇒ maybe not progress" — a bad-event decomposition is not expected to be < 1 for all A; vacuity only if quoted without a bad-event bound. Framing error: judging a decomposition lemma by standalone quantitative content.
  4. Minor: "the deployed wallet never encodes adversary-chosen values" — over-broad; bottom-layer WOTS signs message-influenced roots; the correct statement is that adversary influence is ThC-mediated, which is precisely why TCC_C (not raw encode collision) is the right assumption.
  5. The claim "2^127-to-one" — VERIFIED consistent: msgWOTS = mdgstblock = 8·n_m = 16n bits = 256 bits at n=16 (:46, :191-197, :270); codeword space w^len = 8^43 = 2^129 (C10DeployedGeometry.ec:168). Average fiber 2^127. ✓.

  Also verify: "pkWOTS_from_sigWOTS reads message only via encode" — verified in countermodel proof (pkfs_computes_fun etc.) and the game code. The claim about A_coll is mechanized, admit-free, with controls — I read the whole file; consistent. I did not re-run EasyCrypt (read-only, container-only). State that plainly.

  One more thing to verify — the claim that M_EUF_GCMA_WOTSC_NPRF's is_fresh is per-index only (m' ≠ m of get(i)): verified :204-207. And dist_addresses over query addresses: :209-210. ✓. Note: TCC_C inherits these, which is why the cross-instance replay (commit both mates) fails: the mate at a different address doesn't collide at ad_i... wait, actually let me re-examine the "commit both mates" attack once more under TCC_C, since TCC_C's win is keyed to instance i like the +C game. Mates (ad_1,x_1), (ad_2,x_2) with ad_1 ≠ ad_2: d_2 = ThC ps ad_2 x_2 c_2. Forge (i=1, m'=x_2, c'=c_2): game computes d' = ThC ps ad_1 x_2 c_2 ≠ encode-related to d_2. encode d' vs encode d_1: no relation — the birthday collision was between d_1 = ThC ad_1 x_1 and d_2 = ThC ad_2 x_2. d' = ThC ad_1 x_2 is a fresh random value. So no win. To win, need collision at the SAME address: ThC ps ad_1 x_1 c_1 vs ThC ps ad_1 x_2 c_2 — the adversary can search for exactly this during choose! Birthday over same-address ThC outputs: 2^59-ish work finds x_1, x_2, c_1, c_2 with same address, same codeword, distinct digests, both predC. Commit (ad_1, x_1) AND (ad_1, x_2)? dist_wgpidxs violated (same wgpidxs twice) → TCC_C keeps dist → no win. Commit only x_1, forge x_2: but then the pair search must happen... during choose the adversary found the pair; it commits x_1; at forge it outputs x_2, c_2. dist_addresses: queries = [ad_1] → uniq ✓!! Wait — dist_wgpidxs is over QUERY addresses only ([ad_1], uniq ✓). is_fresh: m' = x_2 ≠ x_1 ✓. d' = ThC ps ad_1 x_2 c_2 = d_2 ≠ d_1 ✓ (distinct digests). encode d' = encode d_2 = encode d_1 ✓. predC ✓✓. TCC_C WIN! And in the real +C game: is_valid = verify(pk_1, x_2, (sig_1?, c_?))... the forgery signature: replay sig from query 1: sigc' = (sig_1, c_2)? verify(pk_1, m'=x_2, (sig_1, c_2)): recomputes em' = encode(ThC ps ad_1 x_2 c_2) = encode d_2 = encode d_1 = em_1, chain-walk from sig_1 → pk_1 ✓, okC = predC d_2 ✓. VALID. is_fresh x_2 ≠ x_1 ✓. dist ✓. disj_wgpidxs: adversary's OC tweaks vs query addresses — the OC tweaks are emb_tw ad_1 (pkcotype), disjoint from wgpidxs (chtype) ✓. So this is a GENUINE FORGERY against the +C game and against TCC_C, at birthday cost over same-address codewords ≈ sqrt(#surface codewords at... the codeword space for the search is the surface: ~2^118) ≈ 2^59 work!!

  Hold on. This contradicts my earlier "commit both mates kills freshness" — NO: commit ONLY x_1; the mate x_2 is never queried; the collision pair was pre-computed during choose using OC (which is live and keyed!). The query side d_1 = ThC ps ad_1 x_1 (grindC ps ad_1 x_1) — the adversary must match the ORACLE's counter c_1 = grindC result, which it can compute itself via OC (deterministic grind, first predC counter in enum order). ✓. So TCC_C is achievable at ~2^59-2^64 work — i.e., TCC_C as an ASSUMPTION is quantitatively a ~59-64-bit assumption at C10 geometry, NOT 129-bit. And more: the actual WOTS+C scheme admits a forgery at that cost (in the model, given the birthday search).

  Wait, wait. But this attack is a forgery on M_EUF_GCMA_WOTSC_NPRF — meaning the WHOLE WOTS+C bound (S-TCR + WOTS terms + BadEnc-term) must account for it. Does it break S-TCR(+C)? The attack finds d_1 ≠ d_2 with same codeword but d_1 ≠ d_2 — no ThC collision, so S-TCR is not violated. The attack's cost is the birthday search over surface codewords via OC — each OC.query is one thfc evaluation; ~2^59-2^64 queries. So EUF security of WOTS+C at C10 is at most ~2^59± in this model?? Unless the deployed geometry's surface is bigger. Let me compute #surface more carefully: 43 digits, each 0..7, sum exactly 205. Mean 150.5, variance 43·5.25 = 225.75, σ ≈ 15.02. z = (205-150.5)/15.02 ≈ 3.63. Gaussian tail mass ≈ 1.4e-4 ≈ 2^-12.8. So #surface ≈ 2^129 · 2^-12.8 = 2^116.2; birthday ≈ 2^58.1. So ~2^58 work. Hmm — that's LOW for a scheme presumably targeting 128-bit. BUT is this a real attack on the DEPLOYED wallet? The deployed wallet's WOTS messages are internal nodes, adversary-influenced only through ThC-mediated roots as discussed; the adversary in the full EUF-CMA game chooses hypertree messages; R_MEUFGCMAWOTSC maps them to WOTS+C queries x_i = roots computed... In the deployed composition, can the adversary play the same-address birthday game? It would need: two values x_1 (a committed cube root at some instance ad) and a forge-side root' at the SAME instance with colliding codewords. The forge-side root' is derived from its forgery — the adversary grinds forgery attempts; per attempt probability 2^-116ish of hitting the specific codeword of d_1?? No wait — in the deployed setting the adversary can't pre-commit to "x_1 and x_2 share a codeword at ad" because x_1 is itself a hash output of the hypertree below (it can grind committed messages too...). Each candidate committed message gives x_1 = ThC-ish derived root — actually the roots ARE computed via trh/pkco from WOTS pks below... the adversary can compute them (it sees the cube). So it can grind: for many candidate committed messages, compute the induced x at instance ad and its codeword; store; ALSO for many candidate forgery messages compute induced x' at ad and codeword; look for match across the two pools — birthday between pools of size 2^58 each — total work 2^59. Hmm, so the deployed composition may admit the same ~2^59 attack?! Unless the model's per-instance address uniqueness and the NAGCMA commit structure interfere... they don't seem to.

  BUT: is this attack already excluded by OTHER terms in the bound? The attack's win is a valid forgery; the bound says Pr[win] ≤ UD + TCR + PRE + BadEnc-term + S-TCR. The BadEnc-term (TCC_C) is exactly where this attack lands. So no contradiction — the bound HONESTLY carries it: the attack says TCC_C advantage can be ~2^-58 per... no: advantage = probability of SUCCESS = 1 after 2^59 work — i.e., TCC_C is NOT a 128-bit assumption; it's a ~59-bit assumption at C10 geometry (if the surface fraction estimate holds). That's the quantitative content the user's charge carries. It's not fatal to the architecture — it's the ANSWER to "how small is the charge": not 2^-128-small, but birthday-over-surface small, ~2^58-2^64 work factor. Whether that's acceptable depends on C10's security target. THIS is arguably the strongest objection/finding: the named assumption TCC_C is not a "128-bit hardness" — it has a birthday floor at sqrt(#surface-codewords) ≈ 2^58-60 at c10 geometry, so the charged headline bound can never certify more than ~58 bits of WOTS-layer security through this term, no matter how the assumption is named. If C10's target is 128-bit, the parameter set itself (len=43, w=8, target 205) is the constraint, and no placement decision fixes that.

  Hmm, but wait — is the surface-fraction right? Let me double-check the codeword space: w^len = 8^43 = 2^129 exactly (verified lemma c10_codeword_space). Number of 43-tuples over {0..7} summing to 205. Actually, hmm, but do all codewords have preimages under the encoder? The encoder maps 2^256 digests onto... if encode is surjective-ish, fibers uniform 2^127. The birthday search: adversary computes d = ThC(...), checks predC d (2^-12.8 pass rate), computes cw = encode d, stores in table keyed by cw. Two surface digests with same cw: after collecting 2^58 surface digests, expect a collision IF the number of DISTINCT surface codewords actually reached is ~2^116. Since encode could map many surface digests to the same codeword — the collision probability is birthday over the IMAGE of the surface under encode, which is at most 2^116 distinct values. Yes — ~2^58 work. (Constants: also need both predC and distinct digests; fine.)

  BUT hmm, one more check on the attack's validity in the MODEL: does the model's grind/oracle path let the adversary learn cw_1 = encode d_1? The adversary computes d_1 candidates itself via OC (two queries per (x,c): emb_in0/emb_in1) and applies encode — encode_msgWOTS is an abstract op but the adversary is a probabilistic TURING machine in EasyCrypt — ops are deterministic functions it can compute (they're not keyed). ✓. And it must ensure the oracle's grind on x_1 returns the c_1 it used: grindC = STCRC_WC.G.grind deterministic; the adversary replicates it by enumerating counters (as R_int's O_wrap does — it does exactly this via OC). ✓.

  So the attack is model-valid. Actually hmm, wait — does it also need is_fresh at WOTS-TW level d' ≠ d? d_2 ≠ d_1 with same codeword: yes since encode is a function and... no, same codeword doesn't imply same digest; the pair is chosen with d_1 ≠ d_2 (birthday gives distinct digests trivially w.h.p.). ✓.

  So my strongest objection becomes: **the charge is not just unbounded at the WOTS-TW layer — at the +C layer it has a birthday floor of ≈ sqrt(#surface codewords), which at C10 geometry (w=8, len=43, target 205) is roughly 2^58 work, not 2^128.** The placement question is therefore almost moot quantitatively: wherever you put it, the honest bound through this term is ~2^-58-class. If C10 claims 128-bit WOTS-layer security, the term exposes a parameter-level weakness, not a proof-engineering one. (Caveat: my 2^58 is a Gaussian estimate of the sum-205 tail of 43 octal digits, not computed exactly; the exact count is a finite computation — coeff of x^205 in ((1-x^8)/(1-x))^43 — the repo could compute it; tcollres-leg/ThCWidth may already have the tools.)

  Hmm — but hold on, is this attack ACTUALLY new/noticed? The user's countermodel header says "THE DEPLOYED WALLET IS NOT AFFECTED, AND THIS IS NOT AN ATTACK... C10's WOTS layer never encodes an adversary-chosen value (compute_fors_pk takes no message argument)". My same-address birthday forgery is an attack on the WOTS+C EUF-naCMA GAME as modeled (M_EUF_GCMA_WOTSC_NPRF), where the adversary DOES choose query messages. Whether the deployed wallet gives an adversary the ability to choose WOTS+C inputs at a chosen address is the deployment question they flag. In the full SPHINCS+ composition, the FORS root signed by WOTS depends on the message digest — adversary-influenced. Their header claims the deployed usage doesn't allow free choice. Fine — but the GAME allows it, and the bound's TCC_C term is exactly the price of that gap. Also note the attack needs ~2^58 OC queries — the collection oracle query count is bounded? In the games, OC queries aren't obviously bounded... c bounds O.query count (nrqs ≤ c), not OC. So allowed.

  Actually, let me reconsider whether dist_wgpidxs really allows my attack: queries = one query (wad_1). uniq of singleton ✓ (countermodel relies on the same, :24). disj_wgpidxs adlO adlOC: OC tweaks from the grind: emb_tw ad_1 — pkcotype vs chtype — disjoint (emb_disj_concrete, verified WOTS_C_Real:153). ✓.

  So the ~2^58 birthday attack on the +C game / TCC_C stands as model-valid. This dramatically sharpens the answer to Q3: the architecture is salvageable as bookkeeping, but the NUMBER it will produce is ~2^-58-class per the geometry, and if that's below the security target, the honest conclusion is a parameter finding about C10's WOTS+C (len/w/target), not a proof finding. Hmm, though actually — hmm, wait. I should double check the security target: is 2^58 forgery work actually a break of the deployed scheme? The forged signature is for x_2 which the adversary committed... no — x_2 was never committed as a query; only x_1 was queried. The forgery message x_2 is a dgstblock node value. In the hypertree composition, for this to be a full SPHINCS+ forgery, x_2 must arise as the root' of a forged subtree path — the adversary needs the colliding pair to consist of (honest cube root at instance i) and (forgery-induced root at instance i). It can grind forgery candidates; each candidate root' is ThC-mediated; it needs encode(ThC(root')) = cw_i for the cw of the honest d_i. That's a SINGLE target codeword per instance → 2^-116 per candidate... OR it can aim at ANY of the q cube instances (q ≈ c, the WOTS instance count = total instances in hypertree ~ thousands) → q·2^-116 per try ≈ still 2^-100+. Hmm! So in the COMPOSED setting, the adversary cannot pre-build a colliding pair at a chosen address because the honest side's x-values are committed before... no wait, it CAN grind the honest side too (by choosing committed messages), building pool A of (committed msg → induced roots at various instances), and pool B of (forgery candidates → induced root' at some instance). For a cross match, need SAME instance AND same codeword. Pool A has ~c entries per committed message (each message induces a path of d instances... in NAGCMA the whole cube is built). Pool sizes: 2^58 committed candidates × path instances... The matching requires same (instance, codeword) pair — the birthday space is now (instance × codeword) = c × 2^116 — slightly bigger. Work ~sqrt(c·2^116) ≈ 2^58 + log(c)/2. So still ~2^60-ish. Feasible-ish. So the composed attack also stands at ~2^60, IF the adversary can induce chosen-ish roots on both sides. In SPHINCS+ the forgery-side root at layer j depends on the forged signature below; the adversary has wide latitude. OK, I won't claim the composed attack definitively — I'll flag it as the concrete audit question with the model-level attack verified by construction.

  Hmm, actually — WAIT. I need to double-check one thing about the model-level attack: in M_EUF_GCMA_WOTSC_NPRF, is there any bound on OC queries or on choose's work? No — adversaries are unrestricted PPT (well, EasyCrypt doesn't enforce PPT). So "attack at 2^58 work" = the ADVANTAGE of an adversary making 2^58 OC queries is Ω(1). The bound's TCC_C term for such an adversary must be ≥ that — consistent. So the honest quantitative statement: TCC_C advantage ≤ q_OC · 2^-116 per... union bound: each OC query gives a surface digest w.p. 2^-12.8; each surface digest hits an existing table entry w.p. table-size/2^116... anyway ~q_OC^2/2^116·2^-12.8... — the standard birthday bound. So TCC_C ≈ q²/2^129-ish class. Fine.

  Now — is this actually the "single strongest objection"? It's strong because it converts the entire placement debate into a number, and the number is mediocre. But is it an objection to their DECISION? Their decision: where to place the charge. My answer: keep + bridge. The strongest objection to THAT... or to the whole architecture? Deliverable (4) says "the single strongest objection you have". I'll deliver: the birthday floor — the assumption you're about to name is not a 128-bit assumption at C10 geometry; it's ~2^58–2^64 (birthday over the constant-sum surface, same-address, model-valid attack sketched and its preconditions verified at source). So (a) placement barely matters quantitatively; (b) before building the TCC_C bridge, compute the exact surface count (coefficient extraction, finite) and decide whether the headline can live with a 2^-58-class term. And the caveat: my 2^58 is an estimate — the exact number is a finite computation I did not do.

  Let me also sanity-check the claim "S-TCR doesn't imply TCC_C" — encode-collision without digest-collision: yes since encode is 2^127-to-1, codeword collisions are 2^127× easier... in fact the attack above finds them at 2^58 while ThC collisions cost 2^128. ✓.

  Also, for Q1, "what breaks if I restate it at +C": enumerate concretely:
  - The WOTS-TW theorem MUST keep the summand (countermodel proves the un-charged version false — conditionally, but at deployed geometry the condition is populated). So restating at +C means: +C game gets a BadEncC flag; prove ChargedInteractive: Pr[M_WOTSC(A)] ≤ S-TCR + [WOTS-TW terms at R_int(A)] + Pr[BadEncC(A)]... but that requires re-proving the interactive composition with the flag threaded — the existing interactive_D1/interactive_hop2 would need the disjunction carried — the mu_split happens INSIDE the WOTS-TW theorem, and at the +C layer you'd split M_WOTSC directly: Pr[win] = Pr[win∧badC] + Pr[win∧¬badC]; the ¬badC half reduces to the WOTS-TW game at R_int(A) where... the WOTS-TW game's own win would include ITS badenc possibility — you still apply the charged WOTS-TW theorem inside, and then need Pr[BadEnc(R_int(A)) | ¬badC]... you'd prove Pr[win ∧ ¬badC] ≤ WOTS-TW(R_int(A)) ≤ terms + BadEnc(R_int(A)), and then bound BadEnc(R_int(A)) by BadEncC(A) + (the ¬badC conditioning doesn't help since BadEnc(R_int(A)) implies badC). So you end up needing the SAME bridge anyway. Conclusion: restating at +C changes nothing structurally; the bridge lemma BadEnc(R_int_WOTSTW(A)) ≤ TCC_C(A) is unavoidable and sufficient. What breaks if you try: duplicated work, a forked +C game, re-proved couplings. What does NOT break: R_int_WOTSTW does not destroy the structure — verified at :1765-1797 (query stores d=ThC as the WOTS message; grind via OC replicable) and :1807-1814 (forge emits ThC ps ad_i m' c').

  Good. Also should I verify that in Game4_WOTSTWES_BadEnc the queried message stored is the raw m argument (so under R_int it's d)? O_Game34_WOTSTWES_AltX.query: admpksig <- (ad, m, insubd pk, insubd sig) — yes, m is the argument = d from O_wrap. ✓ (:3247).

  One more check for the bridge's exactness: Game4's get(i) returns the i-th query (ad_i, d_i, pk_i, sig_i); badenc is encode(d_i) = encode(m') where m' = ThC ps ad_i m' c'. TCC_C as I defined needs d_i = ThC ps ad_i x_i c_i — under R_int, d_i = join of OC queries at (emb_tw ad_i, emb_in (x_i, c_i)) = ThC pp ad_i x_i c_i — yes by construction (:1788-1790). And c_i stored? O_wrap.qs includes sigc = (sig_tw, c) — the counter is stored and returned. ✓. And P d_i (res conjunct) ⇒ predC(ThC ...) for the TCC_C win. ✓. The only wrinkle: if grind FAILED at instance i, d_i is whatever the loop last computed with found=false, c=witness... and P d_i probably false → res false → excluded. ✓ consistent.

  Now, the withholding question once more, precisely: "Note M_EUF_GCMA_WOTSC_NPRF's adversary WITHHOLDS ps from choose (141-143) — is that the lever, and is it enough?" My answer: it is NOT the lever — OC is initialized with ps BEFORE choose runs (:197-201) and choose may call OC.query (:142), so the adversary evaluates ThC under the real seed during choose; withholding ps's VALUE only prevents offline (oracle-free) computation, which matters for PRF-type secrecy arguments, not for this collision event. The levers that make TCC_C plausible-but-birthday-bounded are: per-index freshness, dist_wgpidxs (same-address requirement), and encode's fiber structure. And "enough" — nothing makes it 128-bit; the floor is birthday-over-surface.

  Let me also decide on the named assumption's concrete form for deliverable 2. House shape (game + reduction):

  ```
  module TCC_C (A : Adv_MEUFGCMA_WOTSC) = {
    module A = A(O_MEUFGCMA_WOTSC_Default, FC.O_THFC_Default)
    proc main() : bool = {
      ... ps <$ dpseed; inits; A.choose(); (i, m', sigc') <@ A.forge(ps);
      (ad, x, pk, (sig, cc)) <@ O.get(i);
      d  <- ThC ps ad x cc;
      d' <- ThC ps ad m' sigc'.`2;
      return 0 <= i < nrqs /\ nrqs <= c /\ dist /\ disj /\
             d' <> d /\ predC d /\ predC d' /\
             encode_msgWOTS d = encode_msgWOTS d';
    }
  }
  ```
  Reduction: lemma BadEnc_at_Rint_le_TCCC (A <: Adv_MEUFGCMA_WOTSC{-R_int_WOTSTW, ...}) &m :
    Pr[Game4_WOTSTWES_BadEnc(R_int_WOTSTW(A)).main() @ &m : res /\ BadEncFlag.badenc]
    <= Pr[TCC_C(A).main() @ &m : res].
  Proof: byequiv — the two games share the transcript via the existing coupling invariants (choose_tw/forge_tw); event inclusion: res ∧ badenc gives all six conjuncts (freshness from is_fresh, predC from P m ∧ P m', encode equality from the flag, the rest verbatim). That's "a game + a reduction, not a hand-wave" in the repo's idiom. Uniform in A — this is the uniformity the user thought was impossible.

  Also mention: the name could follow IncEnc.ec's T_COLL_RES (Def 11) — the repo already gestures at this: fork :1473 "That gap IS the T-COLL-RES event (Def 11)". But note IncEnc.ec's T_COLL_RES is for the IncEnc encoder — different object; don't reuse the module, reuse the NAME PATTERN (e.g. T_COLL_RES_CW / "codeword T-COLL-RES").

  Now, how confident am I in the 2^58 estimate? The count of 43-tuples over {0..7} summing to 205: total 8^43 ≈ 2^129. The distribution is Irwin–Hall-like (sum of 43 uniform{0..7}), mean 150.5, var 43·(64-1)/12 = 43·5.25 = 225.75, σ = 15.02. P(sum=205) ≈ (1/σ)φ(3.63) ≈ (1/15)(0.0008) ≈ 5.3e-5 ≈ 2^-14.2. So #surface ≈ 2^114.8, birthday ≈ 2^57.4. With lattice corrections, call it 2^57–2^60. I'll present it as ~2^57–2^60 and flag the exact computation as finite and undone.

  Hmm, one subtlety: the attack only needs ONE side on the surface? No: TCC_C win needs predC d AND predC d' — both on surface. The birthday table is over surface digests only, so both are surface by construction. ✓ cost per surface digest = 2^14.2 ThC evals... wait no: each ThC eval yields a digest; it's surface w.p. 2^-14.2; so collecting 2^57.4 surface digests costs 2^57.4+14.2 = 2^71.6 evals?? NO — birthday: number of pairs among N surface digests ≈ N²/2, collision when N²/2 ≈ #surface_codewords = 2^114.8 → N ≈ 2^57.9 surface digests → 2^57.9 · 2^14.2 = 2^72 evals. Hmm wait that's wrong: each digest is surface w.p. p = 2^-14.2; to get N surface digests costs N/p evals = 2^57.9·2^14.2 = 2^72.1. Hmm, that changes the number: ~2^72 work, not 2^58. Let me redo: alternative view — each ThC eval produces a codeword; the win needs two evals (one committed-side, one forge-side) with same codeword, BOTH predC. Per eval, probability p_s = 2^-14.2 of being on surface. Effective birthday over codeword values weighted by... Each eval lands on surface codeword cw w.p. p_s · (fiber-weighted distribution). Number of evals to collision ≈ sqrt(π/2 · 1/Σ q_cw²) where q_cw = per-eval prob of codeword cw. If uniform over surface: q = p_s/2^114.8 = 2^-129 (makes sense: 2^129 total codewords, eval uniform over... wait is the per-eval codeword uniform over ALL 2^129 codewords? d uniform 256-bit → cw = encode d; if fibers uniform 2^127, cw uniform over 2^129 codewords ✓). Then birthday over codewords directly: collision (any pair, same cw) after ~2^64.5 evals. But TCC_C needs both predC: codeword cw determines predC? predC d = (digitsum(encode d) = target) = property of cw alone! So "surface codeword" ⟺ predC. A codeword collision on a surface codeword: restrict birthday to surface codewords: effective per-eval prob of landing on surface at all is p_s; collisions on surface codewords: ~sqrt(2^114.8) surface-landing evals = 2^57.4 surface evals = 2^57.4/p_s... no wait: birthday over the 2^114.8 surface codewords needs ~2^57.4 samples FROM the surface distribution; each sample costs 1/p_s evals → 2^57.4 × 2^14.2 = 2^71.6 total evals. Alternatively direct: collision on any surface codeword after N evals: expected #colliding pairs on surface codewords ≈ N²/2 × Σ_{cw ∈ surface} q_cw² = N²/2 × 2^114.8 × 2^-258 = N² · 2^-258+114.8 = N² · 2^-143.2... hmm that gives N ≈ 2^71.6. ✓ consistent: ~2^72 work. OK so ~2^72, not 2^58. (The naive "2^58" forgot the surface-pass cost.) So the floor is ~2^72-ish — below 128 but not catastrophic. With lattice correction p_s might be 2^-12 to 2^-15; so work ≈ 2^70–2^74. So: TCC_C at C10 geometry is a ~2^70-75-class assumption. Still well below the 2^128 target.

  Let me double check p_s: P(sum of 43 U{0..7} = 205). z=3.63, φ(z) = (1/√2π)e^{-6.58} = 0.3989·0.00139 = 5.5e-4. p ≈ φ/σ = 5.5e-4/15.02 = 3.7e-5 ≈ 2^-14.7. So #surface = 2^129·2^-14.7 = 2^114.3. Work ≈ sqrt(2^114.3)·2^14.7 = 2^57.2·2^14.7 = 2^71.9. Yes ~2^72.

  Hmm, and actually is there an even better attack that skips the predC requirement on one side? TCC_C win requires predC d (queried side) — but that's automatic when grind succeeds... the queried side's digest is ALWAYS on the surface (grind). So the adversary doesn't pay p_s on the query side: d_1 = ThC(ps, ad, x_1, grind(x_1)) is guaranteed predC! So the birthday is: forge-side candidates need predC (pay p_s per candidate) AND match one of the committed surface codewords. Commit q queries (q ≤ c, the instance bound — c is the number of WOTS instances, potentially large!). Attack: commit c queries at DISTINCT addresses (all their digests on surface, codewords cw_1..cw_c). Forge-side: grind (m', c') candidates at a chosen committed address ad_i: per candidate, predC w.p. 2^-14.7, then match cw_i w.p. 2^-114.3 → 2^-129 per candidate → 2^129/c work if targeting any of c instances... wait, per candidate the adversary picks the address ad — it computes d' = ThC ps ad m' c' — it can check against cw of THAT address only (one per address). If it varies ad over committed addresses too, each (ad, m', c') candidate checks against one cw_ad. Per candidate: hit = p(predC ∧ cw match) = 2^-14.7 · 2^-114.3 = 2^-129. So 2^129 work — no birthday gain, because the query-side codewords are FIXED (the adversary can't choose which surface codeword the query lands on — it can, though! It chooses x_i, evaluates d_i via OC BEFORE... can it? During choose, it computes d(x) candidates via OC and SELECTS which x to commit! So it can commit queries whose codewords it likes. But the forge side must match one of them: with q committed surface codewords (distinct, adversary-chosen from its OC samples), forge-side per-candidate hit = q · 2^-129... where's the birthday? The birthday needs BOTH sides free. Committed side: the adversary can sample MANY x candidates during choose via OC and commit the q ≤ c best — but committing more than c queries violates nrqs ≤ c. Hmm — but the c bound: c = number of WOTS instances = bigi ... = total hypertree instances, could be ~2^20-ish. So commit c queries each with known surface codewords (cost c evals), then forge-side birthday against the SET: per candidate 2^-14.7·(c/2^114.3)... = c·2^-129. Work = 2^129/c. With c = 2^20 → 2^109. Still no birthday. The TRUE birthday requires colliding two ADVERSARY-SAMPLED values, but one side must be a committed query (capped at c and all-signed → the mate must be fresh, so it can't be a committed one... wait it CAN match a committed query's codeword — that's the whole event: forge side d' collides with committed d_i, d' ≠ d_i). So the collision pair = (committed d_i, forge d'). The committed side is capped at c samples (all get signed). If the adversary could commit unlimited queries it would do full birthday. With cap c, work ≈ 2^129/c... hmm, no wait — it can SAMPLE unlimited x candidates via OC during choose but only COMMIT c. Sampling doesn't help: it needs committed codewords; committing all c with known codewords; the forge side then needs to hit one of those c specific codewords: per-candidate c/2^129 (d' uniform over 2^129 codewords... predC required too — but if cw_ad is surface, matching it implies predC d' ✓). So per-candidate hit c/2^129, work 2^129/c. BUT the pair (d', d_i) — is that a "birthday pair"? The adversary sampled d_i values itself via OC — it could instead NOT commit and just look for codeword collisions among its OWN samples (full birthday, 2^64.5·(1/p_s...) — computed above ~2^72 for surface-both), obtaining (x_1, c_1, x_2, c_2) same address, same surface codeword — then COMMIT x_1 (the oracle's grind will reproduce c_1? the oracle grinds the FIRST predC counter in enum order — the adversary's c_1 must equal grindC ps ad x_1; it computes that itself by enumeration ✓) and FORGE x_2. One committed query! c-cap irrelevant. So full birthday applies: ~2^72 work total (2^64.5·2^14.7/√... anyway ~2^72). Right — the query side d_1 is adversary-sampled before commitment (via OC during choose), so both sides are free: genuine birthday. ~2^72. OK so final: ~2^72-class floor at C10 geometry. (Dominated by p_s = P[digit sum = 205] ≈ 2^-15 and #surface ≈ 2^114.)

  So: the strongest objection — the TCC_C charge has a birthday floor ≈ sqrt(#surface-codewords)/p_s... ≈ 2^72 at C10 geometry (estimate; exact count finite and undone), via a model-valid same-address codeword-collision replay forgery. Below any 128-bit target; so the placement debate is secondary to a parameter/quantity question. And crucially the naive intuition "encode is 2^127-to-one so the charge is about 2^127/2^256 = 2^-129 events" is wrong by birthday + surface-concentration effects. Also the withheld-seed is not the lever (OC live during choose enables the pre-computation).

  I should sanity-check the claim that the adversary can replicate the oracle's grind: R_int_WOTSTW.O_wrap.query does exactly this via OC (:1778-1787) — it enumerates counters and checks predC via OC queries, deterministic first-found = grind (per grindP/grindC_E lemmas). The adversary has the same OC access. ✓ verified.

  Also verify the forge-side signature replay works in the +C game for the attack: sig' = sig_1 (from query 1), counter c' = c_2 (the adversary's mate counter). verify(pk_1, x_2, (sig_1, c_2)): em = encode_msgWOTS_C ps ad_1 x_2 c_2 = encode d_2 = encode d_1; chain-walk from sig_1 gives pk_1 (since codewords match — the pkfs transfer lemma, proven in the countermodel at the WOTS-TW level; the +C verify uses the same walk, WOTS_C_Scheme.ec:93-99); okC = predC(ThC ps ad_1 x_2 c_2) = predC d_2 ✓. is_fresh x_2 ≠ x_1 ✓. VALID FORGERY. ✓ So the attack works at the +C GAME level directly — meaning the +C game's EUF advantage itself has a ~2^72 adversary, and any honest bound MUST have a term covering it (the TCC_C term). This CONFIRMS the architecture: the charge is not optional anywhere; it also says the charge's true size is birthday-class.

  Hmm — although, wait: I should double check that the attack also needs d_1 = d at the queried instance to be computed with the oracle's OWN grind — when the adversary commits x_1, the oracle O_MEUFGCMA_WOTSC_Default.query calls WOTS_C_ES.sign which grinds c = grindC ps ad x_1 (first predC counter) — the adversary pre-computed exactly this c_1, so d_1 = ThC ps ad x_1 c_1 as predicted ✓, and the stored counter in the signature is c_1. The forged counter c_2 might equal c_1? If c_2 = c_1 then d_2 = ThC(x_2, c_1) ≠ d_1 = ThC(x_1, c_1) still fine (different messages). ✓ no issue.

  Alright. Also for Q4, another framing error worth listing: "the term has to be bounded one layer UP, at +C, where the WOTS message is ThC ps ad x c and the adversary cannot choose it freely" — partially wrong: the +C adversary DOES choose the query messages x freely (that's the whole M_EUF_GCMA_WOTSC interface); what it can't do is choose the DIGEST d directly. The leverage is ThC-mediation, not message-withholding. And even that only gives birthday-class, not exponential-small, security.

  Do I have enough verification? Summary of VERIFIED vs not:
  - Countermodel file: read in full; proofs as claimed; I did NOT compile (read-only, container-only). Their .eco receipts exist with fresh timestamps; cm.out exists. I can peek at cm.out to confirm RC. Let me check cm.out and closure.out quickly.
  - nhchwcoll_hchwpre_msg :1505-1520 read; complete case split ✓.
  - Charged theorem :6764-6771 read ✓; Step split :6482-6494 ✓; badenc-branch discharge :6693-6696 ✓.
  - Game4_WOTSTWES_BadEnc :3262-3308 read ✓ (flag, P m ∧ P m' conjuncts, dist/disj).
  - Alt_BadEnc_eq / EqPr_BadEnc :6745-6762 ✓.
  - R_int_WOTSTW :1753-1815 read ✓.
  - WOTS_C_Scheme :141-144 (choose {O.query, OC.query}, forge(ps)), :197-216 (init order, per-index freshness, dist) ✓.
  - WOTS_C_Real :214-216 ThC = join of two thfc; :279 predC = P ✓.
  - Geometry: c10_n=16, c10_w=8, c10_len=43, target 205, w^len = 2^129, msgWOTS = mdgstblock = 8·n_m = 2·8n = 256 bits ✓ (fork :46, :191-197, :270; geometry :66-70, :168).
  - XmssmtCC_All application :8904-8912 ✓.
  - GprocQWiredWotsCharged full read ✓.
  - R_MEUFGCMAWOTSC_EUFNAGCMA_C choose reads A(OC).choose then queries per-leaf ✓ (:481-535).
  - NOT verified: exact surface count (estimate), compile receipts (I'll glance at cm.out).

  Let me glance at cm.out and closure.out, then write the final answer. Also double-check the EUF-NAGCMA game's choose-time ps visibility for A_ht: Adv_EUFNAGCMA — MM45's NAGCMA adversary choose() gets no key (non-adaptive = commits message list before keys). The reduction R_MEUFGCMAWOTSC_EUFNAGCMA_C.choose passes OC to A_ht — verified. In the composed WOTS-TW game, when is ps available to R's choose? Game4: A.choose() before A.forge(ps) — choose gets no ps ✓ but OC live ✓.

  One more thing — the user asked specifically: "does WOTS_C_Interactive.ec:1753's R_int_WOTSTW let a +C-level event be pushed down, or does the reduction destroy the structure needed?" My answer: preserves — with the one caveat that the +C game and the WOTS-TW game sample ps identically and R_int's O_wrap stores the digests via the SAME qs the flag inspects... the flag is in Game4 which reads its own O_MEUFGCMA_WOTSTWESNPRF.qs (WOTS-TW oracle), storing d. So the push-down of the +C event "encode(ThC(x,c)) = encode(ThC(m',c'))" lands exactly on Game4's badenc when A := R_int_WOTSTW(B). That's the bridge lemma, uniform in B. ✓.

  Quick look at cm.out for receipt verification.

• Let me verify the compile receipts exist as claimed, and double-check the exact `P`-gate and game conjuncts one last time.[-] [0048] 13.7% (-1.0B / [frag -1.0B])[\] [0049] 13.9% (-1.0B / [frag -1.0B])[|] [0050] 13.9% (-1.0B / [frag -1.0B])[/] [0054] 14.8% (-1.0B / [frag -1.0B])[-] [0055] 14.9% (-1.0B / [frag -1.0B])[\] [0056] 14.9% (-1.0B / [frag -1.0B])[|] [0065] 17.0% (-1.0B / [frag -1.0B])[/] [0066] 17.0% (-1.0B / [frag -1.0B])[-] [0067] 17.1% (-1.0B / [frag -1.0B])[\] [0068] 17.3% (-1.0B / [frag -1.0B])[|] [0069] 17.4% (-1.0B / [frag -1.0B])[/] [0070] 17.4% (-1.0B / [frag -1.0B])[-] [0076] 18.1% (-1.0B / [frag -1.0B])[\] [0077] 18.2% (-1.0B / [frag -1.0B])[|] [0078] 18.2% (-1.0B / [frag -1.0B])[/] [0079] 18.4% (-1.0B / [frag -1.0B])[-] [0080] 18.4% (-1.0B / [frag -1.0B])[\] [0099] 21.6% (-1.0B / [frag -1.0B])[|] [0187] 48.2% (-1.0B / [frag -1.0B])[/] [0190] 48.8% (-1.0B / [frag -1.0B])[-] [0190] 48.9% (-1.0B / [frag -1.0B])[\] [0190] 49.1% (-1.0B / [frag -1.0B])[|] [0191] 49.1% (-1.0B / [frag -1.0B])[/] [0197] 50.8% (-1.0B / [frag -1.0B])[-] [0198] 50.8% (-1.0B / [frag -1.0B])[\] [0199] 50.8% (-1.0B / [frag -1.0B])[|] [0206] 53.0% (-1.0B / [frag -1.0B])[/] [0207] 53.2% (-1.0B / [frag -1.0B])[-] [0208] 53.4% (-1.0B / [frag -1.0B])[\] [0209] 53.7% (-1.0B / [frag -1.0B])[|] [0210] 54.0% (-1.0B / [frag -1.0B])[/] [0216] 55.9% (-1.0B / [frag -1.0B])[-] [0217] 56.1% (-1.0B / [frag -1.0B])[\] [0224] 58.5% (-1.0B / [frag -1.0B])[|] [0225] 58.6% (-1.0B / [frag -1.0B])[/] [0236] 61.6% (-1.0B / [frag -1.0B])[-] [0237] 61.6% (-1.0B / [frag -1.0B])[\] [0238] 61.6% (-1.0B / [frag -1.0B])[|] [0240] 62.3% (-1.0B / [frag -1.0B])[/] [0241] 62.4% (-1.0B / [frag -1.0B])[-] [0242] 62.7% (-1.0B / [frag -1.0B])[\] [0243] 62.8% (-1.0B / [frag -1.0B])[|] [0248] 64.2% (-1.0B / [frag -1.0B])[/] [0257] 67.0% (-1.0B / [frag -1.0B])[-] [0258] 67.1% (-1.0B / [frag -1.0B])[\] [0259] 67.4% (-1.0B / [frag -1.0B])[|] [0260] 67.6% (-1.0B / [frag -1.0B])[/] [0261] 67.7% (-1.0B / [frag -1.0B])[-] [0262] 68.0% (-1.0B / [frag -1.0B])[\] [0263] 68.1% (-1.0B / [frag -1.0B])[|] [0264] 68.3% (-1.0B / [frag -1.0B])[/] [0265] 68.4% (-1.0B / [frag -1.0B])[-] [0266] 68.6% (-1.0B / [frag -1.0B])[\] [0267] 68.7% (-1.0B / [frag -1.0B])[|] [0268] 68.8% (-1.0B / [frag -1.0B])[/] [0269] 68.9% (-1.0B / [frag -1.0B])[-] [0279] 71.0% (-1.0B / [frag -1.0B])[\] [0280] 71.0% (-1.0B / [frag -1.0B])[|] [0281] 71.1% (-1.0B / [frag -1.0B])[/] [0282] 71.3% (-1.0B / [frag -1.0B])[-] [0283] 71.5% (-1.0B / [frag -1.0B])[\] [0284] 71.7% (-1.0B / [frag -1.0B])[|] [0285] 71.9% (-1.0B / [frag -1.0B])[/] [0286] 71.9% (-1.0B / [frag -1.0B])[-] [0298] 75.2% (-1.0B / [frag -1.0B])[\] [0299] 75.3% (-1.0B / [frag -1.0B])[|] [0300] 75.5% (-1.0B / [frag -1.0B])[/] [0301] 75.7% (-1.0B / [frag -1.0B])[-] [0302] 75.7% (-1.0B / [frag -1.0B])[\] [0306] 76.4% (-1.0B / [frag -1.0B])[|] [0306] 76.4% (-1.0B / [frag -1.0B])[/] [0306] 76.5% (-1.0B / [frag -1.0B])[-] [0307] 76.5% (-1.0B / [frag -1.0B])[\] [0312] 77.9% (-1.0B / [frag -1.0B])[|] [0313] 78.0% (-1.0B / [frag -1.0B])[/] [0314] 78.0% (-1.0B / [frag -1.0B])[-] [0315] 78.1% (-1.0B / [frag -1.0B])[\] [0316] 78.4% (-1.0B / [frag -1.0B])[|] [0317] 78.4% (-1.0B / [frag -1.0B])[/] [0318] 78.5% (-1.0B / [frag -1.0B])[-] [0320] 78.7% (-1.0B / [frag -1.0B])[\] [0320] 78.7% (-1.0B / [frag -1.0B])[|] [0320] 78.9% (-1.0B / [frag -1.0B])[/] [0321] 79.0% (-1.0B / [frag -1.0B])[-] [0323] 79.3% (-1.0B / [frag -1.0B])[\] [0324] 79.3% (-1.0B / [frag -1.0B])[|] [0325] 79.3% (-1.0B / [frag -1.0B])[/] [0326] 79.4% (-1.0B / [frag -1.0B])[-] [0327] 79.5% (-1.0B / [frag -1.0B])[\] [0328] 79.7% (-1.0B / [frag -1.0B])[|] [0329] 79.7% (-1.0B / [frag -1.0B])[/] [0330] 79.9% (-1.0B / [frag -1.0B])[-] [0331] 80.2% (-1.0B / [frag -1.0B])[\] [0332] 80.3% (-1.0B / [frag -1.0B])[|] [0333] 80.3% (-1.0B / [frag -1.0B])[/] [0335] 80.8% (-1.0B / [frag -1.0B])[-] [0335] 80.8% (-1.0B / [frag -1.0B])[\] [0335] 81.0% (-1.0B / [frag -1.0B])[|] [0336] 81.0% (-1.0B / [frag -1.0B])[/] [0338] 81.5% (-1.0B / [frag -1.0B])[-] [0338] 81.5% (-1.0B / [frag -1.0B])[\] [0338] 81.6% (-1.0B / [frag -1.0B])[|] [0339] 81.6% (-1.0B / [frag -1.0B])[/] [0342] 82.2% (-1.0B / [frag -1.0B])[-] [0342] 82.3% (-1.0B / [frag -1.0B])[\] [0342] 82.4% (-1.0B / [frag -1.0B])[|] [0343] 82.4% (-1.0B / [frag -1.0B])[/] [0344] 82.7% (-1.0B / [frag -1.0B])[-] [0344] 82.7% (-1.0B / [frag -1.0B])[\] [0344] 82.8% (-1.0B / [frag -1.0B])[|] [0345] 82.8% (-1.0B / [frag -1.0B])[/] [0346] 83.1% (-1.0B / [frag -1.0B])[-] [0346] 83.2% (-1.0B / [frag -1.0B])[\] [0346] 83.2% (-1.0B / [frag -1.0B])[|] [0347] 83.3% (-1.0B / [frag -1.0B])[/] [0348] 83.6% (-1.0B / [frag -1.0B])[-] [0348] 83.6% (-1.0B / [frag -1.0B])[\] [0348] 83.7% (-1.0B / [frag -1.0B])[|] [0349] 83.7% (-1.0B / [frag -1.0B])[/] [0350] 84.1% (-1.0B / [frag -1.0B])[-] [0350] 84.1% (-1.0B / [frag -1.0B])[\] [0350] 84.2% (-1.0B / [frag -1.0B])[|] [0351] 84.2% (-1.0B / [frag -1.0B])[/] [0352] 84.4% (-1.0B / [frag -1.0B])[-] [0352] 84.5% (-1.0B / [frag -1.0B])[\] [0352] 84.6% (-1.0B / [frag -1.0B])[|] [0353] 84.6% (-1.0B / [frag -1.0B])[/] [0354] 84.9% (-1.0B / [frag -1.0B])[-] [0354] 84.9% (-1.0B / [frag -1.0B])[\] [0354] 85.0% (-1.0B / [frag -1.0B])[|] [0355] 85.0% (-1.0B / [frag -1.0B])[/] [0357] 85.4% (-1.0B / [frag -1.0B])[-] [0358] 85.4% (-1.0B / [frag -1.0B])[\] [0359] 85.4% (-1.0B / [frag -1.0B])[|] [0360] 85.8% (-1.0B / [frag -1.0B])[/] [0361] 86.2% (-1.0B / [frag -1.0B])[-] [0362] 86.3% (-1.0B / [frag -1.0B])[\] [0363] 86.4% (-1.0B / [frag -1.0B])[|] [0364] 86.5% (-1.0B / [frag -1.0B])[/] [0371] 88.3% (-1.0B / [frag -1.0B])[-] [0372] 88.4% (-1.0B / [frag -1.0B])[\] [0373] 88.5% (-1.0B / [frag -1.0B])[|] [0374] 88.5% (-1.0B / [frag -1.0B])[/] [0380] 90.3% (-1.0B / [frag -1.0B])[-] [0381] 90.6% (-1.0B / [frag -1.0B])[\] [0388] 92.6% (-1.0B / [frag -1.0B])[|] [0389] 92.7% (-1.0B / [frag -1.0B])[/] [0394] 94.0% (-1.0B / [frag -1.0B])[-] [0395] 94.2% (-1.0B / [frag -1.0B])[\] [0400] 95.6% (-1.0B / [frag -1.0B])[|] [0401] 95.7% (-1.0B / [frag -1.0B])[/] [0402] 95.7% (-1.0B / [frag -1.0B])[-] [0403] 95.9% (-1.0B / [frag -1.0B])[\] [0406] 96.8% (-1.0B / [frag -1.0B])[|] [0407] 96.9% (-1.0B / [frag -1.0B])[/] [0409] 97.4% (-1.0B / [frag -1.0B])[-] [0410] 97.5% (-1.0B / [frag -1.0B])[\] [0418] 99.5% (-1.0B / [frag -1.0B])[|] [0419] 99.5% (-1.0B / [frag -1.0B])[/] [0420] 99.6% (-1.0B / [frag -1.0B])[-] [0421] 99.7% (-1.0B / [frag -1.0B])[\] [0422] 100.0% (-1.0B / [frag -1.0B])[|] [0423] 100.0% (-1.0B / [frag -1.0B])[/] [0423] 100.0% (-1.0B / [frag -1.0B])                                                   __RC=0
---
OK   Grind
OK   STCR_C
OK   WOTS_C_Real
OK   WOTS_C_Scheme
OK   XMSSMT_C_Scheme
OK   WOTS_C_Reduction
OK   WOTS_C_Interactive
OK   XmssmtCC_All
OK   RtopCSoundness
OK   FxChain
OK   FORS_C10
OK   FORS_C10_Multi
OK   GprocFORSC10
OK   FORS_C_TreePort
OK   GprocVI
OK   SphincsC10CapstoneWired
OK   SphincsC10Content
OK   C10DeployedGeometry
OK   GFailCharged
OK   XmssmtCCCharged
OK   SphincsC10CapstoneCharged
OK   C10DeployedInstance
OK   C10DeployedCapstone
OK   C10SpecControls
OK   GprocT1Opre
OK   GprocT2Trh
OK   GprocT3Trco
OK   GprocQBound
OK   GprocQWired
OK   DarkSide
OK   DarkSideC10
OK   GprocChargedQWired
### CLOSURE_FILES=32 FAILURES=0
__DONE
---
3
-rw-r--r-- 1 1001 root 6253 Aug 13 14:27 experiments/wots-badenc/controls/CtlA.out
-rw-r--r-- 1 1001 root 6493 Aug 13 14:27 experiments/wots-badenc/controls/CtlB.out
-rw-r--r-- 1 1001 root 6528 Aug 13 14:27 experiments/wots-badenc/controls/CtlC.out
-rw-r--r-- 1 1001 root 6776 Aug 13 14:27 experiments/wots-badenc/controls/CtlD.out


• Receipts check out: cm.out ends `__RC=0`, closure 32 files 0 failures, controls exist. (3 "admit" hits in the countermodel are prose mentions, consistent with their claim; the file has no `admit.` tactic per their grep convention — I saw none in my full read.)

  Note: closure.out shows GprocQWiredWotsCharged isn't in the list shown? The list shows GprocQWired, GprocChargedQWired... 32 files; the user's claim mentions GprocQWiredWotsCharged.ec reduces the summand — there might be a separate receipt. Not crucial.

  Now write the final answer. Structure per deliverables:

  (1) Placement verdict: KEEP the charge at the WOTS-TW layer; bound it at +C via a uniform bridge. Explain why "move to +C" doesn't eliminate the WOTS-TW term, why uniformity is free (their "only one instantiation" is wrong), what breaks if restated.

  (2) Named assumption: TCC_C game + bridge reduction; withholding is NOT the lever.

  (3) Salvageable: yes, with conditions; bad-event decomposition shape.

  (4) Strongest objection: the birthday floor — model-valid same-address codeword-collision replay attack gives TCC_C a ~2^72-class floor at C10 geometry (estimate), so the term will never certify 128-bit regardless of placement; and the withheld-ps lever is illusory because OC is live during choose. Also, combine: the "cannot choose it freely" claim is wrong — the +C adversary chooses x freely and can pre-compute the oracle's grind via OC during choose.

  Verification ledger: mark each load-bearing claim VERIFIED at source with path:line, or flagged as estimate/unverified.

  Keep it blunt and organized. Language: English. Length: substantial but tight.

• I have everything I need. All four questions turn on facts I could check at source; the receipts (`cm.out` ends `__RC=0`, `closure.out` 32 files / 0 failures, four control outputs present) are consistent with your claims. I did not re-run EasyCrypt (read-only, container-only gate) — "VERIFIED" below means I read the statement and its proof script, not that I re-checked the build.

  ---

  ## Verification ledger (load-bearing facts only)

  - `nhchwcoll_hchwpre_msg` concludes the disjunction, proved complete by case split over the proved `:1476` — **VERIFIED**, `experiments/wots-badenc/base/WOTS_TW_ES.ec:1505-1520`.
  - The charge is born in `Step_Game4_WOTSTWES_SMDTPREC` via `Pr[mu_split BadEncFlag.badenc]`, and the `!badenc` half discharges the left disjunct by contradiction (`:6693-6696`) — **VERIFIED**, `:6482-6494, 6690-6696`.
  - `Game4_WOTSTWES_BadEnc.res` includes `P m /\ P m'` and `dist_wgpidxs`/`disj_wgpidxs`; the flag is `em = em'` assigned after `A` finishes — **VERIFIED**, `:3290-3307`.
  - `msgWOTS = mdgstblock` = `8*n_m = 16*n` bits = **256 bits** at `n=16` (`:46, :191-197, :270`); codeword space `w^len = 8^43 = 2^129` (`cdrafts-split/C10DeployedGeometry.ec:66-70, :168`). So "2^127-to-one" (average fiber) is consistent with source — **VERIFIED**.
  - `R_int_WOTSTW` stores `d = ThC pp ad m c` as the WOTS-TW query message and forges `ThC ps ad_i m' c'` — **VERIFIED**, `cdrafts-split/WOTS_C_Interactive.ec:1788-1793, 1807-1814`.
  - `M_EUF_GCMA_WOTSC_NPRF`: `OC.init(ps)` runs **before** `A.choose()`, and `choose` may call `OC.query` (`{ O.query, OC.query }`) — **VERIFIED**, `cdrafts-split/WOTS_C_Scheme.ec:141-144, 197-201`. Freshness is per-index (`m' <> m` of `get(i)` only), `:204-207`.
  - `R_MEUFGCMAWOTSC_EUFNAGCMA_C.choose` hands `A_ht` the **live keyed** `OC` during choose — **VERIFIED**, `cdrafts-split/XMSSMT_C_Reduction.ec:498`.
  - `A_coll` countermodel, incl. `badenc_is_one` and the four controls — read in full; consistent. Conditional as advertised (`cm, cm'` free, collision as hypothesis).

  ---

  ## (1) Verdict on placement: **KEEP it at WOTS-TW, bound it at +C — and your dichotomy is partly false**

  The charge cannot move. `mu_split` happens inside `Step_Game4_WOTSTWES_SMDTPREC` because the disjunction is generated there: without carrying `badenc` through Game4, the PRE-extraction branch has no `hchwpre` (your own countermodel proves the charge-free theorem is refutable). So "define it at the +C layer and push it down" is not an available option — the event is *born* below. What breaks if you try anyway: you fork `M_EUF_GCMA_WOTSC_NPRF` with a flag, re-prove the whole interactive coupling (`interactive_hop2`, `choose_tw`, `forge_tw`), and at the end you still apply the charged WOTS-TW theorem inside — and still need the same bridge, since `BadEnc(R_int_WOTSTW(A))` implies your +C-level event, never the converse.

  But the crucial correction: **your premise "it can only be bounded for the specific deployed adversary" is wrong.** `R_int_WOTSTW` is a functor. The event commutes through it *uniformly in `A <: Adv_MEUFGCMA_WOTSC`*: Game4's stored message at index `i` is `d_i = ThC ps ad_i x_i c_i` and the forgery message is `ThC ps ad_i m' c'`, so `res /\ badenc` at `R_int_WOTSTW(A)` is, conjunct-for-conjunct, a +C-level event about `A`. The reduction **preserves** the structure needed (verified at the lines above); push-down is simply the wrong direction. The right move is one bridge lemma, uniform in `A`:

  ```
  Pr[Game4_WOTSTWES_BadEnc(R_int_WOTSTW(A)) : res /\ badenc]  <=  Pr[TCC_C(A) : res]
  ```

  That is "defined at +C" in every sense that matters, with zero re-proof of the WOTS-TW development.

  ## (2) The named assumption

  Name it in the repo's own idiom — the fork already calls this gap "the T-COLL-RES event (Def 11)" (`WOTS_TW_ES.ec:1473`); `IncEnc.ec` has the module-name pattern. Call it **`T_COLL_RES_CW`** (codeword target-collision of `encode ∘ ThC` on the `predC` surface). The game, over `Adv_MEUFGCMA_WOTSC` unchanged:

  ```
  ps <$ dpseed; O.init(ps); OC.init(ps); A.choose(); (i,m',sigc') <@ A.forge(ps);
  (ad, x, pk, (sig,cc)) <@ O.get(i);
  win := 0 <= i < nrqs <= c  /\  dist_addresses  /\  disj_wgpidxs
       /\ ThC ps ad m' sigc'.`2 <> ThC ps ad x cc
       /\ predC (ThC ps ad x cc)  /\  predC (ThC ps ad m' sigc'.`2)
       /\ encode_msgWOTS (ThC ps ad x cc) = encode_msgWOTS (ThC ps ad m' sigc'.`2)
  ```

  Keep every bookkeeping conjunct: each is implied by `res /\ badenc` on the Game4 side (`is_fresh` gives digest-inequality, `P m /\ P m'` gives both `predC`s, the flag gives encode-equality), so the bridge is pure event inclusion over the already-mechanized coupling invariants — and keeping them makes the assumption as *weak* as possible. This is not implied by S-TCR(+C): a codeword collision needs no `ThC` collision, and fibers are ~2^127-wide, so it is a genuinely new assumption, not a corollary.

  **Is seed-withholding the lever? No — and this matters mechanically.** `OC` is keyed with the real `ps` *before* `choose` runs, and `choose` may call `OC.query` (verified above). The adversary evaluates `ThC` under the real seed throughout choose; withholding `ps`'s value only blocks oracle-free offline computation, which is irrelevant to a collision event. If you state the assumption believing withholding is the protection, you will write a game without `OC` in choose — and that game **under-models the real adversary**, a silent soundness gap in the bridge. The actual levers are: per-index freshness, `dist_wgpidxs` (kills cross-address replay — your countermodel's singleton-query trick), and `ThC`-mediation of digests. None of them is seed secrecy.

  ## (3) Is the architecture salvageable? **Yes — what you built is the game-playing lemma, and the prior state was a false admit.**

  `Pr[G] ≤ terms + Pr[bad]` is the Bellare–Rogaway fundamental-lemma shape. Such a decomposition is *never* expected to be < 1 for all `A`; "RHS ≥ 1 for `A_coll`" is not vacuity of the theorem, it is the absence of a bad-event bound at that instantiation — a different defect, located at the quotation site, not in the theorem. Your framing error is judging a decomposition lemma by its standalone quantitative content. The architecture is salvageable **on two conditions**: (a) `MEUFGCMA_WOTSTWESNPRF_Charged` is treated as an internal decomposition, never quoted as a standalone WOTS-TW security statement for arbitrary `A`; (b) the `T_COLL_RES_CW` term eventually replaces the raw `Game4_WOTSTWES_BadEnc` term at the headline (`GprocQWiredWotsCharged` currently displays it raw — honest, but an undischarged debt, and your own countermodel proves it is 1 for some adversaries, so the headline currently proves nothing quantitative until the bridge lands).

  ## (4) The single strongest objection

  **The charge has a birthday floor at C10 geometry, and it is far below 128 bits — so the placement debate is secondary to a number you haven't computed yet.** Concretely, the following is a *model-valid* winning strategy against both `T_COLL_RES_CW` and the +C game itself, and every precondition is verified at source:

  - During `choose`, using the live keyed `OC`: enumerate counters exactly as `R_int_WOTSTW.O_wrap.query` does (`:1778-1787`) to replicate the oracle's deterministic grind; birthday-search pairs `(x_1,c_1), (x_2,c_2)` at the **same address** with both digests on the surface and `encode d_1 = encode d_2`, `d_1 ≠ d_2`. Commit only `(wad_1, x_1)`.
  - Forge `(0, x_2, (sig_1, c_2))`. Verify recomputes `encode(ThC ps ad_1 x_2 c_2) = encode d_1`, so the chain-walk from `sig_1` rebuilds `pk_1` (your own `pkfs_encode_transfer`, lifted); `okC` holds since `predC d_2`; `is_fresh` holds (`x_2 ≠ x_1`); `dist_wgpidxs` holds (singleton query). A genuine replay forgery — no `ThC` collision, so S-TCR(+C) does not see it.

  Cost: birthday over the constant-sum surface. At `w=8, len=43, target=205`, the surface fraction is the sum-205 tail of 43 octal digits — my Gaussian estimate is ≈ 2^-14.7 of the 2^129 codewords, putting the work factor at roughly **2^70–2^74** ThC evaluations (the 2^-14.7 surface-pass cost dominates the sqrt(2^114) collision term). **That number is an estimate, not verified** — but the exact count is the finite coefficient `[x^205]((1-x^8)/(1-x))^43`, one line of arithmetic the repo can do exactly.

  Consequences, bluntly: the intuition "fibers are 2^127 of 2^256, so the charge is a 2^-129-class event" is wrong by birthday-plus-surface concentration; the honest WOTS-layer term at C10 parameters is ~2^-72-class no matter where you place it or what you name it; and your header's "the adversary cannot choose it freely" is wrong in the model — it chooses `x` freely and only the *digest* is `ThC`-mediated, which is exactly what makes the birthday attack rather than the 2^-129 event the right cost model. If C10's WOTS layer is supposed to certify 128-bit security through this term, the obstruction is the parameter set (`len=43, w=8, target=205`), not the proof engineering. Compute the exact surface count before building the bridge — if it comes out at 2^-72-class, the deliverable changes from "bound the charge" to "report a parameter-level grinding-margin finding" (your `tools/forsc_grinding_margin.py` is the genre), and the bridge becomes the vehicle for carrying that number, honestly, to the headline.

To resume this session: kimi -r session_7f1f57d2-835f-4718-a7bc-b97f974d4425
