# Threat → claim map (the G3 coverage-completeness artifact)

**Snapshot date: 2026-07-02; status overrides reviewed 2026-07-15.** This is the *bottom-up, threat-indexed dual* of [`ASSURANCE_CASE.md`](ASSURANCE_CASE.md)'s top-down claim tree. The assurance case starts from what is **proven** and decomposes it; this map starts from what could **go wrong** (every threat / invariant-violation / adversarial-review attack-class) and asks *does any claim cover it?* — so an **unclaimed threat becomes a visible red row instead of a silent absence** (the G3 gap the FV adversarial-review playbook names, `docs/verification/fv-adversarial-review-playbook.md` line ~189).

> **Controlling 2026-07-15 overrides pending a generated table refresh:**
> `S-FW-PREIMAGE-EXPAND` is **PARTIAL—legacy V1 only**, not current coverage;
> G10 contributes no current rollback/unsigned-image state theorem. EasyCrypt
> contributes partial research evidence to `S-SPHINCS-BREAK` but leaves A5
> cited-TCB. Current extraction proves committed generated artifacts, not
> current-Rust freshness. Current Kani census is 148 harnesses/25 files; 140
> harnesses/19 files lie in mutation-enrolled files, leaving eight/six outside,
> and three of 31 mutation groups are full-only. See the
> [coordinator report](../../../docs/security/adversarial-review/findings/fv-full-stack-2026-07-15-coordinator.md)
> and current [`FV_SURFACE_MAP.md`](FV_SURFACE_MAP.md). Until the table is
> regenerated, these overrides take precedence over conflicting rows/counts.

It closes the coverage-completeness blind spot: per-theorem gates can only tell you a claim is *hollow*; they cannot tell you a threat has **no claim at all**, because there is no theorem to turn red. This map is that missing view.

> **Not a source of truth for what is *claimable*** — that is [`THE_CLAIM.md`](THE_CLAIM.md). This is a coverage index; every COVERED/PARTIAL row routes to the artifact that owns the proof. Where it could drift from `ASSURANCE_CASE.md` / `THE_CLAIM.md`, defer to those.

## How this was built (and its own honesty discipline)

A background workflow enumerated the threat surface from four independent readers (`docs/security/threat-model.md` as the SSOT + the 9 CLAUDE.md invariants + ship-blockers + the 9 `docs/security/adversarial-review/` family playbooks → **62 threats + 85 attack-classes**), the claim/coverage inventory (**62 claims** across Lean / Halmos-Kontrol / Kani / protocol models / silicon-E2E / host-render / gates), and the review-round inventory (**24 rounds**, see [`REVIEW_PROVENANCE.md`](REVIEW_PROVENANCE.md)). These were reconciled into **61 surfaces**.

The failure modes are **asymmetric**: a wrongly-**COVERED** row is *false assurance* (the exact thing this doc exists to kill); a wrongly-**UNCLAIMED** row is merely conservative. So **every** COVERED/PARTIAL mapping was adversarially cite-verified — an independent agent resolved the cited `file:line` / theorem / make-target and downgraded any citation that did not genuinely cover *that* threat. The verification caught **8 mis-citations** (folded in below as downgrades/re-anchors) and, on finalize, one wrongly-**UNCLAIMED** row (`S-NS-TOCTOU`, which *is* Kani-covered). When a citation could not be firmly resolved the row was left PARTIAL/UNCLAIMED, never COVERED.

## Legend

**Verdict** — ✅ COVERED: a resolved claim genuinely covers the threat · 🟡 PARTIAL: real but incomplete coverage (one layer only — e.g. model proven, no model→deployed bridge; or a positive test, not a ∀-proof) · 🔴 UNCLAIMED: in-scope threat with **no covering claim** (the rows that matter most) · ⚪ OUT-OF-SCOPE: deliberately outside the assurance stack (physical/coercion/CRQC/novel-crypto-break/silicon-lab or a named ship-blocker), listed so the boundary is explicit, not silent.

**Evidence** — `K` kernel-proven Lean ∀ · `B` bytecode-symbolic (Halmos/Kontrol) · `C` corpus/KAT/Kani-bounded · `T` cited-TCB · `silicon-E2E` on-hardware test · `host-test` render/decode host test · `gate` CI anti-vacuity/enforcement gate · `audit` manual/LLM review (see the same tiers in `ASSURANCE_CASE.md` §0).

**Coverage summary:** 61 surfaces — **4 COVERED · 30 PARTIAL · 1 UNCLAIMED · 26 OUT-OF-SCOPE**.

## Covered (4)

Threats with a resolved, cite-verified covering claim. Even here the deployed-artifact bridge is the shared A3.x/codehash-pin caveat carried by every K/B claim (see `ASSURANCE_CASE.md` §7).

| Surface | Threat (threat-model source) | Verdict | Covering claim(s) — evidence : cite | Last review |
|---|---|---|---|---|
| `S-CROSS-CHAIN-REPLAY` | Replay a slot signature across chains (Claim 5)<br><sub>threat-model.md §5 Claim 5 / §7.12 'Cross-chain'</sub> | ✅ COVERED [^1] | `K` G0-theft-free — Theorems.lean:279 — sphincsDigest binds chainId at [328..360); deployed PQSmartWallet.sol:388 binds block.chainid in the same SHA-256 preimage; collision lemma Theorems.lean:632-639<br>`K` slot-kdf-chainbind — domain/src/lib.rs slot_entropy (~705-767) mixes chain_id.to_be_bytes() into the slot KDF preimage — defense-in-depth per-chain keys | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-EIP1271-BOOTSTRAP` | EIP-1271 authorised with bootstrap key (ownerIndex==0)<br><sub>threat-model.md §7.12 'Authorise EIP-1271 with bootstrap'</sub> | ✅ COVERED | `K` G8-eip1271-K — Invariants.lean:891 (eip1271_forbids_bootstrap) — verified anchor<br>`B` G8-eip1271-B — halmos/HalmosIsValidSignature.t.sol — forbids-bootstrap is clean bytecode forall | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-FW-PREIMAGE-EXPAND` | Expand the signed FW-update preimage (auditor reconstructability)<br><sub>threat-model.md §7.8 intro (nests FW6; R1 fv_scope=out-of-scope, but current-design enforcement is real)</sub> | ✅ COVERED | `K` G10-fw-preimage — FwManifestSpec.lean:90 (preimage_layout_injective) — 75B domain-separated, injective<br>`C` kani-domain-fwmanifest — fw-manifest/src/lib.rs:1033 — FW6 compile-assert + Kani layout keep it frozen | PLAYBOOK-FWUPDATE-SECUREBOOT · source-read · 2026-07-02 |
| `S-STORAGE-COLLISION` | ERC-7201 namespaced-storage collision (counter/owner integrity)<br><sub>threat-model.md §7.12 / Inv #7 (from SOL3)</sub> | ✅ COVERED | `gate` lean-selector-parity — StorageSlotParity.t.sol (verified: file exists) + make verify-lean-proto-domain<br>`K` G6-owner-K — Wallet/MultiOwnable.lean — namespaced slot, mappings, no packed sub-word aliasing over Lean Storage model | FV-ADV-2026-07-02 · executing · 2026-07-02 |

## Partial (30)

Real but incomplete coverage — the honest majority. Each row has genuine evidence on one layer but a documented gap (model without a model→Rust/bytecode span; a positive silicon/host test rather than a ∀-proof; a build-time gate rather than a runtime proof). The footnotes carry the cite-verification detail.

| Surface | Threat (threat-model source) | Verdict | Covering claim(s) — evidence : cite | Last review |
|---|---|---|---|---|
| `S-BOOTSTRAP-ROTATE` | Master-key rotation changes CREATE2 address (Inv #6)<br><sub>threat-model.md §5 Claim 5/6 / §8.6 (nests SOL8)</sub> | 🟡 PARTIAL | `B` G6-owner-B — halmos/HalmosMultiOwnable + kontrol/KontrolOwnerTable — no rotate selector on bytecode<br>`gate` contract-codehash-freeze — PinnedCodehashes.t.sol | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-CAP-RESET` | Reset/increase the monotonic on-chain caps (Inv #7)<br><sub>threat-model.md §5 Claim 6 / §7.12</sub> | 🟡 PARTIAL [^2] | `K` no_reset_path — Wallet/Invariants.lean:385 (bumpBootstrap never decreases; preserves others; no axioms)<br>`gate` verify-storage-mutators — Makefile:48-57 (closed-world mutator allowlist forces a no-decrease lemma)<br>`B` PQBootstrapCapEvasion — test/…/PQBootstrapCapEvasion.t.sol (deployed bump is revert-proof, anti-under-count) | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-CLASSICAL-FALLBACK` | Second/classical verifier introduced as fallback (Inv #5)<br><sub>threat-model.md §5 Claim 4 / §8.5 (nests SOL5, SOL7)</sub> | 🟡 PARTIAL | `K` G6-owner-K — Invariants.lean/MultiOwnable.lean — upgrade_path_unreachable (binds MODEL only, no bytecode corollary)<br>`gate` contract-codehash-freeze — PinnedCodehashes.t.sol — a swapped verifier changes codehash | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-CLEAR-SIGN-DECODE-DRIFT` | Clear-sign decoder mis-binds calldata to displayed intent<br><sub>threat-model.md §7.6 rows 1&3 (nests CS6-CS9)</sub> | 🟡 PARTIAL [^3] | `C` kani-multisend — tx/src/multisend.rs (16 harnesses) — canonical MultiSendCallOnly acceptance<br>`C` kani-cowswap — tx/src/cowswap_order.rs — orderUid/presign binding<br>`host-test` host-render-erc7730 — erc7730_render_pure_tests.rs (59) — CS9 legacy walker.rs footgun (not re-exported); CS5 scalar-hide rests on build-time dbgen gate | FV-KANI-2026-07-01 · source-read · 2026-07-01 |
| `S-CT-BRANCH` | Secret-dependent branch / cache-timing leakage<br><sub>threat-model.md §7.10 row 3; FV_SURFACE_MAP #7 (nests FI4 by-design-leaky, FI10)</sub> | 🟡 PARTIAL | `gate` gate-enforcement — scripts/gate_enforcement.json — checkct CT/SCA gate on 5 drivers, self-flagged local/manual not per-PR<br>`audit` surface-gap-unreviewed — FV_SURFACE_MAP.md:9-30 — surface #7 CT/SCA NEVER adversarially reviewed | PLAYBOOK-SCA-FI · source-read · 2026-07-02 |
| `S-DEBUG-FLAG-SHIP` | Production build ships debug-log / e2e-test / mock-se<br><sub>threat-model.md §7.7 row 5 / §9.6 (nests UI2, UI3, SL3, SL4)</sub> | 🟡 PARTIAL | `gate` gate-enforcement — make verify-gate-enforcement — compile_error! fences in nsc/mod.rs (UI2/UI3 fixed 07-02); SL3 meta: fence wall itself NOT in verify-gate-enforcement | PLAYBOOK-SILICON-LOCKDOWN · source-read · 2026-07-02 |
| `S-DEP-BUG` | Implementation bug in an audited/unaudited code path (most-likely real failure)<br><sub>threat-model.md §10.10 (meta-residual)</sub> | 🟡 PARTIAL | `audit` surface-gap-unreviewed — FV_SURFACE_MAP.md:9-30 — 5 of 8 FV surfaces never adversarially reviewed; 07-01 FV pass source-read-only<br>`audit` scope-firmware-unproven — ASSURANCE_CASE.md:302-332 §5/D6 — Inv #2/#3/#4 + clear-sign pipeline have ZERO Lean coverage<br>`gate` gate-kani-mutation — make verify-kani-mutation — curated anti-vacuity, 57 harnesses unscreened | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-DISPLAY-VS-SIGN` | Device signs X while displaying Y (WYSIWYS break, Claim 9)<br><sub>threat-model.md §5 Claim 9 / §7.6 row 1 (nests CS1, SOL1, G5, UI6/UI7)</sub> | 🟡 PARTIAL [^4] | `K` G5-exec-K — Theorems.lean:656 (executeBatch_faithful) + SOL1 folds sha256(callData)<br>`host-test` host-render-cowswap — display_under_test/cowswap_render_pure_tests.rs — LCD==decoded, bound to canonical bytes<br>`host-test` host-render-safe — safe_display_render_pure_tests.rs | PLAYBOOK-CLEAR-SIGNING · source-read · 2026-07-02 |
| `S-EIP1271-USEROP-PATH` | EIP-1271 view path used to authorise a UserOp (forgery oracle)<br><sub>threat-model.md §7.12 'Use an EIP-1271 path' (nests OC1, OC2)</sub> | 🟡 PARTIAL [^5] | `K` G8-eip1271-K — OffchainBinding.lean — offchain_nested_disjoint_from_userop_digest; keccak/sha256 cross-separation<br>`K` G3-nonvacuity-fence — Invariants.lean:1045 (unauthorized_userop_breaks_hash) | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-FW-ROLLBACK` | Downgrade to a vulnerable older firmware<br><sub>threat-model.md §5 Claim 8 CORRECTION / §7.8 row 3 (nests FW2, FW10)</sub> | 🟡 PARTIAL | `K` G10-fw-preimage — FwManifestSpec.lean:90 — version bound in preimage; anti-rollback is OTP-floor SINGLE-LAYER (FW10: claimed OPTIGA E1E0 cross-check ABSENT in cmd_fw_commit.rs) | PLAYBOOK-FWUPDATE-SECUREBOOT · source-read · 2026-07-02 |
| `S-FW-UNSIGNED` | Flash an unsigned/malicious firmware image (Claim 8)<br><sub>threat-model.md §5 Claim 8 / §7.7 row 1 (nests FW1, FW3, FW5, FW8, FW9)</sub> | 🟡 PARTIAL | `K` G10-fw-preimage — FwManifestSpec.lean:117 — signed_preimage_authorizes_one_firmware (over Aeneas-extracted spec, D5 TCB)<br>`C` kani-domain-fwmanifest — fw-manifest/src/lib.rs:1012 — parse panic-free<br>`silicon-E2E` si-fw-update — make test-update-hw; SECURITY-REVIEW-2026-05 C-1 fixed verify-before-erase | PLAYBOOK-FWUPDATE-SECUREBOOT · source-read · 2026-07-02 |
| `S-NS-LENGTH-CONFUSION` | NS lies about a length field (over/under-read, TZ3)<br><sub>threat-model.md §7.5 row 3</sub> | 🟡 PARTIAL | `C` kani-aa — aa/src/offchain_header.rs:181,210 — 17B header incl payload_len; residual = per-handler inlined offset arithmetic | FV-KANI-2026-07-01 · source-read · 2026-07-01 |
| `S-NS-PANIC-DOS` | NS panics S code via crafted gateway input (brick DoS)<br><sub>threat-model.md §7.5 row 5; README FV §3</sub> | 🟡 PARTIAL | `C` kani-safetx — tx/src/safe_tx.rs (14 harnesses) panic/OOB-free bounded<br>`C` kani-erc7730 — pqsigner-erc7730/src/render/* (~22 harnesses)<br>`gate` gate-kani-mutation — make verify-kani-mutation — CURATED: only 6 mut/4 of 17 files; 57 harnesses unscreened | FV-KANI-2026-07-01 · source-read · 2026-07-01 |
| `S-NS-PTR-CONFUSION` | NS supplies secure-memory pointer to gateway (TZ1)<br><sub>threat-model.md §7.5 row 1</sub> | 🟡 PARTIAL | `C` kani-nsptr — shared/src/ns_ptr_validate.rs:279.. (8 kani::proof) — bounded, decision-level | FV-KANI-2026-07-01 · source-read · 2026-07-01 |
| `S-NS-SECRET-LEAK` | Secret material reaches non-secure world (Inv #4)<br><sub>threat-model.md §7.5 / §8.4 (nests TZ5, TZ8)</sub> | 🟡 PARTIAL | `silicon-E2E` si-gtzc-enforcement — make gtzc-enforcement-hw — 7/7 secure peripherals RAZ; no per-handler egress proof | PLAYBOOK-TRUSTZONE-GATEWAY · source-read · 2026-07-02 |
| `S-NS-TOCTOU` | NS races to mutate buffer after validation (TZ2)<br><sub>threat-model.md §7.5 row 2</sub> | 🟡 PARTIAL [^6] | `C` kani-nsptr — shared/src/ns_ptr_validate.rs (8 kani::proof: NsPtr<T> range/alias/disjointness)<br>`audit` toctou-snapshot — secure/src/nsc/ns_ptr.rs:153-166 (copy NS buf to S-stack via byte-wise read_volatile before parse) | PLAYBOOK-TRUSTZONE-GATEWAY · source-read · 2026-07-02 |
| `S-OFFCHAIN-OVERSPEND` | EIP-1271 off-chain counter rollback / over-spend (Inv #9)<br><sub>threat-model.md §7.12 / §8.9 (nests OC3-OC6, OC9)</sub> | 🟡 PARTIAL | `K` G4-reachable-cap — Invariants.lean:855 — on-chain monotonic combined cap<br>`C` contract-realsig-fuzz — PQMultiOpBundle.t.sol — SOL2 reentrancy MITIGATED but UNTESTED; firmware gap/refuse is design-only | PLAYBOOK-OFFCHAIN-SIGNING · source-read · 2026-07-02 |
| `S-PAGE123-TEAR` | Page-123 compaction crash-atomicity tear<br><sub>threat-model.md §8 'F3 residual' (nests OC3)</sub> | 🟡 PARTIAL | `K` G4-reachable-cap — Invariants.lean:855 — on-chain monotonicity backstops the bounded rollback; firmware compaction atomicity itself unverified | PLAYBOOK-OFFCHAIN-SIGNING · source-read · 2026-07-02 |
| `S-PIN-BRUTE` | Online PIN brute past 10 (three-way per-attempt consumption; directional page124/E120 boot check, Claim 3)<br><sub>threat-model.md §5 Claim 3 / §7.4 (nests SE3, SE4)</sub> | 🟡 PARTIAL | `silicon-E2E` si-pin-gate-3way — make pin-gate-wipe-e2e (10-wrong→wipe both SE)<br>`silicon-E2E` si-optiga-hw-counter — make optiga-hw-counter-e2e; cold-reboot reconcile receipt remains open | PLAYBOOK-SECURE-ELEMENT · source-read · 2026-07-02 |
| `S-PIN-COMPARE-SW` | Software PIN compare instead of SE silicon (Inv #2)<br><sub>threat-model.md §7.4 / §6.2</sub> | 🟡 PARTIAL [^7] | `C` proto-pin-lockstep — tamarin/pin_lockstep.spthy:119 — abstract<br>`silicon-E2E` si-pin-gate-3way — make pin-gate-hw-counter-e2e | PLAYBOOK-SECURE-ELEMENT · source-read · 2026-07-02 |
| `S-SE-ADMIN-READ` | SE admin channel used to READ (not just delete) the half (Claim 2)<br><sub>threat-model.md §5 Claim 2 / §7.1 row 4 (nests SE6)</sub> | 🟡 PARTIAL | `silicon-E2E` si-se050-stress — make se050-stress — S-5/S-6 + admin-extract-attempt-e2e exercise the AC-split (admin has DELETE, not READ) | PLAYBOOK-SECURE-ELEMENT · source-read · 2026-07-02 |
| `S-SE-TUNNEL-SNOOP` | I2C bus snoop / MITM of SE channels (Inv #3)<br><sub>threat-model.md §7.2 rows 1-2 / §8.3 (nests SE1, SE5)</sub> | 🟡 PARTIAL | `C` proto-scp03-shield — tamarin/scp03_replay.spthy:40 + proverif optiga_shield_handshake.pv — abstract model<br>`silicon-E2E` si-saes-selftest — make saes-self-test-hw — DHUK KDF root only, not the tunnel itself | FV-PROTOCOL-QUERYBODY-2026-07-02 · executing · 2026-07-02 |
| `S-SEED-SPLIT` | Single-chip break reveals seed bits (dual-SE XOR split, Inv #1)<br><sub>threat-model.md §5 Claim 1 / §6.1 (nests SE2, SE9)</sub> | 🟡 PARTIAL | `K` G9-split-secrecy — SplitSecrecy.lean:121 (reconstruct) — IT 2-of-2, but needs HW TRNG uniform/indep; SE050-half statistical<br>`C` proto-dual-se-secrecy — proverif/dual_se_unlock.pv + tamarin/seed_split_xor.spthy:61 — abstract model, not deployed firmware | FV-PROTOCOL-QUERYBODY-2026-07-02 · executing · 2026-07-02 |
| `S-SLOT-STATE-CORRUPT` | Per-signature flash slot-state corruption (Inv #8 stateless slot)<br><sub>threat-model.md §8.8 (nests UC6)</sub> | 🟡 PARTIAL [^8] | `audit` stateless-invariant — threat-model.md §8 Inv #8 — only per-sig flash state is the page-123 counter; SLOT_CACHE lives in SRAM only (nsc/state.rs:319)<br>`audit` f3-compaction — hw/flash.rs::compact_page:1557-1605 — replays USEROP_SIGS first; documented residual: single-page erase-then-replay, not two-phase | PLAYBOOK-USB-COMPANION · source-read · 2026-07-02 |
| `S-SOL6-CROSS-SLOT-REMOVE` | Any slot key can removeOwnerAtIndex another non-bootstrap slot<br><sub>threat-model.md §8 'SOL6 design note' (availability, NOT theft; by-design, tighten-decision open)</sub> | 🟡 PARTIAL | `K` G6-owner-K — Invariants.lean I-4 cannot_remove_bootstrap — bounds blast radius (index 0 unremovable + re-addable; removed slot can't move funds). theft_free deliberately silent (not theft) | PLAYBOOK-ONCHAIN-CONTRACTS · source-read · 2026-07-02 |
| `S-SPOOF-TOKEN-META` | Spoofed ERC-20 token metadata / malicious ERC-7730 descriptor<br><sub>threat-model.md §7.6 rows 2&3 (nests CS3, CS4, CS10)</sub> | 🟡 PARTIAL | `host-test` host-render-erc7730 — erc7730_render_pure_tests.rs — pinned ERC7730_DESCRIPTORS_ROOT; DB decimals correctness is data-QA (WYSIWYS-ERC20-DECIMALS ea47a114 found+fixed real magnitude-hide) | PLAYBOOK-CLEAR-SIGNING · source-read · 2026-07-02 |
| `S-SRAM-NS-READ` | Read Secure SRAM / DMA into S-SRAM from NS (TZSC)<br><sub>threat-model.md §7.3 rows 2-3 / §9.3 (nests TZ6, T-DMA-S-SRAM)</sub> | 🟡 PARTIAL | `silicon-E2E` si-gtzc-enforcement — make gtzc-enforcement-hw — 7/7 secure peripherals RAZ-fault; residual TZSC_SECCFGR4/GTZC2/TAMP + full DMA restore pending | PLAYBOOK-TRUSTZONE-GATEWAY · source-read · 2026-07-02 |
| `S-USB-OVERFLOW` | Buffer overflow in APDU reassembly<br><sub>threat-model.md §7.13 'Buffer overflow' (UC1; fuzz coverage tracked gap)</sub> | 🟡 PARTIAL [^9] | `C` kani-usb-reassembly — shared/src/apdu_framing.rs kani_harnesses (process_frame continuation + first-frame/scrub, ChainState cursor, parse_apdu_header) — ∀ panic/OOB-free over arbitrary assembler state + symbolic frame; mutation-gated (kani_mutations.json: usb_apdu_reassembly_bounds) | FV-KANI-USB-2026-07-02 · mutation-verified · 2026-07-02 |
| `S-USEROP-REPLAY` | Replay a Type 2 signature across two UserOps<br><sub>threat-model.md §7.12 'Re-use a Type 2 sig'</sub> | 🟡 PARTIAL [^10] | `K` G0-theft-free — Theorems.lean:279 — digest binds nonce/sender/entryPoint/callData (covers the DISTINCT-nonce replay case)<br>`T` A2-entrypoint — same-nonce reuse blocked by EntryPoint v0.6 NonceManager — cited-TCB; entrypoint_no_replay axiom REMOVED 2026-06-14, deployed discharge NOT done (defeater D2) | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-VERIFIER-FORGE` | Forge a C10 sig the on-chain verifier accepts (Claim 4 / Inv #5)<br><sub>threat-model.md §5 Claim 4 / §7.12 row 1</sub> | 🟡 PARTIAL | `K` G0-theft-free — Theorems.lean:279 — kernel forall (verified anchor)<br>`C` G2-verifier-corpus — SPHINCsC10AsmMutantCorpus.t.sol — A3.1 CORPUS-discharged, forall-symbolic OPEN<br>`T` G3-eufcma-reduction — EUFCMA.lean:157 — hardness is CITED opaque axiom<br>`K` G3-nonvacuity-fence — EUFCMA.lean:195 + Invariants.lean:1045<br>`K` G3-quantitative — Quantitative.lean:257 — 96-bit floor at cap<br>`B` G2-verifier-inputgate-B — halmos/HalmosVerifier.t.sol | FV-ADV-2026-07-02 · executing · 2026-07-02 |

## Unclaimed — in-scope, no covering claim (1)

**The rows this artifact exists to surface.** In-scope threats the assurance stack does not cover today. Mitigations may exist (e.g. consumption-mask / F-16 shuffle for SCA) but there is no *claim* — so they are not COVERED. These are the honest re-review / next-work targets.

| Surface | Threat (threat-model source) | Verdict | Covering claim(s) — evidence : cite | Last review |
|---|---|---|---|---|
| `S-SCA-PRF-LEAK` | Horizontal DPA/EM leakage of SPHINCS+C10 PRF(SK.seed)<br><sub>threat-model.md §7.10 rows 1-2 / §7.3 row 5 (statistical mitigation only; HASH periph zero DPA resistance)</sub> | 🔴 UNCLAIMED | — *(no covering claim)* | PLAYBOOK-SCA-FI · source-read · 2026-07-02 |

## Out of scope — deliberate boundary (26)

Threats explicitly outside the assurance stack: physical/lab attacks (chip decap, cold-boot, DHUK/OTP extraction), coercion, novel SHA-256/SPHINCS+ cryptanalysis, CRQC harvest-now-decrypt-later, MEV, and the named OPTIGA/SE050 **ship-blockers** (S-1/S-2/S-3/S-7d — tracked in `docs/production-todo.md` + `docs/STATUS.md`, not here). Listed so the boundary is explicit rather than a silent gap. See README 'explicitly do not defend against' + `threat-model.md`.

| Surface | Threat (threat-model source) | Verdict | Covering claim(s) — evidence : cite | Last review |
|---|---|---|---|---|
| `S-CHIP-SWAP` | Mid-transit or post-ownership chip swap (SE/MCU)<br><sub>threat-model.md §7.11 / §9.4 (boot-time attestation not landed)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-CLONE-HSM` | Clone device with stolen factory/vendor HSM key<br><sub>threat-model.md §7.11 'Clone' / §10.3 (transparency log not landed)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-COERCION` | Coerced unlock (rubber-hose / shoulder-surf)<br><sub>threat-model.md §10.1 / §7.16 (architecturally out of scope)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-COLD-BOOT-SRAM` | Cold-boot attack on S-SRAM within active window<br><sub>threat-model.md §7.3 row 1 (120s window residual)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | ZEROIZE-AUDIT-2026-07 · executing · 2026-07-01 |
| `S-DECAP-DUAL` | Simultaneous decap of both secure elements<br><sub>threat-model.md §10.2 / §7.15 (explicitly out of scope)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-DHUK-OTP-EXTRACT` | Decap STM32 to extract DHUK / OTP master<br><sub>threat-model.md §7.15 row 1 / §5 Claim 2 (nests SL1, SL6)</sub> | ⚪ OUT-OF-SCOPE | `K` G9-split-secrecy — SplitSecrecy.lean:121 — split + on-device PIN gate still applies even if DHUK extracted (bricks possible, funds not extractable alone) | PLAYBOOK-SILICON-LOCKDOWN · source-read · 2026-07-02 |
| `S-FI-CRYPTO-PERIPH-TAMP` | Crypto-peripheral fault + TAMP response is log-only<br><sub>threat-model.md §7.9 row 4 / §9.5 (nests FI9, T-TAMP-LOG-ONLY)</sub> | ⚪ OUT-OF-SCOPE | `silicon-E2E` si-tzic-wipe — make tzic-wipe-hw — wipe path exists under tamp-wipe; TAMP is log-only on this branch, ITAMP9 planned | PLAYBOOK-SCA-FI · source-read · 2026-07-02 |
| `S-FI-SIG-GRAFT` | SLH-DSA single-fault grafting on the signer<br><sub>threat-model.md §7.9 row 2 / §6.3 (nests FI1, FI2, FI3, FI6; Genêt TCHES 2023)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-SCA-FI · source-read · 2026-07-02 |
| `S-MEASURED-BOOT-BYPASS` | Active slot lies about its firmware fingerprint<br><sub>threat-model.md §14; CLAUDE.md Lifecycle (nests FW4; human-in-loop)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-FWUPDATE-SECUREBOOT · source-read · 2026-07-02 |
| `S-MEV` | MEV / front-running / sandwich on a signed transaction<br><sub>threat-model.md §7.12 'MEV' / §10.7 (not a wallet-fund-extraction class)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-MPU-PRIV-ESCALATION` | Any S-world bug can call secret_keys::derive_into directly<br><sub>threat-model.md §3 boundary B-TZ-S / §9.9 (MPU privilege banking absent today)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-OPTIGA-F1D0-CHANGE` | OPTIGA F1D0 Change=ALW → desoldered PIN brute-force (ship-blocker S-1)<br><sub>threat-model.md §5 Claim 3 PROVISIONAL; CLAUDE.md Ship blockers S-1 (deferred-by-design)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-SILICON-LOCKDOWN · source-read · 2026-07-02 |
| `S-OPTIGA-NO-LOCKOUT` | No silicon-enforced OPTIGA lockout without hw-counter (ship-blocker S-3)<br><sub>CLAUDE.md Ship blockers S-3</sub> | ⚪ OUT-OF-SCOPE | `silicon-E2E` si-optiga-hw-counter — make optiga-hw-counter-e2e — counter works when enabled; production requires `optiga-hw-counter`, so E120 is lockout authority and F1E1 is only the provisioning/reset sentinel; its final lifecycle remains factory/silicon-gated | PLAYBOOK-SILICON-LOCKDOWN · source-read · 2026-07-02 |
| `S-OPTIGA-TA-BYPASS` | OPTIGA SetObjectProtected bypass via public sample cert (ship-blocker S-2)<br><sub>CLAUDE.md Ship blockers S-2 (deferred-by-design)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-SILICON-LOCKDOWN · source-read · 2026-07-02 |
| `S-PIN-GLITCH` | FI glitch to skip PIN-check / counter bump<br><sub>threat-model.md §7.4 / §7.9 (R1 fv_scope=out-of-scope)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-SCA-FI · source-read · 2026-07-02 |
| `S-PREBOOT-INTERCEPT` | Pre-first-boot intercept flashes key-exfiltrating stub<br><sub>threat-model.md §7.11 'Pre-first-boot' / §10.4 (procedural)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-PRIVACY-DEANON` | Cross-chain CREATE2 address correlation (deanonymisation)<br><sub>threat-model.md §10.8 / §2 asset S7 (per-tx rotation would break Claim 6)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-QUANTUM-HNDL` | CRQC harvest-now-decrypt-later of SE channel traffic (Claim 7 caveat)<br><sub>threat-model.md §5 Claim 7 / §7.14 row 2 / §9.1 (ML-KEM inner wrap unimplemented)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | *unreviewed* |
| `S-SE050-LOCKOUT-UNVERIFIED` | SE050 real lockout VERIFY SW unverified on silicon (S-7d)<br><sub>CLAUDE.md Ship blockers S-7d-silicon (validation gap)</sub> | ⚪ OUT-OF-SCOPE | `silicon-E2E` si-se050-stress — make se050-stress — S-5/S-6 verifiers landed; S-7d drive-to-lockout capture still open | PLAYBOOK-SECURE-ELEMENT · source-read · 2026-07-02 |
| `S-SHA256-BREAK` | Novel cryptanalytic break of SHA-256<br><sub>threat-model.md §10.5 (named FV axiom, not proven)</sub> | ⚪ OUT-OF-SCOPE | `T` A1-precompile — Bridge/Refinement.lean — SHA-256 is a CITED axiom (collision/preimage resistance opaque, never proven) | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-SPHINCS-BREAK` | Novel cryptanalytic break of SPHINCS+C10<br><sub>threat-model.md §10.6 (EUF-CMA opaque axiom)</sub> | ⚪ OUT-OF-SCOPE | `K` G3-quantitative — Quantitative.lean:257 — kernel-checked 96-bit generic-attack floor at cap over CITED upstream bit-count terms<br>`T` G3-eufcma-reduction — EUFCMA.lean:157 — hardness is the cited axiom | FV-ADV-2026-07-02 · executing · 2026-07-02 |
| `S-UI-SPOOF-NS` | NS spoofs button press or LCD content (buttons S-owned)<br><sub>threat-model.md §7.6 rows 4-5 (nests UI4, UI5, UI8; R1 fv_scope=out-of-scope)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-TRUSTED-UI · source-read · 2026-07-02 |
| `S-USB-DOS` | USB OUT flood / battery-drain DoS<br><sub>threat-model.md §7.5 row 4 / §7.13 'Stuck-NS' (DoS-only, not fund-extracting)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-USB-COMPANION · source-read · 2026-07-02 |
| `S-USB-FI-MINCLAMP` | EMFI on USB length min()-clamp<br><sub>threat-model.md §7.13 'EMFI on min()' (R1 fv_scope=out-of-scope)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-USB-COMPANION · source-read · 2026-07-02 |
| `S-USB-FIFO-LEAK` | DWC2 TxFIFO atomicity leaks stale prior-session data<br><sub>threat-model.md §7.13 'ZLP race' (UC4)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-USB-COMPANION · source-read · 2026-07-02 |
| `S-VENDOR-PK-SUBSTITUTE` | Reflash FSBL with a different vendor pubkey<br><sub>threat-model.md §7.7 / §9.8 (defence FLASH_OTP_BLOCK_VENDOR_PK_HASH not burned)</sub> | ⚪ OUT-OF-SCOPE | — *(no covering claim)* | PLAYBOOK-SILICON-LOCKDOWN · source-read · 2026-07-02 |

## Caveats, downgrades & re-anchors (cite-verification findings)

Each note is the adversarial cite-verifier's resolution for a surface whose synth citation did not cleanly hold — the reason it is PARTIAL/re-anchored rather than COVERED. This is the map's audit trail.

[^1]: **`S-CROSS-CHAIN-REPLAY` — Replay a slot signature across chains (Claim 5).** One of two cites is misresolved; the surface is nonetheless genuinely covered by the other.  CITE 1 HOLDS (load-bearing): Theorems.lean:279 anchors `theorem theft_free`; its conclusion (line 293) requires any wallet-balance decrement to be backed by a valid C10 sig over `digest = sphincsDigest op σ.entryPointAddress σ.chainId`. `sphincsDigest` (ValidateUserOp.lean:254-279) hashes a preimage containing chainId at bytes [328..360). I independently confirmed the DEPLOYED contract binds `block.chainid` in the same SHA-256 preimage (PQSmartWallet.sol:388), and a collision lemma (Theorems.lean:632-639) ties same-digest→BreaksHash. Hence a chain-A slot sig cannot authorize a chain-B spend (distinct chainId→distinct digest→EUF-CMA). Depth: source-read of a Lean proof that is proven modulo axioms A1-A5 and bridged to deployed bytecode via A3.1 (R1 bytecode↔AST transcription is a cited-TCB residual). This is a model+deployed-artifact anchor, not model-only.  CITE 2 DOES NOT HOLD: domain/src/lib.rs:856 is inside `deserialize_pin_state`/its Kani harness — a non-production mock-SE PIN-state recovery parser explicitly documented as NOT the shipping gate — not any slot KDF. The actual chain-bound slot KDF is at lines 705-767 (`slot_entropy` mixes chain_id.to_be_bytes()). Line number is wrong AND, even corrected, per-chain key derivation is only defense-in-depth, not the primary anti-replay proof.  Verdict stays COVERED because cite 1 + the on-chain block.chainid binding fully cover the threat; cite_holds=false flags that the domain cite must be repointed to lines 705-767 (and the Theorems.lean anchor carries a cited-TCB bridge caveat, so this is a proof-modulo-axioms coverage, not silicon-E2E).

[^2]: **`S-CAP-RESET` — Reset/increase the monotonic on-chain caps (Inv #7).** RE-ANCHORED on finalize: the synth headline cite `Invariants.lean:855` (reachable_implies_combinedCap) is the cap-ENFORCEMENT invariant — insensitive to resets (a reset trivially preserves ≤cap, so it cannot detect a resetter). The no-reset property is `no_reset_path` (:385, kernel, no axioms), confirmed present. Verdict held at PARTIAL: model-level K + closed-world gate + deployed test, bridged to bytecode via the shared A3.2/codehash pin (not an explicit no-reset bytecode discharge).

[^3]: **`S-CLEAR-SIGN-DECODE-DRIFT` — Clear-sign decoder mis-binds calldata to displayed intent.** multisend.rs cite HOLDS: 16 #[kani::proof]s genuinely prove canonical-acceptance (decode==Ok ⟹ unique canonical encoding, no hidden trailing payload) plus record_walk_field_fidelity/exact_tiling (displayed operation/to/value/data are verbatim payload bytes) — real coverage of the multiSend mis-bind threat, but bounded-∀ (≤2 records), host 64-bit usize, not re-proven at 32-bit (self-documented finding 3b-3). erc7730_render_pure_tests.rs cite HOLDS at host-test depth: 59 tests over the dbgen corpus; its CS9 (legacy walker not re-exported) and CS5 (scalar-hide rests on build-time dbgen gate, cf. line 1433) qualifiers are accurate. cowswap_order.rs cite DOES NOT HOLD as labeled: the file header (lines 14-18, 38-47) states its Kani module proves only 204-byte decode-soundness/field-extraction and that the setPreSignature cross-check + keccak struct_hash 'stay in secure/' — the claimed orderUid/presign BINDING lives in uncited secure/src/tx/eip712/safe/cow_binding.rs and is NOT Kani-proven here. So the CoW binding leg — the most binding-sensitive part of this threat — is uncovered by the cite. Coverage is genuinely partial (mixed bounded-Kani + host-test depth, CoW binding uncited), so PARTIAL stands; cite_holds=false because one citation overstates a binding proof that the cited artifact does not contain.

[^4]: **`S-DISPLAY-VS-SIGN` — Device signs X while displaying Y (WYSIWYS break, Claim 9).** Verified all three citations against the source. (1) G5-exec-K = `executeBatch_faithful` at contracts/verification/lean/SphincsCVerify/Spec/Theorems.lean:656-684 proves the on-chain EXECUTE is faithful to the SIGNED calldata (E-1..E-8: caller=EntryPoint, value-outflow=sum of signed values, credit consumed, runs in signed order), and SOL1 folds sha256(callData) into the digest — this is the SIGN↔EXECUTE / CALLDATA↔DIGEST binding (Claim 3/4), a NEIGHBORING threat. The Lean model has no display in scope, so it does NOT cover DISPLAY↔SIGN (what the user sees vs what is signed). Misattributed → cite_holds=false. (2) host-render-cowswap (secure/src/display_under_test/cowswap_render_pure_tests.rs) genuinely binds SIGNED canonical bytes to the rendered LCD rows with non-vacuous flips + golden-grid hash — real display↔sign coverage for the CoW path. (3) host-render-safe (safe_display_render_pure_tests.rs) routes through the real verify_and_bind_trailer decode (raw_data cross-checked vs canonical data_hash), asserts inner amount/recipient/Safe-address with non-vacuity — real coverage for the Safe path. Net: the two host-render cites provide genuine PARTIAL coverage (only CoW + Safe shapes; ERC-20-direct, ERC-7730, typed_call and by-design blind-sign paths lack a universal WYSIWYS proof), while the primary K-labeled Lean cite covers the wrong link. COVERED would be false assurance; UNCLAIMED would discard real evidence. Verdict stays PARTIAL, cite_holds=false.

[^5]: **`S-EIP1271-USEROP-PATH` — EIP-1271 view path used to authorise a UserOp (forgery oracle).** Both cites resolve to real, non-vacuous theorems on-topic for the OC1/OC2 forgery-oracle threat, but each covers only a model-level half with a self-disclosed bridge gap, so together they are PARTIAL, not COVERED. G8 (OffchainBinding.lean:112-118, offchain_nested_disjoint_from_userop_digest) proves MESSAGE-image disjointness only — its own docstring (l.96-111) says it establishes 'message-disjointness, not signature-non-validity', and that replaySafeHash/keccak256 are an opaque MODEL whose deployed-bytecode binding is 'carried separately by test/halmos/HalmosIsValidSignature.t.sol, NOT by this model'. G3 (Invariants.lean:1045-1056, unauthorized_userop_breaks_hash) supplies the missing signature-non-validity step but is premise-gated (requires caller-supplied ¬transcriptHasMsg), lives at the Hypertree.verify layer, and is explicitly 'structurally instantiable and conditionally non-vacuous, NOT an unconditional theorem' (l.1032-1044); its docstring states the alignment to the deployed on-chain ByteVec verifier 'is the existing A3.1 axiom, not re-proven here.' Neither theorem bridges to the deployed firmware nesting (aa/src/eip1271.rs) or the on-chain verifier accept except via axiom/separate Halmos test. The real airtight defense (on-chain bare SHA-256 sphincsDigest vs off-chain keccak-nested replaySafeHash + firmware-always-nests + aa/tests/offchain_raw32_forgery.rs regression, per the OC1/OC2 catalog) exists but is NOT the cited evidence. Per the honesty contract (model without deployed bridge = PARTIAL), downgrade COVERED->PARTIAL.

[^6]: **`S-NS-TOCTOU` — NS races to mutate buffer after validation (TZ2).** SYNTH MISS corrected on finalize: R3 enumerated `kani-nsptr` but synth left covering[] empty (→ wrongly-UNCLAIMED). The 8 Kani harnesses prove the pointer VALIDATION (range/alias/disjointness, C); the race itself is defeated by the copy-then-parse snapshot (audit/design, not a proof) — hence PARTIAL, not COVERED.

[^7]: **`S-PIN-COMPARE-SW` — Software PIN compare instead of SE silicon (Inv #2).** One of the two covering citations is mis-attributed to this threat. Citation `proto-pin-lockstep` (tamarin/pin_lockstep.spthy:119, lemma fresh_synced_means_no_reset) proves the three-way counter RECONCILE property (a partial counter reset desyncs from the un-reset counters and is wiped at boot). By its own MODEL SCOPE caveats (lines 20-25, 58-64) it abstracts each counter to a 'fresh'/'zeroed' STATUS and models NEITHER the wrong-PIN bump rule NOR where the PIN compare executes — it says nothing about software-vs-SE-silicon compare. That is a neighboring threat (counter-reset/lockstep desync), not S-PIN-COMPARE-SW; it does not cover this surface (and carries its own fidelity caveat that the symmetric model overstates the deployed directional reconcile). Hence cite_holds=false. However, citation `si-pin-gate-3way` (make pin-gate-hw-counter-e2e, Makefile:2629-2650) genuinely exercises MCU page-124 + OPTIGA E120 LUC + SE050 UserID counters on real STM32U585 silicon, driving PIN cycles under dual-se+optiga-hw-counter; silicon-monotonic OPTIGA/SE050 lockout on wrong PINs is direct partial evidence that the PIN gate/compare is enforced in SE silicon (matching threat-model.md §6.2 line 186 / §7.4 line 230). This alone sustains PARTIAL — not COVERED, because it is a positive functional test rather than a proof that no software compare exists, OPTIGA ship-blockers S-1/S-2/S-3 remain open (F1D0 Change=ALW + sample cert), and in shipping config the SE050 counter read returns None. Verdict retained at PARTIAL; not downgraded to UNCLAIMED because the silicon-E2E citation independently holds.

[^8]: **`S-SLOT-STATE-CORRUPT` — Per-signature flash slot-state corruption (Inv #8 stateless slot).** Both offered citations fail resolution. Citation 1 (domain/src/lib.rs:856, labelled "slot KDF derivation binding") actually anchors the `encrypted_secrets` field of `deserialize_pin_state` — a PIN-state blob parser, NOT slot KDF and NOT flash slot-state; its Kani harness (deserialize_pin_state_panic_free, L879-887) proves only panic-freedom and its own doc (L873-878) explicitly scopes it to the NON-PRODUCTION mock-SE/Tropic01 software-MACD recovery path, "NOT the shipping dual-SE auth gate." Wrong subsystem, non-production. Citation 2 (G8-eip1271-K) is a bare prose claim with no file:line/theorem/make-target anchor and speaks to on-chain Type-1 slot-key install semantics, not per-signature flash slot-state corruption or the page-123 counter/compaction. Neither cite covers THIS threat, so cite_holds=false. Verdict stays PARTIAL on the merits — genuine partial coverage exists but was mis-cited: the statelessness invariant (threat-model.md §8 invariant 8, "the only per-signature flash state is the page-123 EIP-1271 counter; everything else is re-derived in SRAM"), SLOT_CACHE living in SRAM only (nsc/state.rs:319), and the F3 crash-atomicity mitigation replaying USEROP_SIGS first in hw/flash.rs::compact_page (L1557-1605) with the residual honestly documented in threat-model §8. This is design-argued + partial-mitigation with a known documented residual (compaction is single-page erase-then-replay, not two-phase), which is exactly PARTIAL — not COVERED (no proof/silicon bridge to crash-atomicity) and not UNCLAIMED (the invariant is genuinely implemented). Depth: source-read only.

[^9]: **`S-USB-OVERFLOW` — Buffer overflow in APDU reassembly.** CLOSED 2026-07-02 (UNCLAIMED -> PARTIAL): the attacker-facing HID reassembly + APDU framing + chain-cursor (shared/src/apdu_framing.rs — the one layer between a malicious USB host and the firmware) was proptest-SAMPLED; now EXHAUSTIVELY Kani-proven panic/OOB-free over a fully arbitrary assembler state (no kani::assume) + symbolic frame, incl. rx_pos at/over the MAX_APDU_RX boundary (the overflow-critical continuation write). Anti-vacuity: removing the reassembly bounds guard flips process_frame_continuation_no_oob to VERIFICATION:- FAILED. PARTIAL not COVERED: bounded C-tier (host 64-bit usize, all lengths <=4096 << 2^32 so no 32-bit divergence; the seq-0 scrub loop is bounded in-harness, full range via proptest), AND the per-command dispatch in nonsecure::usb::commands stays host-uncompilable = the other half of the §35 P7 USB-decoder item.

[^10]: **`S-USEROP-REPLAY` — Replay a Type 2 signature across two UserOps.** Verdict PARTIAL is correct, but the citation set does not cleanly hold. (1) G0-theft-free/Theorems.lean:279 HOLDS at model level: theft_free requires a verifying sig over sphincsDigest, and sphincsDigestPreimage (ValidateUserOp.lean:259) binds op.nonce (also sender/callData/entryPoint/chainId), so a Type-2 sig over nonce N cannot verify for a different nonce M — this covers the *distinct-nonce* replay case only, and is a theft-freedom theorem, not a replay theorem. (2) A2-entrypoint is the load-bearing cite for the PRIMARY threat (same-nonce reuse) and it is explicitly UNDISCHARGED: the entrypoint_no_replay axiom was REMOVED 2026-06-14 (Bridge/EntryPoint.lean:159-168) because handleOp never reads op.nonce; EntryPoint v0.6 NonceManager replay protection is a cited-TCB fact 'simply not modelled in Lean,' deployed-bytecode discharge not done (defeater D2). This is why the surface is PARTIAL not COVERED — honest, but it is an assumption, not coverage. (3) kani-aa/aa/src/userop.rs:776 is MISATTRIBUTED and irrelevant: line 776 is decode_flags_total_and_bounded (a bitfield Kani proof, not userOpHash/nonce), and the real compute_user_op_hash (line 524) is explicitly documented (lines 507-510) as IGNORED on-chain since the firmware signs a SHA-256 sphincs digest, not the keccak userOpHash. cite_holds=false because one of three cites is bogus and the same-nonce replay protection rests wholly on un-modelled cited-TCB; corrected_verdict stays PARTIAL because the digest-binds-nonce half is genuinely proven in-model. **UPDATE 2026-07-17 (named the cited-TCB leaf — `T-EP-NONCE`):** the same-nonce residual's cited-TCB fact is now stated OBSERVABLY, not as a black box. Per the canonical eth-infinitism EntryPoint v0.6.0 `NonceManager`, `_validateAndUpdateNonce(sender, nonce)` returns `nonceSequenceNumber[sender][key]++ == seq` (`key = nonce >> 64`, `seq = uint64(nonce)`): a reused `(sender, nonce)` yields `false` (not a revert inside the wallet), whereupon the EntryPoint reverts the bundled op with `FailedOp` **AA25 invalid account nonce** — so the op **never reaches the execution phase**, hence no wallet-balance change. That is the exact guarantee the wallet relies on (the wallet itself never reads `op.nonce` except as a `sphincsDigest` input; `PQSmartWallet.sol:378`). **Pending receipt to strengthen the binding:** the EntryPoint is currently pinned by ADDRESS only (`0x5FF1…789`); the honest cited-TCB leaf would bind that address's deployed `extcodehash` to the audited canonical v0.6.0 singleton via a fork-gated receipt (needs a live RPC `cast code | cast keccak`, not available in the current sandbox — tracked). This does NOT discharge the assumption (that stays cited-TCB / defeater D2 open); it makes the leaf named, observable, and receipt-pending instead of an unstated gap.

## Cross-links (amend, don't duplicate)

- [`ASSURANCE_CASE.md`](ASSURANCE_CASE.md) — the top-down claim tree (G0–G10) + the §7 defeater register (D1–D9). COVERED/PARTIAL rows here should agree with a leaf there.
- [`FV_SURFACE_MAP.md`](FV_SURFACE_MAP.md) — which of the 8 FV surfaces the adversarial review has actually swept (the `audit`-evidence provenance for the review depth).
- [`docs/security/threat-model.md`](../../../docs/security/threat-model.md) — the threat SSOT this map is indexed by; every `threat-model source` cell points into it.
- [`REVIEW_PROVENANCE.md`](REVIEW_PROVENANCE.md) — the `Last review` column's depth/date ledger.

## How to keep this honest

- On a new claim landing: flip the covered threat's row and add the cite (verify it resolves first).
- On a new threat/attack-class: add a row — an unmapped threat defaults to 🔴 UNCLAIMED, not omission.
- Never up-rate PARTIAL→COVERED without resolving the model→deployed bridge the footnote names.
- A 🔴 UNCLAIMED row that gets a first covering claim is a material assurance change — record it.

*Generated 2026-07-02 from the `g3-threat-claim-map-and-provenance-ledger` workflow (enumeration + adversarial cite-verification) + hand finalization (the 9 session-limited verifiers + the two COVERED-cite confirmations + the S-NS-TOCTOU / S-CAP-RESET corrections). A coverage index, not a proof — every node routes to its owning artifact.*
