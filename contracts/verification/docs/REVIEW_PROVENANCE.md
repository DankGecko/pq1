# Review-provenance ledger

**Snapshot date: 2026-07-02.** A standing record of *which* surface was adversarially reviewed, *at what depth*, and *when* — so a **stale, source-only "no finding" cannot masquerade as durable assurance**. A green review from three weeks ago that only *read* the source is weaker evidence than one that *ran the tool* yesterday; this ledger makes that difference first-class instead of invisible. It operationalizes the review-provenance field the FV adversarial-review playbook names (`docs/verification/fv-adversarial-review-playbook.md` line ~189).

Pairs with [`THREAT_CLAIM_MAP.md`](THREAT_CLAIM_MAP.md) (the `Last review` column there points here).

## Depth legend (weakest → strongest)

| Depth | What it means | Epistemic weight |
|---|---|---|
| `source-read` | reviewer READ the code/model/proof; no live build/run | weakest — a stale source-read is the exact trap this ledger guards |
| `manual-audit` | human/LLM adversarial audit that traced concrete exploit paths (found real HIGHs) | strong for what it looked at; coverage is by-hand, not exhaustive |
| `executing` | the tool was actually RUN (proverif/tamarin/kani/lake/forge), verdicts read from output | strong — binds the artifact, not a reading of it |
| `mutation-verified` | an anti-vacuity mutation was shown to flip the verdict (proof is non-hollow) | strongest software tier — the gate is proven load-bearing |
| `silicon-E2E` | validated on real STM32U585 / SE hardware | strongest for hardware-bound properties |

**25 rounds on record.**

| Round | Date | Surface reviewed | Depth | Findings | Verdict | Doc / commit |
|---|---|---|---|---|---|---|
| PLAYBOOK-USB-COMPANION | 2026-07-02 | NS-side USB stack + hostile-companion threat model — hostile-host containment (cross-cutting outer-attacker lens) | `source-read` | Adversarial-review PLAYBOOK — cross-cutting lens mapping what a fully-hostile host can attempt across every flow to the on-device defense that owns it. Recipe, not an executed run. | Recipe/catalog (source-read), cross-cutting lens. | docs/security/adversarial-review/usb-companion-adversarial-review.md |
| PLAYBOOK-TRUSTZONE-GATEWAY | 2026-07-02 | NSC veneers, NS-pointer validation, SAU/GTZC — invariant #4 secrets only in S-world. TZ1-TZ8 catalog | `source-read` | Adversarial-review PLAYBOOK (recipe + master prompt + TZ1-TZ8 catalog). Prior TZ4 pass (per memory) found nsc_register_heartbeat weaker validator → work-todo #12b, and corrected CLAUDE.md drift (master=0xF1D2, PBS DHUK-derived). This file is the recipe, not that run's finding record. | Recipe/catalog (source-read). Same disclaimed boundary as the family. | docs/security/adversarial-review/trustzone-gateway-adversarial-review.md |
| PLAYBOOK-TRUSTED-UI | 2026-07-02 | confirm dialog, PIN entry, seed wizard, buttons, timer — physical-consent gating (sign without a genuine confirm / NS spoofs consent / timer bypass) | `source-read` | Adversarial-review PLAYBOOK (recipe + catalog). | Recipe/catalog (source-read). Disclaimed boundary. | docs/security/adversarial-review/trusted-ui-adversarial-review.md |
| PLAYBOOK-SILICON-LOCKDOWN | 2026-07-02 | STM32U5 option bytes (RDP/WRP/HDP/OTP/debug), OPTIGA LcsO ratchet, SE050 lockdown, secure-boot immutability — hardening depth (no single-point collapse) | `source-read` | Adversarial-review PLAYBOOK — lockdown-depth lens using an (a) enforced / (b) deferred-by-design / (c) unenforced taxonomy where only (c) is a review target. Ties to the OPTIGA S-1/S-2/S-3 ship blockers (deferred-by-design). Recipe, not an executed run. | Recipe/catalog (source-read), lockdown-depth lens. Irreversible LcsO/factory work explicitly deferred to pre-ship silicon. | docs/security/adversarial-review/silicon-lockdown-adversarial-review.md |
| PLAYBOOK-SECURE-ELEMENT | 2026-07-02 | OPTIGA + SE050 drivers, provisioning, PIN lockstep — invariants #1 XOR split, #2 PIN lockstep, #3 SE tunnels. SE1-SE9 catalog | `source-read` | Adversarial-review PLAYBOOK (recipe + master prompt + SE1-SE9 catalog anchored to plaintext-on-I2C / PIN-desync / advertised≠actual failure modes). | Recipe/catalog (source-read). Disclaimed boundary. | docs/security/adversarial-review/secure-element-adversarial-review.md |
| PLAYBOOK-SCA-FI | 2026-07-02 | FI-hardened sign chain, FI primitives, CT compares, zeroize — single-fault safety + trace secrecy. FI1-FI10 catalog | `source-read` | Adversarial-review PLAYBOOK (recipe + master prompt + FI1-FI10 catalog). Layer-1 defenses referenced: rainbow FI sweeps, cargo-checkct, zeroize-audit. Recipe, not an executed rainbow/lascar bench run (those are red-teaming.md / silicon). | Recipe/catalog (source-read). Bench SCA/FI is deferred to silicon per its own boundary. | docs/security/adversarial-review/sca-fi-adversarial-review.md |
| PLAYBOOK-ONCHAIN-CONTRACTS | 2026-07-02 | PQSmartWallet/Factory/PQMultiOwnable/Yul verifier (Solidity-level safety beyond theft_free) — reentrancy / cross-slot auth / storage collision the Lean proof doesn't reach | `source-read` | Adversarial-review PLAYBOOK (recipe + catalog) for the Solidity surface the Lean model abstracts over. Recipe, not an executed Halmos/Kontrol run (those gates run in CI separately). | Recipe/catalog (source-read). Disclaimed boundary. | docs/security/adversarial-review/onchain-contracts-adversarial-review.md |
| PLAYBOOK-OFFCHAIN-SIGNING | 2026-07-02 | EIP-1271/6492 + page-123 counter state machine — invariant #9 anti-forgery binding (companion-nests-hash → UserOp forgery oracle / counter rollback) | `source-read` | Adversarial-review PLAYBOOK (recipe + catalog). Anchors the 2026-06-11 RAW32 on-device-nesting fix as the load-bearing defense. | Recipe/catalog (source-read). Disclaimed boundary. | docs/security/adversarial-review/offchain-signing-adversarial-review.md |
| PLAYBOOK-FWUPDATE-SECUREBOOT | 2026-07-02 | fw_update/, fw-manifest/, fwsign/, fsbl/, measured-boot — Claim 8 + boot integrity | `source-read` | Adversarial-review PLAYBOOK (recipe + master prompt catalog for unsigned/downgraded-image / brick / fingerprint-spoof modes). Related executing evidence lives elsewhere (security-review-2026-05 C-1/C-2 fixes). | Recipe/catalog (source-read). Disclaimed boundary. | docs/security/adversarial-review/firmware-update-secure-boot-adversarial-review.md |
| PLAYBOOK-CLEAR-SIGNING | 2026-07-02 | Trusted-display clear-signing — ERC-7730 + EIP-712 decoders, display renderers, Merkle bundles; WYSIWYS property (Claim 9). CS1-CS10 catalog | `source-read` | Reusable adversarial-review PLAYBOOK (recipe + Part-C master prompt + CS1-CS10 failure catalog anchored to real gates/near-misses in-tree). NOT an executed swarm run — a source-read review guide with a Status column separating defended-by-construction / claim-vs-code tension / by-design-limited / found-this-surface. Layer-1 defenses referenced: render harness, Kani kernels, fuzz differentials. Explicit boundary: a playbook attests no COVERED surface lets its invariant drift as of the last EXECUTING pass; it cannot attest uncovered paths, silicon, or that a source-only pass proved anything. | Recipe/catalog document (source-read), not a completed executing round; its own stated boundary disclaims durable assurance absent an executing pass. | docs/security/adversarial-review/clear-signing-adversarial-review.md |
| FV-PROTOCOL-QUERYBODY-2026-07-02 | 2026-07-02 | Protocol-model query bodies — V2 tautology/anti-vacuity across 17 ProVerif RESULTs + 8 Tamarin lemmas (dual_se_unlock / scp03_handshake / optiga_shield_handshake / scp03_replay / pin_lockstep) | `executing` | EXECUTING round: proverif + tamarin-prover 1.12.0 RUN on live tree, plus a live mutation. Every ProVerif RESULT and Tamarin lemma confirmed NON-vacuous on current tree — injected reachability probes fired every correspondence premise, no positive result a tautology. 5 confirmed LOW/INFO, 0 medium+ (confirmed[] empty by medium-floor). Two shapes: (a) anti-vacuity coverage asymmetry — the PM-2 reachability-witness fix in dual_se_unlock.pv never carried to scp03_handshake.pv / optiga_shield_handshake.pv (F1 MUTATION-PROVEN: breaking baseline optiga leg leaves gate GREEN while both mutual-auth correspondences go vacuously-true); (b) header claim-vs-proof gaps — 'injective-agreement'/'session-key secrecy'/'LOCKSTEP' advertised but not queried. | Query bodies sound today (every positive result empirically witnessed reachable); the anti-vacuity retrofit and a few model headers overstate — durability/labeling defect, not unsoundness. No fixes applied. | contracts/verification/docs/findings/ADVERSARIAL_REVIEW_PROTOCOL_QUERYBODY_2026-07-02.md |
| FV-KANI-USB-2026-07-02 | 2026-07-02 | USB APDU reassembly + framing + chain-cursor (shared/src/apdu_framing.rs) — the attacker-facing hostile-companion parse layer (S-USB-OVERFLOW) | `mutation-verified` | 4 Kani harnesses proving ∀ panic/OOB-freedom: process_frame continuation (arbitrary assembler state incl. rx_pos at/over MAX_APDU_RX + symbolic frame -> no OOB write into the reassembly buffer) + first-frame/scrub + ChainState cursor-within-capacity + parse_apdu_header soundness; plus ApduComplete(len) <= MAX_APDU_RX. Anti-vacuity CONFIRMED: removing the reassembly bounds guard flips process_frame_continuation to FAILED (kani_mutations.json: usb_apdu_reassembly_bounds). Host proptest (fuzz_props) still green. | No out-of-bounds write reachable; a previously proptest-SAMPLED surface is now EXHAUSTIVELY proven. Moves S-USB-OVERFLOW UNCLAIMED -> PARTIAL (bounded C-tier, host-width caveat; the per-command dispatch in nonsecure::usb::commands stays host-uncompilable = other half of §35 P7). | shared/src/apdu_framing.rs (kani_harnesses) + scripts/kani_mutations.json |
| FV-ADV-2026-07-02 | 2026-07-02 | FV soundness full-surface: the Lean tree re-attack PLUS first adversarial pass on the 5 never-reviewed surfaces (Aeneas §33, protocol models, Kani/Miri, CT/SCA, fuzz/differential); ~30 agents, V1-V11 + G1-G5 | `executing` | First EXECUTING round (the residual the 07-01 source-read rounds named). make verify-lean4checker RAN to completion: external-kernel re-check ACCEPTED every declaration across all 55 modules (exit 0), incl Crypto/*. verify-audit executed: live 92-entry dump matches AXIOM_STATUS.json on the 7 target theorems, exactly 15 axiom decls all accounted, zero native_decide/ofReduceBool/@[extern] on proof paths. Confirmed findings all G1/V5/V9: HIGH exec-lean F1 = verify-three-claims deterministically SIGPIPE-fails + its CI job at nested contracts/.github path GH Actions never runs + absent from gate_enforcement.json; HIGH exec-extracted F1 = §33 CI enforcement fictional (lean-extracted.yml no schedule, 2 extract-* regen-diffs RED and merged green). | Headline theorems sound and now externally kernel-checked (smuggled-axiom/under-report doubt empirically closed); the assurance SYSTEM overstates (gates advertised as enforced that never fire on their surface). No fixes applied this pass. | contracts/verification/docs/findings/ADVERSARIAL_REVIEW_2026-07-02.md |
| ZEROIZE-AUDIT-2026-07 | 2026-07-01 | Secret-lifecycle zeroization — pqsigner-domain (BIP-39→C10 derivation) + sphincs-c10 keygen/from_parts; source + MIR/LLVM-IR/asm at O0/O1/O2 (Trail of Bits zeroize-audit skill) | `executing` | Tool-run (cargo +nightly, MIR+LLVM-IR+asm). Q1 validated: every existing zeroize::Zeroize wipe survives -O1/-O2 as volatile stores (no dead-store elim; volatile-store count O0=2→O1=552→O2=456). 8 REAL findings FIXED: transient SHA-256/HMAC INPUT buffers held secret pre-images (BIP-39 seed, sk_seed, slot entropy, master_lo) dropped UNWIPED — asm-confirmed retained on stack frame at ret; severity medium/defense-in-depth (physical-extraction/fault-dump). 2 upstream tool bugs found. Secure-crate wipe path validated manually (compile_error fences block the audit scripts). | Zeroize approach validated at asm level; 8 un-named-intermediate residue findings fixed. Run in support of the rr-1 HardFault-zeroize backstop. | docs/security/zeroize-audit-2026-07.md |
| FV-KANI-2026-07-01 | 2026-07-01 | Firmware #[kani::proof] surface — 93 harnesses / 17 files (ERC-7730/typed-call/safe_tx/erc20/rlp/nsptr/offchain decoders); angle kani-decoder-vacuity (V1/V3/V6/V8/V11 + G1/G3) | `source-read` | 7 cluster-finders → per-finding independent refute → synthesis. Ground-truth counts/gate scopes executed via grep/python but NO full cargo kani harness re-run. No harness found vacuous in the load-bearing sense; the two medium candidates (typed-call parse_text_sig coverage, abi::walk OOB over-claim) both DOWNGRADED to info by refutation. Survivors = 13 low + 7 info, 0 medium+ (confirmed[] empty by construction). Structural fact: anti-vacuity gate verify-kani-mutation guards only 6 mutations/4 of 17 files, 57 harnesses/13 files unscreened (seeded input, disclosed D4). Genuinely-independent survivor #6 = kani gate polices_paths omits domain/tx-core (FIXED 07-01). Two honesty caveats: multisend finder died mid-run (backfilled no cross-vote); finding #1 was seeded. | No firmware-Kani harness vacuous in load-bearing sense; no attacker-triggerable defect on current code. Survivors are coverage-completeness/gate-scope/bounded-vs-universal assurance gaps that could let a FUTURE regression slip past green. | contracts/verification/docs/findings/ADVERSARIAL_REVIEW_KANI_2026-07-01.md |
| FV-ADV-2026-07-01 | 2026-07-01 | FV soundness of the Lean on-chain tree (theft_free + V1-V11 vacuity catalog) vs the claims inventory (ASSURANCE_CASE / THE_CLAIM / AXIOM_STATUS.json / PROOF_MAP / OPEN_PROOF_OBLIGATIONS + SphincsCVerify Lean tree) | `source-read` | 5 angle-scoped reviewers walked V1-V11; 15 confirmed/open cross-voted, 9 refuted. Every mechanical vacuity class that would break theft_free (V1/V4/V6/V7) was attacked and HELD. Survivors are ALL ledger/CI-automation/gate-coverage/doc-integrity gaps, no math hole: HIGH F1 = verify-ledger-consistency gate never fires on AXIOM_STATUS.json-only edits (lean-fv.yml paths filter omits docs/**; PoC 3 real merged commits); MEDIUM = A3.1 bridge axiom advertised as false '=verifyYulModel' ∀ (live is '=execC10Asm'), two prose closure= claims outside machine-checked closures block, storage-mutators reachability precondition guarded only by unwired script; LOW = A5-EUFCMA.lean_type reproduces retired →False shape. | No confirmed finding hollows a headline soundness claim; residual is enforcement/ledger-integrity. EXPLICITLY SOURCE-READ ONLY — no lake build / #print axioms / lean4checker this pass (named as the biggest residual). | contracts/verification/docs/findings/ADVERSARIAL_REVIEW_2026-07-01.md |
| FEATURE-ROADMAP-RESEARCH-2026-06 | 2026-06-30 | Feature/appeal research (NOT a security threat-coverage round) — 15-agent web-research workflow; 46 candidates, 27 ruled out; invariant-aware feasibility panel | `source-read` | Web-research + 3-lens feasibility panel. Strategic call: PQ1 is a deliberate slow-money signer (Safe PQ co-signer + cold vault). Top-5 = WalletConnect→Safe-cosigner→cold-vault→ERC-7730 pack→v0.6 sponsored-deploy. Included as a research round for provenance completeness; it asserts NO security coverage and must not be counted as a threat-coverage pass. | Product/feature research, not a security review. Zero coverage claims — listed only so the ledger is exhaustive. | docs/feature-roadmap-research-2026-06.md |
| SOTA-FW-BOUNDED-VERIF-2026-06 | 2026-06-29 | Firmware bounded/property-verification SOTA + first slice — where to point bounded verification on the Rust no_std firmware (decoders + counters), which tool, the concrete first slice | `executing` | Decision record with an explicit evidence-tier scheme ([code]/[tool]/[lit]/[claim]); [tool] tier = ran the binary. Decided Kani 0.67 (already installed, make kani wired) as the vehicle; first slice = multiSend canonical-acceptance Kani proof (extract to pqsigner-tx). Excludes contracts (Halmos/Kontrol) and SPHINCS+ kernels (Aeneas/Lean track; SHA-256 cores choke Charon/Aeneas). | Decision record (partly executing — tools run). Sets the firmware-Kani surface the KANI-2026-07-01 round later attacked. | docs/firmware-bounded-verification-sota-2026-06.md |
| WYSIWYS-ERC20-MISATTRIB-01981a14 | 2026-06-28 | Clear-signing WYSIWYS — direct ERC-20 transfer/transferFrom/approve metadata attribution in single-tx + batch handlers (v1_ms disjunct in verified_meta) | `manual-audit` | HIGH: a direct ERC-20 transfer to token Y rendered on trusted display with a DIFFERENT DB token T's name/symbol/decimals — reachable by attaching a bogus (non-verifying) safe_v1 trailer referencing T; the v1_ms disjunct matched raw unverified trailer bytes and the direct render branch never re-checked meta.contract==tx.to (comment falsely claimed it did). Attacker-controlled headline identity + understated amount scale; twin in batch handler. Fix (defense-in-depth both layers): render-path address match direct_erc20_meta_matches (authoritative, both handlers), + source-side disjunct gating requiring Safe context actually verified (single fault must defeat both layers). | HIGH FIXED. Legitimate Safe-wrapped ERC-20/CoW/multiSend flows unaffected. | git 01981a14 |
| WYSIWYS-ERC20-DECIMALS-ea47a114 | 2026-06-28 | Clear-signing WYSIWYS — firmware-pinned ERC-20 DB decimals correctness (magnitude-hiding via decimals larger than true on-chain value) | `executing` | HIGH magnitude-hiding: the pinned ERC-20 DB shipped tokens with decimals LARGER than true on-chain value; display renders amount/10^decimals so a too-large decimals shows a full-balance drain as dust (50k FLUX as '0.000005 FLUX', correct symbol, non-zero so zero-collapse guard misses). VERIFIED REAL not FP: direct eth_call decimals() on multiple independent full nodes (publicnode/blastapi/1rpc all return 0x..08 for FLUX/MEME). Corrected 20 erc20.json + 2 e2e entries (10 were real drain risks), regenerated ERC20_DB_ROOT. New tool tools/verify_erc20_decimals.py cross-checks every DB entry vs on-chain decimals() with 2nd-reading + bogus-selector-collision guards. | HIGH FIXED, on-chain-verified (eth_call across independent nodes). Systemic follow-up open: build_erc20_db.py still ingests list decimals verbatim (verifier not yet wired into build). | git ea47a114 + docs/security/vulns/VULN-erc20-decimals-inflation-flux-meme.md |
| WYSIWYS-APPROVEHASH-0a26a134 | 2026-06-28 | Clear-signing WYSIWYS — Safe approveHash(bytes32) clear-sign gate in single-tx + batch sign handlers (wysiwys-adversarial-audit workflow, 8/10 regions) | `manual-audit` | Adversarial audit found lone survivor (3/3 REAL after adversarial verify): the mandatory approveHash gate keyed on EXACT calldata length (==36 B) was a parser differential vs the on-chain Safe singleton (Solidity external dispatch ignores trailing bytes), so 37-B selector\|\|hash\|\|0x00 skipped the gate AND failed safe_v1, falling through to GENERIC blind-sign of an approveHash = invisible full-signature pre-approval = drain of any co-signed Safe behind a routine BLIND SIGN screen. HIGH. Fix: key gate on SELECTOR alone via shared safe::is_approve_hash_claim. Tests run: new regression + 2142 host tests pass, make secure builds, make e2e all scenarios pass. | HIGH FIXED. Manual adversarial audit with tests + e2e executed. | git 0a26a134 + docs/security/vulns/VULN-approvehash-gate-length-bypass.md |
| A3_1-ADV-2026-06-18 | 2026-06-18 | A3.1 interpreter-refinement — the claim that the deployed on-chain SPHINCS+C10 verifier bytecode refines its Lean declarative spec (transcription/interpreter/codehash/SHA-256 fidelity + axiom hygiene) | `manual-audit` | Multi-agent fan-out diffing transcription region-by-region, interpreter vs EVM/Yul, codehash parity, independent vacuity audit, each + adversarial verify pass. Kernel already proves execC10Asm=spec pointwise (execC10Asm_eq, kernel-clean {propext,Classical.choice,Quot.sound}); review attacks the 4 judgments the kernel can't see. Executed corpus gates: verify-interp 12 KAT + 384 bulk interp==spec==expectValid perfect + rejects 8 negatives; verify-cavp Sha256Impl==FIPS-180-4 229/229 NIST CAVP; PinnedCodehashes + DeployedBytecodeReproCheck [PASS] pinned SPHINCsC10Asm codehash == live Base Mainnet 0xeb1e3fcd..2cc5. theft_free axioms exactly the documented 11, no sorryAx/native_decide. | HOLDS UP — no bucket-i (soundness) defect found. Residual = R1 model↔bytecode transcription (cited-TCB). | contracts/verification/docs/findings/A3_1_ADVERSARIAL_REVIEW_2026-06-18.md |
| FAITHFULNESS-AUDIT-2026-06-14 | 2026-06-14 | Faithfulness-to-intent of the Lean verification (does theft_free actually capture 'no unauthorized fund movement'; are axioms the right assumptions; is each theorem strong enough that a real bug breaks it) — the semantic question the consistency loop does NOT answer | `mutation-verified` | 12-agent workflow, 3 adversarial techniques: (1) mutation testing gold standard — inject genuine defect into verified models in isolated worktree, rebuild whole lean/ + executable KAT, record KILLED vs SURVIVED; (2) threat-model→theorem coverage matrix over every CLAUDE.md invariant; (3) per-axiom falsifiability review. All findings reproduced kernel-side against clean HEAD 8377628. Found P1 survivor (cap-bootstrap <→≤ at ValidateUserOp.lean:298 SURVIVED) + Gap-3 replaySafeHash domain separation unmodeled. Both RESOLVED same day: capOk_bootstrap_implies_strict added (mutant re-verified KILLED — now fails compile), Wallet/OffchainBinding.lean models nested replaySafeHash + proves offchain_nested_disjoint_from_userop_digest (theft_free 11-axiom closure unchanged). This is the pass whose lineage caught the earlier EUF-CMA inconsistency + false sha256_injective. | Both action items RESOLVED 2026-06-14; mutation-sensitivity restored. This is the strongest-depth (mutation-verified) FV round on record. | contracts/verification/docs/FAITHFULNESS_AUDIT_2026-06-14.md |
| SOTA-SECURITY-TOOLING-2026-06 | 2026-06 | Security-tooling SOTA survey — LLM/agentic + classical-verification + hardware tooling PQ-Signer could adopt/build (firmware, contracts/Yul, SCA/FI, FV track). ~250 systems | `source-read` | Three-pass deep-research sweep (~250 systems) + local read of SPHINCS-/LeanLoop repos and secure/src/crypto.rs. Adopt-now list: cargo-checkct, Kani, Miri, hevm-equiv, halmos, Tamarin; noted the SPHINCS+ FI countermeasure already lives in c10_sign_verified (Genêt TCHES 2023). NOT a threat-coverage review — a tooling adoption guide (lit/source tier). | Survey/adoption guide, not a coverage round. Informs which gates exist to CLOSE threats; asserts no coverage itself. | docs/verification/security-tooling-sota-2026-06.md |
| SECURITY-REVIEW-2026-05 | 2026-05-12 | Firmware security review — secure/ nonsecure/ proto/ domain/ aa/ tx-core/ tx/ hal/ shared/ fw-manifest/ fsbl/ bip39/ + sphincs-c10 API surface (Solidity verifier, ZK internals, SE driver internals, trusted-UI render OUT of scope) | `manual-audit` | Manual firmware audit with fixes + build verification (cargo check thumbv8m clean). Fixed C-1 Crit (verify FW manifest sig BEFORE destructive commit ops; removed the fingerprint-match gate that protected nothing), C-2 Crit (HandlerGuard::enter prevents use-after-drop on FwUpdateCtx vs SysTick idle-wipe race), H-2/H-3/H-4 (fi::check_true sentinel-gated FI guards on inline sentinels, Type-1 factorySig/addOwnerBytes verify-before-release, gated_unlock double-read), M-1 (HANDLER_DEPTH static mut→AtomicU32), L-2/L-6. | Multiple Crit/High fixed and build-verified. Named out-of-scope surfaces (Solidity/ZK/SE internals/trusted-UI) flagged as warranting own passes — several since covered by later rounds. | docs/security/security-review-2026-05.md |

## Staleness watch — source-read-only rounds (re-review candidates)

These rounds established **no finding at `source-read` depth only** (no live build/run). Their assurance is the weakest tier and decays with every code change to the surface — they are the first candidates for an *executing* re-review. A source-read "clean" is a snapshot of a reading, not a durable guarantee.

| Round | Date | Surface | Why re-review |
|---|---|---|---|
| PLAYBOOK-USB-COMPANION | 2026-07-02 | NS-side USB stack + hostile-companion threat model — hostile-host containment (cross-cutting outer-attacker lens) | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-TRUSTZONE-GATEWAY | 2026-07-02 | NSC veneers, NS-pointer validation, SAU/GTZC — invariant #4 secrets only in S-world. TZ1-TZ8 catalog | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-TRUSTED-UI | 2026-07-02 | confirm dialog, PIN entry, seed wizard, buttons, timer — physical-consent gating (sign without a genuine confirm / NS spoofs consent / timer bypass) | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-SILICON-LOCKDOWN | 2026-07-02 | STM32U5 option bytes (RDP/WRP/HDP/OTP/debug), OPTIGA LcsO ratchet, SE050 lockdown, secure-boot immutability — hardening depth (no single-point collapse) | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-SECURE-ELEMENT | 2026-07-02 | OPTIGA + SE050 drivers, provisioning, PIN lockstep — invariants #1 XOR split, #2 PIN lockstep, #3 SE tunnels. SE1-SE9 catalog | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-SCA-FI | 2026-07-02 | FI-hardened sign chain, FI primitives, CT compares, zeroize — single-fault safety + trace secrecy. FI1-FI10 catalog | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-ONCHAIN-CONTRACTS | 2026-07-02 | PQSmartWallet/Factory/PQMultiOwnable/Yul verifier (Solidity-level safety beyond theft_free) — reentrancy / cross-slot auth / storage collision the Lean proof doesn't reach | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-OFFCHAIN-SIGNING | 2026-07-02 | EIP-1271/6492 + page-123 counter state machine — invariant #9 anti-forgery binding (companion-nests-hash → UserOp forgery oracle / counter rollback) | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-FWUPDATE-SECUREBOOT | 2026-07-02 | fw_update/, fw-manifest/, fwsign/, fsbl/, measured-boot — Claim 8 + boot integrity | source-read only — upgrade to executing (run the tool over the current tree) |
| PLAYBOOK-CLEAR-SIGNING | 2026-07-02 | Trusted-display clear-signing — ERC-7730 + EIP-712 decoders, display renderers, Merkle bundles; WYSIWYS property (Claim 9). CS1-CS10 catalog | source-read only — upgrade to executing (run the tool over the current tree) |
| FV-KANI-2026-07-01 | 2026-07-01 | Firmware #[kani::proof] surface — 93 harnesses / 17 files (ERC-7730/typed-call/safe_tx/erc20/rlp/nsptr/offchain decoders); angle kani-decoder-vacuity (V1/V3/V6/V8/V11 + G1/G3) | source-read only — upgrade to executing (run the tool over the current tree) |
| FV-ADV-2026-07-01 | 2026-07-01 | FV soundness of the Lean on-chain tree (theft_free + V1-V11 vacuity catalog) vs the claims inventory (ASSURANCE_CASE / THE_CLAIM / AXIOM_STATUS.json / PROOF_MAP / OPEN_PROOF_OBLIGATIONS + SphincsCVerify Lean tree) | source-read only — upgrade to executing (run the tool over the current tree) |
| FEATURE-ROADMAP-RESEARCH-2026-06 | 2026-06-30 | Feature/appeal research (NOT a security threat-coverage round) — 15-agent web-research workflow; 46 candidates, 27 ruled out; invariant-aware feasibility panel | source-read only — upgrade to executing (run the tool over the current tree) |
| SOTA-SECURITY-TOOLING-2026-06 | 2026-06 | Security-tooling SOTA survey — LLM/agentic + classical-verification + hardware tooling PQ-Signer could adopt/build (firmware, contracts/Yul, SCA/FI, FV track). ~250 systems | source-read only — upgrade to executing (run the tool over the current tree) |

> The 9 `docs/security/adversarial-review/` family playbooks are `source-read` passes by construction (they enumerate attack-classes against a reading of the code). The 2026-07-01 FV swarm was **source-read only by its own admission**. The genuinely *executing* rounds are the Kani/protocol/A3.1 ones + the 2026-07-02 protocol query-body pass (proverif/tamarin re-run) + the manual WYSIWYS audits (which found the real HIGHs).

## How to append a row

Every future adversarial-review / audit round adds ONE row: `id · date · surface · depth · findings · verdict · doc`. Record the **true** depth — a source-read pass is `source-read` even if it found nothing; up-rating it defeats the ledger's purpose. If the round runs a tool, cite the run; if it flips a mutation, mark `mutation-verified`. When a surface moves from `source-read` to `executing`, note it in the map's `Last review` column too.

*Generated 2026-07-02 from the same workflow's review-round enumeration + hand finalization.*
