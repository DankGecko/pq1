# Allowlist of `theorem ... : True := trivial` (or similar) entries
# permitted in `SphincsCVerify/`.
#
# Format: one entry per line, fully-qualified Lean name.
# Lines starting with `#` are comments. Blank lines are ignored.
#
# Goal: every entry here should eventually be discharged into a real
# theorem with content. The list is documentation-mode; the lint
# script `lint_axioms.sh` fails on any `True`-typed theorem NOT in
# this allowlist.
#
# Add an entry here ONLY if you can name the tier (in the plan file)
# that discharges it, and you put a TODO marker next to the entry.

# (empty) — 2026-06-10: the four remaining placeholders were upgraded to
# real propositional theorems, so the allowlist is now empty.
#   * Wallet.Invariants.no_reset_path  → conjunction of the five
#     Storage-mutator monotonicity lemmas (I-3, "caps unresettable").
#   * Verifier.fors_section_consistent → byte-FORS-helper == structural
#     Fors.reconstructForsPk over the decoded sig (incl. htIdx binding).
#   * Verifier.ht_layer0_consistent / ht_layer1_consistent → byte layer
#     step == Wots.pkFromSig + Hypertree.verifyAuthPath at layer 0 / 1.
# No `True := trivial` theorems remain under SphincsCVerify/.
