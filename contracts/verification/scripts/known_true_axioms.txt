# Allowlist of axioms in `SphincsCVerify/` whose elaborated type
# is `True` or `∀ ..., True` (i.e., placeholder markers without
# semantic content).
#
# Format: one entry per line, fully-qualified Lean name.
# Lines starting with `#` are comments.
#
# After the Phase 0 / Tier-1.9 refactor, the bridge axioms A1
# (`precompile_0x02_is_FIPS_180_4`) and A3.1
# (`solidityVerifier_compiles_correctly`) and A3.2/A3.3/A3.4 are now
# `opaque + axiom-equality` shapes carrying real propositional content,
# and are NOT on this allowlist any more.
#
# What remains:
#
#   - A4 (`evm_bytecode_executes_correctly`) — REMOVED from this allowlist
#     2026-06-14: upgraded from `: True` to a content-bearing statement
#     `∀ (c : Wallet.Execute.Call), Bridge.evmDeliversCall c` (the EVM
#     faithfully delivers every emitted CALL's (target,value,data);
#     KEVM / consensus-client conformance is the cited external
#     discharge). It is still a cited universal-Ethereum TCB axiom and
#     still in `theft_free`'s 11-name closure, but it is no longer a
#     `True`-typed placeholder, so it must NOT be on this allowlist.
#   - A2 (`entrypoint_honest`) — `_Shape`-typed (Prop carries content
#     about the Lean `handleOp` fiction) but the user explicitly opted
#     to keep this as-is rather than discharge against deployed
#     EntryPoint v0.6 bytecode.
#   - The three crypto-shape axioms (SM_DT_TCR_F, ITSR_F,
#     hMsg_random_oracle) — REMOVED from this allowlist 2026-06-26 (P15):
#     they were upgraded 2026-06-14 from `: True` / `∀_, True` placeholders
#     to opaque `_Shape : Prop` types (`SM_DT_TCR_F_Shape`, `ITSR_F_Shape`,
#     `hMsg_RO_Shape` in Crypto/Assumptions.lean), carrying real cited
#     propositional content (Barbosa et al. ASIACRYPT 2024 EUF-CMA
#     reduction; mechanising the advantage bound in Lean is a
#     multi-person-year effort). Exactly like A4, a non-`True`-typed axiom
#     must NOT be on this `True`-only allowlist — leaving them here implied
#     they were still vacuous markers, which is no longer accurate.
#
# The allowlist is now EMPTY: there are no `True`-typed placeholder axioms
# left in `SphincsCVerify/` (lint_axioms reports "True-typed axioms
# (allowlisted): 0").
