{
  "$schema": "../../specs/erc7730-v2.schema.json",
  "_curation_note": "PQSigner semantic curation: deposit is admitted only for the fixed-block Ethereum StarkGate ERC-20 bridge proxy listed by deploymentFormats. The proxy is a multi-token bridge, not a STRK-only contract: the display therefore shows the exact signed L1 token contract separately, uses it to format the exact transferred amount, shows the raw Starknet felt recipient, and shows the exact native L1 message fee. Verified source transfers the exact amount into the bridge and sends an asynchronous L1-to-L2 message; servicing status, limits, L2 processing, and success remain mutable and are not implied. Evidence is historical fixed-block source/runtime authority, not current registry or bridge state, token metadata, completion, future proxy upgrades, fallback, or blind signing. See tests/erc7730-semantic-evidence/bridge-exits.",
  "_pqsigner": {
    "deploymentFormats": [
      {
        "chainId": 1,
        "address": "0xcE5485Cfb26914C5dcE00B9BAF0580364daFC7a4",
        "formats": ["deposit(address token, uint256 amount, uint256 l2Recipient)"]
      }
    ]
  },
  "context": {
    "$id": "StarkGate ERC-20 Bridge",
    "contract": { "deployments": [{ "chainId": 1, "address": "0xcE5485Cfb26914C5dcE00B9BAF0580364daFC7a4" }] }
  },
  "metadata": {
    "owner": "StarkWare",
    "info": { "url": "https://starkgate.starknet.io/", "deploymentDate": "2024-10-01T00:00:00Z" },
    "contractName": "StarkGate ERC-20 Bridge"
  },
  "display": {
    "formats": {
      "deposit(address token, uint256 amount, uint256 l2Recipient)": {
        "$id": "deposit",
        "intent": "Request Starknet deposit",
        "fields": [
          {
            "path": "token",
            "label": "L1 token contract",
            "format": "addressName",
            "params": { "types": ["contract"] },
            "visible": "always"
          },
          {
            "path": "amount",
            "label": "Amount to deposit",
            "format": "tokenAmount",
            "params": { "tokenPath": "token" },
            "visible": "always"
          },
          { "path": "l2Recipient", "label": "Starknet recipient felt", "format": "raw", "visible": "always" },
          { "path": "@.value", "label": "L1 message fee", "format": "amount", "visible": "always" }
        ]
      }
    }
  }
}
