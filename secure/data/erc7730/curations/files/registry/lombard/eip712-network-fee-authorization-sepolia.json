{
  "$schema": "../../specs/erc7730-v2.schema.json",
  "_curation_note": "PQSigner #498 EIP-712 quarantine: the Sepolia feeApproval declaration is retained only as an exact typed-data refusal. The existing bridge-exits evidence pins the StakedLBTC proxy, implementation, runtime, token metadata, and EIP-712 domain name/version, but it does not compactly pin the Sepolia AssetRouter consumer that determines whether signed fee is a maximum ceiling. The descriptor therefore names the value honestly as raw LBTC base units while _pqsigner refuses the only format. Reconsider only after satisfying reason code missing-fixed-block-authority. No trusted leaf, fallback, blind-signing, hardware, production, shipment, or future-upgrade authority is supplied. See tests/erc7730-semantic-evidence/bridge-exits.",
  "_pqsigner": {
    "deploymentFormats": [],
    "refusalOnlyFormats": [
      "feeApproval(uint256 chainId,uint256 fee,uint256 expiry)"
    ]
  },
  "context": {
    "eip712": {
      "deployments": [{ "chainId": 11155111, "address": "0x731eFa688F3679688cf60A3993b8658138953ED6" }],
      "domain": { "name": "Lombard Staked Bitcoin", "version": "1" }
    }
  },
  "metadata": { "owner": "Lombard Finance", "info": { "url": "https://www.lombard.finance/" } },
  "display": {
    "formats": {
      "feeApproval(uint256 chainId,uint256 fee,uint256 expiry)": {
        "intent": "Lombard Network Fee Authorization",
        "fields": [
          { "path": "chainId", "label": "Chain ID", "format": "raw", "visible": "always" },
          { "path": "fee", "label": "Maximum fee (LBTC base units)", "format": "raw", "visible": "always" },
          { "path": "expiry", "label": "Expiry", "format": "date", "params": { "encoding": "timestamp" }, "visible": "always" }
        ]
      }
    }
  }
}
