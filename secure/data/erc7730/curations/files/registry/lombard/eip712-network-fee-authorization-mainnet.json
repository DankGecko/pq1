{
  "$schema": "../../specs/erc7730-v2.schema.json",
  "_curation_note": "PQSigner #498 semantic curation: feeApproval is admitted only for the fixed Ethereum mainnet StakedLBTC proxy below. The signed fee is the maximum LBTC fee ceiling accepted by AssetRouter; the router may charge less through its independently configured maximum-mint-commission cap. The fee display resolves @.to to the exact LBTC metadata leaf (LBTC, 8 decimals) and refuses values that cannot be rendered exactly within the device fraction-digit bound. Evidence pins the proxy/implementation runtimes, official source/compiler closure, EIP-712 domain name/version/deployment, canonical typehash, and maximum-fee consumer semantics. It is historical fixed-block authority, not live commission configuration, signature validity, execution success, future proxy upgrades, fallback, blind signing, hardware, production, or shipment authority. See tests/erc7730-semantic-evidence/lombard-lbtc.",
  "_pqsigner": {
    "deploymentFormats": [
      {
        "chainId": 1,
        "address": "0x8236a87084f8B84306f72007F36F2618A5634494",
        "formats": [
          "feeApproval(uint256 chainId,uint256 fee,uint256 expiry)"
        ]
      }
    ]
  },
  "context": {
    "eip712": {
      "deployments": [{ "chainId": 1, "address": "0x8236a87084f8B84306f72007F36F2618A5634494" }],
      "domain": { "name": "Lombard Staked Bitcoin", "version": "1" }
    }
  },
  "metadata": { "owner": "Lombard Finance", "info": { "url": "https://www.lombard.finance/" } },
  "display": {
    "formats": {
      "feeApproval(uint256 chainId,uint256 fee,uint256 expiry)": {
        "intent": "Lombard Network Fee Authorization",
        "fields": [
          { "path": "chainId", "label": "Chain ID", "format": "raw", "visible": "always" },
          {
            "path": "fee",
            "label": "Maximum LBTC network fee",
            "format": "tokenAmount",
            "params": { "tokenPath": "@.to" },
            "visible": "always"
          },
          { "path": "expiry", "label": "Expiry", "format": "date", "params": { "encoding": "timestamp" }, "visible": "always" }
        ]
      }
    }
  }
}
